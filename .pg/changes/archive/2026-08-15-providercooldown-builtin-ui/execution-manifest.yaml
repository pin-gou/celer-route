schema_version: '2026-06-30'
change: providercooldown-builtin-ui
stages:
- name: dev
  environment: local
  tracks:
  - id: transports
    type: standard
    enabled: true
    reason: 常驻 track（无 on_conditions），LLM 决策启用
    on_conditions_eval:
      matched_rules: []
      unmatched_rules: []
      path_hit_count: 0
      semantic_hit_count: 0
    phase_prompts:
      test:
        tasks_md_section: 1. dev.transports:test - dev 测试先行
      dev:
        tasks_md_section: 2. dev.transports:dev - 实现开发
      review:
        tasks_md_section: 3. dev.transports:review - 静态代码审查
      verify:
        tasks_md_section: 4. dev.transports:verify - dev 集成验证
      gate:
        tasks_md_section: 5. dev.transports:gate - dev 门控审查
  - id: plugins
    type: standard
    enabled: true
    reason: 常驻 track（无 on_conditions），LLM 决策启用
    on_conditions_eval:
      matched_rules: []
      unmatched_rules: []
      path_hit_count: 0
      semantic_hit_count: 0
    phase_prompts:
      test:
        tasks_md_section: 6. dev.plugins:test - dev 测试先行
      dev:
        tasks_md_section: 7. dev.plugins:dev - 实现开发
      review:
        tasks_md_section: 8. dev.plugins:review - 静态代码审查
      verify:
        tasks_md_section: 9. dev.plugins:verify - dev 集成验证
      gate:
        tasks_md_section: 10. dev.plugins:gate - dev 门控审查
  - id: ui
    type: standard
    enabled: true
    reason: 常驻 track（无 on_conditions），LLM 决策启用
    on_conditions_eval:
      matched_rules: []
      unmatched_rules: []
      path_hit_count: 0
      semantic_hit_count: 0
    phase_prompts:
      test:
        tasks_md_section: 11. dev.ui:test - dev 测试先行
      dev:
        tasks_md_section: 12. dev.ui:dev - 实现开发
      review:
        tasks_md_section: 13. dev.ui:review - 静态代码审查
      verify:
        tasks_md_section: 14. dev.ui:verify - dev 集成验证
      gate:
        tasks_md_section: 15. dev.ui:gate - dev 门控审查
- name: int
  environment: local
  tracks:
  - id: scr
    type: scenario
    enabled: true
    reason: 跨 transports + plugins + ui 三模块联调：UI 切换 Switch → REST 写入 → bifrost 内部加载
      → KeyPoolFilter 绑定。属跨 role 协作 + 跨模块联调，启用 scenario track 验证 KeyPoolFilter 静默失效排查
    on_conditions_eval:
      matched_rules: []
      unmatched_rules: []
      path_hit_count: 0
      semantic_hit_count: 0
    phase_prompts:
      scenario-execute:
        tasks_md_section: 16. int.scr:scenario-execute - 真机场景执行
    scenario_yaml: scenario-scr.yaml
final_gate:
  tasks_md_section: 17. final-gate - 最终门控审查
