## 任务：dev.ui

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.ui
- track.modules: ['ui']
- track.max_fix_retries: 5

### Module 配置
  - module: ui
    - root: ui
    - language: typescript
    - build: npm run build
    - lint: npm run lint
    - test.unit: npx vitest run

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: npx vitest run


---

### 门控审查要求（v2.1 加权评分）

审查上一轮 verify 报告，逐项核查 tasks.md 的 G-* 门控项，
**并对每个维度独立打分**（0-100），最后综合计算 gate-score。

### 🔴 v3.x 新增：实现完整性维度（10%，P0 硬约束）

**必须**对 `git diff feat/pg/strip-enterprise-features` 单独执行实现完整性核查。
这是 v3.x 新增的硬约束，防止"代码存在但未实质实现"绕过 verify 阶段的 MockMvc 测试。

执行命令：

```bash
# 硬标记扫描
git diff feat/pg/strip-enterprise-features | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException' | grep -v '\[skip-review\]'

# tasks-V 映射核查（人工提取 tasks.md 关键 API 关键词，在 diff 中 grep）
```

判定：

- 命中 ≥ 1 处（除豁免）→ 维度分 = 0，整个 gate FAIL
- 豁免 ≥ 3 → 维度分 = 0，整个 gate FAIL
- 完全 PASS → 维度分 = 100

### 加权评分维度

| 维度 | 权重 | P0 失败条件（即使总分高也 FAIL） |
|------|------|-------------------------------|
| V-* 验证项通过率 | 50% | 任何 V-* FAIL |
| design.md 一致性 | 20% | 缺核心 API/DTO 改动 |
| scope creep 检查 | 15% | 修改了未在 proposal 声明的文件 |
| 测试质量 | 10% | 新代码无单测覆盖 |
| **实现完整性**（v3.x） | **10%** | **diff 中存在 TODO/stub 或 task-V 缺失 → 整体 FAIL** |
| **runtime_boot**（B3 修复） | — | **verify 报告缺"运行时启动+健康探针"4 步证据 → 整体 FAIL** |

> v3.x 起实现完整性维度占 10% 权重，原"测试质量"维度相应降为可选 0%（合并到实现完整性）。
> **runtime_boot**（B3 修复）：无独立权重，但作为 P0 硬约束——若 verify 报告
> 缺少"启动服务 / 等待就绪 / 真实 e2e / 失败处置"4 步中任一步的原始输出或
> 日志片段，则 gate 直接 FAIL。防止"代码存在但装配失败"（如多构造函数缺
> @Autowired）漏到集成环境。

### gate-score 计算

```
gate-score = Σ(维度分 × 权重)  (0-100)
PASS 判定: gate-score ≥ 80 AND 无 P0 失败
FAIL 判定: gate-score < 80 或 存在 P0 失败
```

### 判定

- **PASS**：gate-score ≥ 80 且无 P0 失败，返回 `gate pass`
- **FAIL**：gate-score < 80 或存在 P0 失败，返回 `gate fail`（需列出具体 G-N 项 + 维度分）
- **🔴 P0 触发条件**（v3.x）：实现完整性维度 FAIL → 无论 gate-score 多高，整体 FAIL

### 返回契约

除 Sub-agent 返回契约的 6 字段外，还必须包含：
- 在 summary 中附加: `"gate_score: <0-100>, p0_failures: [G-N, ...]"`
- 在 report_path 文件中提供每个维度的分数明细
- **必须**含实现完整性维度的 grep 输出与豁免列表

### 写盘要求

完成审查后，自行用 cat > 写盘到 2-build/033-dev.ui-gate.md。
格式：Markdown，包含：
- 每个 G-* 项的结论（PASS / FAIL）及依据
- 每个维度的分数（0-100）和权重
- 最终 gate-score 和 PASS/FAIL 判定
- **实现完整性核查段**：含 grep 输出、豁免列表、tasks-V 映射核查结论

---

### 执行要求

**任务清单**：
## 25. dev.ui:gate - dev 门控审查

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=ui (常驻, 无 on_conditions)
-->

- 无

**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-ui-1 | 无 @enterprise 引用残留 | ui/ 目录就绪 | `grep -r '@enterprise' ui/ \| wc -l` | 命中数为 0（除 rbac.ts 中的 RbacResource 字面定义） |
| V-ui-2 | UI build 通过 | node_modules 已安装 | `cd ui && npm run build` | 退出码 0，产出 out/ |
| V-ui-3 | UI lint 与 typecheck 通过 | node_modules 已安装 | `cd ui && npm run lint && npm run typecheck` | 退出码 0，无错误 |
| V-ui-4 | enterprise 路由不存在 | ui/ 目录就绪 | `ls ui/app/workspace/{rbac,scim,audit-logs,alerting,cluster,guardrails,circuit-breaker,mcp-tool-groups,mcp-auth-config,access-profiles,business-units,user-rankings}` | 所有目录不存在 |
| V-ui-5 | 侧边栏无 enterprise 菜单项 | ui/ 目录就绪 | `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` | 命中数为 0 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：gate）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | **必填**（报告文件绝对路径，文件必须已写盘） |
| `--evidence` | **至少 1 个**（证据文件绝对路径，可多次传） |
| `--tasks-updated` | 选填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`gate` | `pass` / `fail` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase gate \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/033-dev.ui-gate-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/033-dev.ui-gate-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/033-dev.ui-gate-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation