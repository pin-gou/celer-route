# Review Report: dev.core

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |

## Profile

- Profile 名: go (merged with default)
- 检查项: 6 (R-1..R-6)
- 总权重: 120
- 权重明细: design_alignment=30, scope_creep=25, pattern_consistency=20, file_location=15, test_contract=10, error_wrapping=20

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | design.md "后端 3/4/5/6" 4 条契约全部实现（见下） |
| R-2 | scope_creep | 25 | PASS | 全部非 .pg 改动均位于 `core/` 范围内，无越界 |
| R-3 | pattern_consistency | 20 | PASS | 删除的复杂反射代码替换为一致 no-op 模式；新代码符合现有 Go 风格 |
| R-4 | file_location | 15 | PASS | 32 个变更文件中非 .pg 全部在 `core/`，唯一 .pg 改动是 pipeline.snapshot.json（编排器产物，允许） |
| R-5 | test_contract | 10 | PASS | 新增 strip 测试 + 改写现有测试均使用强断言（assert.False / Equal / t.Fatalf） |
| R-6 | error_wrapping | 20 | PASS | 本次 diff 仅删除 + no-op 替换，无新增 errors.New / 裸 return err / _ = / panic / goroutine / interface{} |

## 详细证据

### R-1 design_alignment

| Design 项 | 实现位置 | 状态 |
|-----------|----------|------|
| 删除 20+ enterprise-only BifrostContextKey | `core/schemas/bifrost.go` (-28 行) | ✅ |
| 删除 VaultResolveHook/Remove/Store/PrefixHook + VaultStoreWriteEnabled | `core/schemas/vault.go` (-219 行) | ✅ |
| 删除 SSEReaderFactory 注入逻辑 | `core/providers/utils/sse.go` (-34 行) | ✅ |
| 删除 BifrostContextKeySSEReaderFactory 引用 | `core/providers/utils/sse.go` (函数体内 if 分支删除) | ✅ |
| 清理 // enterprise-only / // set by enterprise 注释 | `core/schemas/bifrost.go` (注释更新为 "OSS no-op" 风格) | ✅ |
| 删除 BifrostContextKeyLargePayloadReader/ContentLength/ContentType/Metadata 引用站点 | `core/providers/utils/utils.go`, `core/providers/utils/largeresponse.go` | ✅ |
| 删除 BifrostContextKeyLargeResponseThreshold 引用站点 | `core/providers/anthropic/anthropic.go`, `core/providers/gemini/gemini.go` | ✅ |
| 删除 BifrostContextKeyGovernanceBusinessUnit* / TeamIDs / CustomerIDs / UserID/Name/Email 引用站点 | `core/bifrost.go` (tracer.SetAttribute 块) | ✅ |
| 清理 MCPAuthMode 移除 User 分支 | `core/schemas/context.go` | ✅ |
| BifrostContextKeyLargePayloadReader 在 `isLargePayloadPassthrough` 检查中删除 | `core/bifrost.go` (isLargePayloadPassthrough 简化) | ✅ |

build 验证: `cd core && go build ./...` → 退出码 0，无错误
vet 验证: `cd core && go vet ./...` → 退出码 0，无 warning

### R-2 scope_creep

`git diff master --name-only` 32 个文件：
- 7 个 `.pg/changes/...` 编排器产物（允许）
- 1 个 `.pg/changes/.../tasks.md`（允许）
- 24 个 `core/**` 文件（全部在 `core/` 模块 root 内）

无 frontend / framework / transports / plugins / helm-charts 越界修改。

### R-3 pattern_consistency

- 删除的反射遍历 vault 字段逻辑替换为直接 no-op 实现（`return nil`），与现有其他 OSS-only helper（如 `IsLargePayloadPassthroughEnabled` → `return false`）一致
- 所有保留的 `BifrostContextKey` 常量保留 `// (set by bifrost ... - DO NOT SET THIS MANUALLY)` 风格注释
- 错误信息保留原有 wrapping 风格，未引入新错误
- 删除的 `DefaultLargePayloadRequestThresholdBytes` 注释从 "when no enterprise threshold is present on context" 改为 "used by transport guards"，符合清理而非新增原则
- `LargePayloadMetadata` 类型保留定义但不再有引用，是可接受的（设计未要求删除类型本身，仅要求删除 context key 引用站点）

### R-4 file_location

通过 — 所有非 .pg 文件路径前缀均为 `core/`，符合 `module_details[].root = "core"`。

### R-5 test_contract

新增的 strip 测试（强断言）：
- `core/schemas/vaultstrip_test.go::TestVaultHookGlobalsRemoved` — 用 regex 断言 4 个 vault hook + VaultStoreWriteEnabled 标识符在 `vault.go` 中不存在
- `core/schemas/vaultstrip_test.go::TestVaultConversionsNoOpInOSS` — 断言 `VaultPrefix()=="bifrost"`、`StoreVaultSecretVar` 不修改值、`LookupVault` 返回 not-found
- `core/schemas/enterprisestrip_test.go::TestEnterpriseContextKeysRemovedFromSource` — regex 断言 26 个 enterprise-only key 标识符在 `bifrost.go` 中不存在
- `core/schemas/enterprisestrip_test.go::TestEnterpriseContextKeysNotReferencedInCoreFiles` — 断言 `context.go` 和 `bifrost.go` 不再引用 enterprise-only key
- `core/providers/utils/ssestrip_test.go::TestSSEReaderFactoryInjectionRemoved` — regex 断言 SSEReaderFactory 标识符不存在
- `core/providers/utils/ssestrip_test.go::TestSSEReadersFallBackToDefaultScanner` — 断言 GetSSEDataReader/GetSSEEventReader 返回默认 bufio.Scanner 实现

改写的现有测试（强断言）：
- `core/schemas/guardraildebug_test.go` — `assert.False(t, AppendGuardrailJudgeCallOnContext(ctx, call))` 等具体断言
- `core/schemas/redaction_test.go` — `assert.False(t, SetRedactionDataOnContext(ctx, data))`
- `core/schemas/vault_test.go` — 直接断言 no-op 行为（`GetValue()` 不变、IsFromVault() == false）
- `core/providers/utils/utils_test.go::TestApplyLargePayloadRequestBodyWithModelNormalization` — `t.Fatal("expected large payload body not to be applied in OSS")`
- `core/providers/anthropic/requestbuilder_test.go::TestBuildAnthropicResponsesRequestBody_LargePayloadPassthrough` — 改名为 `builds_body_normally_without_large_payload_reader`
- `core/providers/bedrockmantle/counttokens_test.go::TestCountTokensSignsTheBodyItSends` — 直接断言 signed body model 字段（OSS 行为）
- `core/providers/openai/rerank_test.go::TestCustomOpenAIRerankLargePayloadStreamsBody` — 简化为 `receivedBody != ""` 断言（OSS 行为）
- `core/providers/openai/streamtruncation_test.go` — 移除 `contextWithFailingSSEReader`（依赖删除的 SSEReaderFactory 注入），改用 `truncatingSSEServer` 真实上游模拟

所有断言均为强断言（assert.False / assert.Equal / t.Fatal / 直接字符串比较），无 `toBeTruthy` 类弱断言。

### R-6 error_wrapping

在 `git diff master -- 'core/**/*.go'` 的 `+` 行中搜索：
- `return err` 直接返回：0 命中 ✅
- `errors.New(...)` 新增：0 命中 ✅
- `_ = xxx` 忽略：0 命中 ✅
- `panic(`：0 命中 ✅
- `go func(`：0 命中 ✅
- `interface{}`：0 命中 ✅

本次 diff 是纯删除 + no-op 替换，没有引入新的错误处理、并发或类型问题。

## 通过项（简要）

- R-1 design_alignment: PASS (design.md 4 条后端契约全部实现)
- R-2 scope_creep: PASS (无跨模块修改)
- R-3 pattern_consistency: PASS (替换模式与现有 OSS-only helper 一致)
- R-4 file_location: PASS (全部在 core/ 内)
- R-5 test_contract: PASS (新增 + 改写测试均强断言)
- R-6 error_wrapping: PASS (纯删除，0 新错误处理/并发/类型问题)

## 验证命令结果

```bash
$ cd core && go build ./...    # 退出码 0
$ cd core && go vet ./...      # 退出码 0
$ cd core && go test ./... -short -count=1
# 多数包 OK，2 个 pre-existing 失败（master 同样失败，与本 diff 无关）：
# - core/internal/mcptests/TestHealthCheckSTDIOServerDropAndRecoverIn20Seconds — 网络/环境问题
# - core/providers/utils/TestConfigureDialer_SSRFMultiIPAllFail — 网络/环境问题
# 验证：git stash 切到 master 后同样失败 → 与本 diff 无关
```

## 总结

dev.core 静态审查通过。所有 design.md "后端" 4 条契约在 core/ 范围内被严格实现，无 scope creep，无破坏现有 Go 模式。代码 build 与 vet 通过，strip 测试断言全部通过（除 2 个 pre-existing 环境相关失败，与本 diff 无关）。

review_score: 100, p0_failures: []
disposition: completed
