# Gate Assessment: int.ui

**Track**: int.ui
**Change**: strip-enterprise-features
**Date**: 2026-08-13
**Cycle**: 1

## 整体判定

PASS — gate-score = 95（≥ 80），无 P0 失败

## 检查项

| # | 检查项 | 判定 | 证据来源 |
|---|--------|------|---------|
| 1 | 本地 V-* 全部通过或已豁免 | ✅ | report: V-ui-1..5 全部 PASS |
| 2 | 交付物与 design.md 一致 | ✅ | 步骤 2.5: 结构化 diff 13/13 匹配 |
| 3 | 无 scope creep | ✅ | 步骤 3: 231 文件全在 ui/ 根，无越界 |
| 4 | 文件位置合规（ui/ 根） | ✅ | git diff --name-only: 全部前缀 ui/，无越界 |
| 5 | Evidence 充分 | ✅ | 每个 V-* 含原始输出；5/5 有证据 |
| 6 | 旧测试无回归 | ✅ | 步骤 1.6: 预存失败 1 个（columns.test.ts 预存）/ 新引入 0 个 |
| 7 | 证据可追溯（抽查） | ✅ | 步骤 1.5: 2/2 项证据可追溯 |
| 8 | 测试质量 | ✅ | 4 test suites, 强断言, 正反路径覆盖, 业务场景命名 |
| 9 | 实现完整性（v3.x P0 维度） | ✅ | ui/ diff 0 TODO/stub；tasks-V 映射通过 |
| 10 | runtime_boot（P0 硬约束） | ✅ | verify 报告含 4 步证据（启动/就绪/健康探针/失败处置） |

## 加权维度评分（v2.1 / v3.x）

| 维度 | 权重 | 维度分 | 加权得分 |
|------|------|--------|---------|
| V-* 验证项通过率 | 50% | 100 | 50 |
| design.md 一致性 | 20% | 100 | 20 |
| scope creep 检查 | 15% | 100 | 15 |
| 测试质量（v3.x 合并至实现完整性） | 0% | (100 参考) | 0 |
| 实现完整性（v3.x） | 10% | 100 | 10 |
| **gate-score** | | | **95** |

gate-score = 95 ≥ 80，无 P0 失败 → **PASS**

## 不通过项详细说明

无（10/10 检查项 PASS，无 FAIL）。

## 步骤 1.5 证据抽查

### 抽查 1: V-ui-1 — 无 @enterprise 引用残留
- **verify evidence**: `grep -rn '@enterprise' ui/ | grep -v rbac.ts` → 0 命中（仅 test 文件字面量）
- **反向追溯**: 直接 grep `ui/` 下所有 .ts/.tsx 文件：
  - `grep -rn '@enterprise' ui/ --include='*.ts' --include='*.tsx' | grep -v rbac.ts | grep -v enterprise-stripping.test.ts` → 0 命中
  - 确认 `ui/app/_fallbacks/enterprise/` 整个目录已删除（`ls` → "No such file or directory"）
- **结论**: 与 verify 证据完全一致 — 仅 rbac.ts 字面定义 + 测试文件自身断言 → **可追溯** ✅

### 抽查 2: V-ui-5 — 侧边栏无 IS_ENTERPRISE 菜单项
- **verify evidence**: `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` → No matches found
- **反向追溯**: 直接 grep sidebar.tsx：`grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` → 0 命中 ✅
- **结论**: 与 verify 证据一致 → **可追溯** ✅

「2/2 项证据可追溯」

## 步骤 1.6 回归分析校验

- verify 报告中 1 个 FAIL 测试：`app/workspace/logs/views/columns.test.ts` — 该文件 `git diff` 显示无改动（0 output），为预存 vitest 配置问题，非本变更引入
- 其余 9 个测试文件全部 PASS，214 tests 全部通过
- 预存失败 1 个（columns.test.ts）/ 新引入 0 个

## 步骤 2.5 design.md 一致性（结构化 diff）

design.md 中明确属于 int.ui track 的承诺：

| # | design.md 期望 | 实际代码 | 判定 |
|---|---------------|---------|------|
| 1 | `ui/lib/rbac.ts` — useRbac() 返回 true + RbacResource/RbacOperation enum | 文件存在，useRbac() 返回 true，枚举含 OSS 子集 | ✅ |
| 2 | `ui/app/_fallbacks/enterprise/` 整个目录删除 | `ls` → "No such file or directory" | ✅ |
| 3 | `tsconfig.json` 中 `@enterprise/*` 与 `@schemas/*` 别名删除 | grep 确认 0 命中 | ✅ |
| 4 | `vite.config.mts` 中 enterprise alias 与 BIFROST_IS_ENTERPRISE 删除 | diff 确认 `-@enterprise`、`-@schemas`、`-BIFROST_IS_ENTERPRISE` 已删除 | ✅ |
| 5 | 30+ enterprise 路由 page.tsx/layout.tsx 删除 | 12 个 enterprise workspace 目录全部不存在 | ✅ |
| 6 | sidebar.tsx 无 IS_ENTERPRISE 条件菜单项 | grep 0 命中 | ✅ |
| 7 | `customerSelector.tsx` 与 `teamSelector.tsx` 删除 | 文件不存在 | ✅ |
| 8 | `apiKeysIndexView.tsx` 搬到 `config/api-keys/views/apiKeysView.tsx` | 文件在目标路径存在 | ✅ |
| 9 | `loginView.tsx` 搬到 `ui/app/login/views/loginView.tsx` | 文件在目标路径存在 | ✅ |
| 10 | virtualKeySheet.tsx 移除 customer/team picker | grep 确认 `CustomerSelector`/`TeamSelector` 0 引用 | ✅ |
| 11 | routingRuleSheet.tsx scope 收窄为 virtual_key/global | `ROUTING_RULE_SCOPES` 仅含 `Global` + `VirtualKey` | ✅ |
| 12 | RoutingRuleScope enum 仅含 global + virtual_key | enum 定义仅 2 值 | ✅ |
| 13 | 无 @enterprise import 引用残留 | grep 全 ui/ 0 命中（除 rbac.ts 字面 + test 断言） | ✅ |

「期望 13 项 / 实际匹配 13 项 / 缺失 0 项」

## 步骤 3 scope creep + 文件位置合规

`git diff db1ef287..HEAD --name-only` 中 `ui/` 前缀的文件共 231 个，全部在 `ui/` 模块根下。无越界文件。

**ui/ 修改总览**：
- 231 files changed, 343 insertions(+), 3443 deletions(-)
- 纯删除为主（-3443 行），新增 343 行主要为测试文件、rbac.ts 本地实现、搬出文件
- 最大单文件修改：`virtualKeySheet.tsx` 360 行（+9/-351，纯删除 customer/team picker 段）→ 净减少，合理
- 新增文件：
  - `ui/lib/enterprise-stripping.test.ts` — 任务 46.1 测试文件 ✅
  - `ui/lib/rbac.ts` — 任务 47.8 本地 rbac 实现 ✅
  - `ui/app/workspace/config/api-keys/views/apiKeysView.tsx` — 搬出 fallback ✅
  - `ui/app/login/views/loginView.tsx` — 搬出 fallback ✅
- 无超 200 行新增型改动（最大新增 97 行 — enterprise-stripping.test.ts）
- 无 scope creep：所有变更均在 proposal「包含」段 + tasks.md ##47 / ##48 范围内

## 步骤 4 Evidence 充分性

- V-ui-1: `grep -rn '@enterprise'` 原始输出 ✅
- V-ui-2: `npm run build` 原始输出 (exit=0, 2.00s) ✅
- V-ui-3: `npm run lint && npm run typecheck` 原始输出 ✅
- V-ui-4: 12 个目录逐项检查原始输出 ✅
- V-ui-5: `grep IS_ENTERPRISE sidebar.tsx` 原始输出 ✅
- 无 SKIP 项，每项有原始证据
- 测试结果: 214 passed, 1 pre-existing failure (columns.test.ts) — 含日志摘要

## 步骤 5 测试质量

- `ui/lib/enterprise-stripping.test.ts`：4 个 describe × 1 test
  - 断言均为强断言：`toEqual([])`, `toBe(false)`, `not.toContain("IS_ENTERPRISE")` ✅
  - 无弱断言（无 `toBeDefined`/`toBeTruthy`） ✅
  - 测试函数名反映业务场景：`no @enterprise references remain`、`fallbacks/enterprise directory deleted`、`enterprise workspace routes removed`、`no IS_ENTERPRISE in sidebar` ✅
  - 正反路径覆盖：每项同时验证"存在即 fail"语义 ✅

「测试质量：4/4 项符合标准，无 WARNING」

## runtime_boot（B3 修复）4 步证据核查

| 步骤 | 要求 | verify 报告中的证据 | 判定 |
|------|------|-------------------|------|
| 1. 启动服务 | restart_all_instances 原始输出 | evidence: `Stopping ui-dev (PID 1015942)...` / `Stopping bifrost-api (PID 1015869)...` / `重新构建 bifrost-http...` / `bifrost-api is ready on port 9080` / `ui-dev is ready on port 3008` | ✅ |
| 2. 等待就绪 | 轮询探针日志 | `pg_http_health_check: OK — /health returned HTTP 200` / `ui-dev is ready on port 3008` | ✅ |
| 3. 真实 e2e | ≥1 次核心链路触发 | `curl -s http://localhost:9080/health` → `{"components":{"db_pings":"ok"},"status":"ok"}`; `curl -s http://localhost:3008/` → HTML 200 | ✅ |
| 4. 失败处置 | 启动失败处理记录 | 无失败：服务首次启动即就绪，无探针超时，无重试记录 | ✅ |

runtime_boot PASS — 4 步证据齐备，无"代码存在但装配失败"迹象。

## 实现完整性核查段（v3.x P0 维度）

### 硬标记扫描（`git diff db1ef287..HEAD -- ui/`）

```
$ git diff db1ef287..HEAD -- ui/ | grep -nE '^\+.*(TODO|FIXME|XXX|stub|not.implemented)' | grep -v '\[skip-review\]'
(0 命中)
```

ui/ diff **0 个**新增 TODO/FIXME/XXX/stub/not-implemented 标记。✅

### 豁免列表

| 范围 | 命中数 | 来源 | 判定 |
|------|--------|------|------|
| `ui/` 代码 diff | 0 | — | 无需豁免 |
| 全分支 `.pg/` 文档 | 多处 | dispatch/gate/review 模板描述文本 | 豁免（非生产代码） |
| 全分支非 `ui/` 非 `.pg/` 代码 | 0 | — | 其他 track 范围，非本 track 管辖 |

### tasks-V 映射核查

| tasks.md 关键 API 关键词 | 代码状态 | 确认方式 |
|--------------------------|---------|---------|
| `@enterprise` import 清理（47.9） | ✅ 已清理 | grep 全 ui/ 0 命中（除 rbac.ts 字面 + test 断言） |
| `ui/app/_fallbacks/enterprise/` 删除（47.1） | ✅ 已删除 | `ls` 确认目录不存在 |
| 30+ enterprise 路由删除（47.4） | ✅ 已删除 | 12 个 workspace 子目录全部不存在 |
| `IS_ENTERPRISE` 常量删除（47.13） | ✅ 已删除 | grep sidebar.tsx 0 命中 |
| sidebar.tsx 3 个条件菜单项删除（47.12） | ✅ 已删除 | grep sidebar.tsx 无 Proxy/Branding/License 条件块 |
| customer/team selector 删除（47.7） | ✅ 已删除 | 文件不存在，VK sheet 无引用 |
| scope 收窄 virtual_key/global（47.11） | ✅ 已实现 | ROUTING_RULE_SCOPES 仅 2 值 |
| V-ui-1 (no @enterprise) | ✅ PASS | grep 验证 |
| V-ui-2 (UI build) | ✅ PASS | npm run build exit=0 |
| V-ui-3 (lint/typecheck) | ✅ PASS | npm run lint exit=0, npm run typecheck exit=0 |
| V-ui-4 (enterprise routes deleted) | ✅ PASS | 12 目录全部不存在 |
| V-ui-5 (sidebar no IS_ENTERPRISE) | ✅ PASS | grep 0 命中 |

**tasks-V 映射核查结论**: 全部 11 项已确认通过 ✅

## 维度分明细

- V-* 通过率: 100（V-ui-1..5 PASS，0 FAIL）
- design.md 一致性: 100（结构化 diff 13/13 匹配）
- scope creep: 100（231 文件全在 ui/，纯删减，无越界）
- 测试质量（参考，权重 0%）: 100
- 实现完整性: 100（ui/ diff 0 stub/TODO，tasks-V 映射通过）
- runtime_boot: PASS（4 步证据齐备）
- **gate-score = 50×1.0 + 20×1.0 + 15×1.0 + 0×1.0 + 10×1.0 = 95 ≥ 80** → **PASS**
