## 任务：dev.framework

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.framework
- track.modules: ['framework']
- track.max_fix_retries: 5

### Module 配置
  - module: framework
    - root: framework
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: go test ./... -short -count=1
    - test.integration: make test-framework

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: go test ./... -short -count=1


---

- stage.environment.instances:
```yaml
bifrost-api:
- name: bifrost-api-1
  host: localhost
  port: 9080
ui-dev:
- name: ui-dev-1
  host: localhost
  port: 3008
```

- stage.environment.hooks:
```yaml
bifrost-api:
  start:
    host: localhost
    script: .pg/hooks/bifrost-api-start.sh
  stop:
    host: localhost
    script: .pg/hooks/bifrost-api-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/bifrost-api-health-check.sh
ui-dev:
  start:
    host: localhost
    script: .pg/hooks/ui-dev-start.sh
  stop:
    host: localhost
    script: .pg/hooks/ui-dev-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/ui-dev-health-check.sh
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session strip-enterprise-features --env local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---

### 实现要求

本阶段需要根据 design.md 实现功能代码。遵循以下原则：
- 只创建和修改生产代码
- 不删除已有测试
- 确保测试仍然通过
- 如果现有测试需要修改以适配新实现，那也是允许的

### 模块路径约束

禁止编辑以下路径的文件：
- 不在 ['framework'] 路径下的文件

### 完善清单

- [ ] 所有 checkstyle / lint 检查 0 警告
- [ ] 所有单元测试 PASS
- [ ] 集成测试 PASS（如环境可用）

---

### 执行要求

**任务清单**：
## 7. dev.framework:dev - 实现开发

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=framework (常驻, 无 on_conditions)
-->

- [ ] 7.1 删除 `framework/featureflags/featureflags.go` 中 `EnterpriseOnly bool` 字段、`ErrFlagEnterpriseOnly` 错误常量
- [ ] 7.2 删除 `framework/featureflags/registry.go` 中 `EnterpriseOnly` 分支
- [ ] 7.3 删除 `framework/featureflags/featureflags.go` 中 `SyncDelegate` 接口与 `Store.delegate` / `delegateM` 字段
- [ ] 7.4 清理 `framework/` 目录下 enterprise 注释
- [ ] 7.5 跑 `cd framework && go build ./...` 与 `go vet ./...` 验证通过

**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-framework-1 | Go 模块 build 通过 | go.work 已就绪 | `cd framework && go build ./...` | 退出码 0，无错误 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：dev）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（实现的 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`dev` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase dev \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/007-dev.framework-dev-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/007-dev.framework-dev-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/007-dev.framework-dev-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation