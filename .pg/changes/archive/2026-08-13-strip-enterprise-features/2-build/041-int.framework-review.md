# Review Report: int.framework

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 80 |
| disposition | completed |

## Profile

- Profile 名: default + go (merged, language=go)
- 检查项: 6 (design_alignment, scope_creep, pattern_consistency, file_location, test_contract, error_wrapping)
- 总权重: 120
- pass_threshold: min(80, 85) = 80

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | framework/featureflags/ 按 design.md 删除 EnterpriseOnly / ErrFlagEnterpriseOnly / SyncDelegate / ScrvStore.delegate / delegateM / isEnterprise / Config.IsEnterprise / FlagStatus.EnterpriseOnly；registry.go 删除 enterprise-only 分支；vault 全局 hook 删除后 configstore 的 vault 存储调用同步移除（key.go/virtualkey.go/vault_callbacks.go no-op）；oauth2 移除 MCPAuthModeUser 分支。design.md framework 契约 100% 完成 |
| R-2 | scope_creep | 25 | PASS | 全部变更文件均在 framework/ root 下。modelcatalog/datasheet/types.go、oauth2/main.go、configstore/vault_callbacks.go 等均属 enterprise/vault 功能移除的连带清理，在 design.md 契约范围内 |
| R-3 | pattern_consistency | 20 | PASS | go build/go vet 通过；无 panic 滥用、无 interface{}；错误统一 %w wrapping；struct tag 完整。**WARN**: featureflags.go L141/L163-164/L200-201 残留 3 处 "delegate is NOT invoked / Restore does NOT fire gossip delegate" 过期注释（对应已删除的 SyncDelegate，属 task 7.4 清理不彻底）；enterprisestrip_test.go 与 vault_callbacks.go 缺文件末尾换行（gofmt 会补）。oauth2/main.go L1342 `uid` 变量成为死代码（不再赋值但仍写入 TableMCPOauthFlow.UserID）。均不阻断 |
| R-4 | file_location | 15 | PASS | 变更文件全在 framework/ 下，无越界 |
| R-5 | test_contract | 10 | PASS | enterprisestrip_test.go 用源码扫描断言 5 个 enterprise 符号残留（EnterpriseOnly/ErrFlagEnterpriseOnly/SyncDelegate/isEnterprise/SetDelegate），featureflags_test.go 更新行为测试（TestStrip_IsEnabledReturnsDefaultForAllFlags / TestStrip_SetAcceptsPreviouslyEnterpriseOnly），删除 vault_callbacks_test.go（覆盖已删除功能）。go test -short 通过 |
| R-6 | error_wrapping | 20 | PASS | featureflags.go Set/Status 均用 `fmt.Errorf("%w: %q", sentinel, id)` 正确 wrapping；无错误吞噬、无字符串比较、sentinel error 集中在 featureflags.go |

## FAIL 项详细

无 FAIL 项。

## 通过项（简要）

- R-1 design_alignment: PASS — 所有 design.md framework 契约已实现
- R-2 scope_creep: PASS — 无跨模块修改
- R-3 pattern_consistency: PASS（3 处过期注释 + 2 处缺末尾换行 + 1 处死代码为 WARN 级清理项，不阻断）
- R-4 file_location: PASS
- R-5 test_contract: PASS — 契约测试覆盖 enterprise 符号删除
- R-6 error_wrapping: PASS

## 未启用检查项说明

- implementation_completeness：本 profile 未启用（enabled: false），无可报告证据行。
- P0 硬约束清单：本 track 无 p0:true 检查项。

## WARN 项（建议后续 cleanup，不阻断本次 review）

1. `framework/featureflags/featureflags.go` L141/L163-164/L200-201：3 处 "delegate is NOT invoked / Restore does NOT fire the gossip delegate" 注释引用了已删除的 SyncDelegate/delegate 机制，建议清理为中性表述（task 7.4 enterprise 注释清理的一部分）。
2. `framework/featureflags/enterprisestrip_test.go` 与 `framework/configstore/vault_callbacks.go`：文件末尾缺换行，`gofmt -w` 可自动修复。
3. `framework/oauth2/main.go` L1342 `uid` 变量：删除 MCPAuthModeUser 分支后不再被赋值，但仍在 L1425 写入 TableMCPOauthFlow.UserID；当前为恒 nil 的死代码，建议确认后再决定是否保留该赋值。
