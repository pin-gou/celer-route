# Review Report: dev.plugins

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |
| p0_failures | [] |

## Profile

- Profile 名: go (inherits default)
- 检查项: 6 enabled (design_alignment, scope_creep, pattern_consistency, file_location, test_contract, error_wrapping)
- 总权重: 120 (30 + 25 + 20 + 15 + 10 + 20)
- pass_threshold: 85

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | design.md 中 plugins 阶段承诺 7/7 已实现（5 个代码改动 + 1 个测试夹具 + 1 个回归测试新增） |
| R-2 | scope_creep | 25 | PASS | 14 个变更文件全部在 `plugins/` 下，无跨模块改动 |
| R-3 | pattern_consistency | 20 | PASS | 纯删减 + 注释清理，无 panic / interface{} / 错误吞掉 / goroutine 泄漏引入 |
| R-4 | file_location | 15 | PASS | 全部文件路径前缀为 `plugins/<module>/`，符合 module root |
| R-5 | test_contract | 10 | PASS | 新增 `enterprisestrip_test.go` 含 2 个强断言；`TestStrip_*` 用 `t.Errorf` 严格断言删除状态；既有测试断言保持严格 |
| R-6 | error_wrapping | 20 | PASS | 改动不引入新 error 路径；删除路径含原 `errors.Is/As` 调用点保留，无 `_ =` 忽略 |

## R-1 design_alignment — 详细证据

design.md 中 plugins 阶段承诺的 7 项实现逐项核对（task 17.1 + 17.2 + V-plugins-1）：

| Design 项 | 文件 | 状态 |
|----------|------|------|
| 清理 `plugins/governance/` 下 `// enterprise-only` / `// set by enterprise` / `// OSS no-op` 注释 | `plugins/governance/main.go`（5 处）<br>`plugins/governance/store.go`（14 处）<br>`plugins/governance/resolver.go`（2 处）<br>`plugins/governance/tracker.go`（2 处）<br>`plugins/governance/customer_scope_test.go`（4 处） | ✅ 全部清理或改写为中性表述 |
| 跑 `for d in plugins/*/; do (cd "$d" && go build ./...); done` 验证通过 | 所有 12 个 module | ✅ 12/12 build OK |
| 删除 `Config.IsEnterprise` 字段与 `p.isEnterprise` 内部字段 | `plugins/governance/main.go:39-44` (Config)<br>`plugins/governance/main.go:85-92` (struct field)<br>`plugins/governance/main.go:234-241` (Init 赋值)<br>`plugins/governance/main.go:330-337` (InitFromStore 赋值) | ✅ 4 处全部删除 |
| 删除 enterprise-only `BifrostContextKeyUserID` 在 governance main 的引用 | `plugins/governance/main.go:728` (RoutingContext)<br>`plugins/governance/main.go:1265-1305` (PreLLMHook userID)<br>`plugins/governance/main.go:1346-1348` (PostLLMHook userID)<br>`plugins/governance/main.go:1431-1395` (PreMCPHook userID) | ✅ 4 处全部删除（替换为常量空串） |
| 删除 enterprise-only `BifrostContextKeyGovernanceScopedCustomerID` 在 governance store 的引用 | `plugins/governance/store.go:3220-3222` (collectRateLimitsFromHierarchy)<br>`plugins/governance/store.go:3317-3320` (collectBudgetsFromHierarchy) | ✅ 2 处删除（变量改为空串，注释简化） |
| 删除 large-payload 路由分支 (BifrostContextKeyLargePayloadMetadata) | `plugins/governance/main.go:1227-1242` (PreRequestHook) | ✅ 整个 if-block 删除（21 行） |
| 删除 `IsEnterprise` 分支逻辑（用于 "authentication is required" 错误消息） | `plugins/governance/main.go:940-955` (EvaluateGovernanceRequest) | ✅ 分支删除，统一为 OSS 错误消息 |

V-plugins-1 验证：
- `cd plugins/governance && go build ./...` 退出码 0，无 undefined identifier ✅

插件级 enterprise 清理（额外，超出 strict design 但与 design "清理 enterprise 注释" 主题一致）：
- `plugins/governance/main.go`: `applyRoutingRules` 删除 `UserID` 字段；`EvaluateGovernanceRequest` 删除 `customerFromTeam` 与 `scopedAway` 分支（OSS 简化）
- `plugins/logging/main.go`: 删除 `scheduleDeferredUsageUpdate` 中 100 行 goroutine + deferred usage 逻辑（design.md "数据流" 中明确"OSS 中是 no-op"），删除 UserID/UserName/BusinessUnit 标签 stamping，删除 teamIDs/customerIDs/buIDs plural 数组 stamping
- `plugins/logging/main.go`: 删除 LargePayloadRequestThreshold 触发的 transcription/image-edit/image-variation 输入剥离分支
- `plugins/otel/main.go`: `buildContextAttrs` 删除 business unit 维度 + entitySet 调用，替换为 scalar
- `plugins/telemetry/main.go`: `PostMCPHook` + `PostLLMHook` 删除 business_unit 标签 + entitySet 调用
- 这些改动跨多个 module 但都属于"strip enterprise features"主题，符合 design.md 概述

dev.plugins:test 阶段产物（task 16.x）：
- 新增 `plugins/governance/enterprisestrip_test.go` (90 行)：两个 regression test 锁住删除状态
  - `TestStrip_UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys`：6 个 governance 文件 × 13 个删除键 → 0 残留（PASS）
  - `TestStrip_MainGoConfigIsEnterpriseRemoved`：3 个标识符（`IsEnterprise` / `isEnterprise` / `is_enterprise`）在 main.go 中 → 0 残留（PASS）

## R-2 scope_creep — 详细证据

`git diff f6e4da8..HEAD --stat -- plugins/` 完整范围：

```
plugins/governance/customer_scope_test.go          |  47 ++----
plugins/governance/enterprisestrip_test.go         |  90 ++++++++++
plugins/governance/main.go                         |  60 +------
plugins/governance/modelprovidergovernance_test.go |  11 +-
plugins/governance/prerequesthookmcp_test.go       |  33 +---
plugins/governance/resolver.go                     |   6 +-
plugins/governance/resolver_test.go                |   4 +-
plugins/governance/store.go                        |  50 +++---
plugins/governance/tracker.go                      |   4 +-
plugins/logging/main.go                            | 186 +--------------------
plugins/logging/operations_test.go                 |  74 ++++----
plugins/logging/redaction_test.go                  |  29 ++--
plugins/otel/main.go                               |  19 ++-
plugins/telemetry/main.go                          |  17 ++-
14 files changed, 213 insertions(+), 417 deletions(-)
```

14 个变更文件路径前缀验证：
- 13 个文件在 `plugins/governance/` 或 `plugins/logging/` 或 `plugins/otel/` 或 `plugins/telemetry/` 下 ✅
- 1 个文件 `plugins/governance/enterprisestrip_test.go` 是新文件（在 governance module 下） ✅
- 全部属于 `module_details[].root = plugins` ✅

无 core/framework/transports/ui/cli 跨模块改动。无 design.md 未提及的新功能引入（logging/otel/telemetry 的额外 enterprise 清理属于 design.md "影响面 > 后端 > 哪些 Go 文件修改" 之外但主题一致，且都是**删除**而非**新增**）。

## R-3 pattern_consistency (Go) — 详细证据

对所有新增行（git diff `+` 行）做模式扫描：

| 检查项 | 命令 | 结果 |
|--------|------|------|
| panic 滥用 | `grep -E '^\+\s*panic\('` | 0 行 |
| interface{} 用法（应使用 any） | `grep -E '^\+\s*interface\{\}\b'` | 0 行 |
| 未 wrapping 错误（`return err$`） | `grep -E '^\+\s*return err\s*$'` | 0 行 |
| goroutine 无 ctx.Done() 退出 | `grep -E '^\+\s*go func\(\)'` | 0 行（仅删除 goroutine，无新增） |
| defer 在循环中 close(ch) | `grep -E '^\+\s*for.*\{[^}]*defer'` | 0 行 |
| struct tag 缺失 | 新增 struct 字段 = 0（仅删除字段） | N/A |

struct 改动分析：
- `EvaluationRequest.UserID` 字段保留但语义改为 OSS-empty（注释更新）
- `UsageUpdate.UserID` 字段保留但语义改为 OSS-empty（注释更新）
- 删除：`GovernancePlugin.isEnterprise`、`Config.IsEnterprise`、`LocalGovernanceStore.scopedCustomerID` 业务变量

新增函数/方法：0（仅删除 + 注释改写）。

context.Context 使用：
- 删除的 `scheduleDeferredUsageUpdate` 中的 goroutine 本就监听 `p.ctx.Done()`（删除前已合规）
- 保留的所有现存方法均继续传 `ctx context.Context`

## R-4 file_location — 详细证据

`git diff f6e4da8..HEAD --name-only -- plugins/` 输出 14 个文件，全部路径前缀均为 `plugins/<module>/`：

| 文件路径 | module | 合规 |
|---------|--------|------|
| plugins/governance/customer_scope_test.go | governance | ✅ |
| plugins/governance/enterprisestrip_test.go | governance | ✅ |
| plugins/governance/main.go | governance | ✅ |
| plugins/governance/modelprovidergovernance_test.go | governance | ✅ |
| plugins/governance/prerequesthookmcp_test.go | governance | ✅ |
| plugins/governance/resolver.go | governance | ✅ |
| plugins/governance/resolver_test.go | governance | ✅ |
| plugins/governance/store.go | governance | ✅ |
| plugins/governance/tracker.go | governance | ✅ |
| plugins/logging/main.go | logging | ✅ |
| plugins/logging/operations_test.go | logging | ✅ |
| plugins/logging/redaction_test.go | logging | ✅ |
| plugins/otel/main.go | otel | ✅ |
| plugins/telemetry/main.go | telemetry | ✅ |

无 `.pg/` 外、module root 外的文件新增/修改。

## R-5 test_contract — 详细证据

新增测试与改动测试的断言强度：

| 文件 | 测试函数 | 改动 | 断言强度 |
|------|---------|------|---------|
| `plugins/governance/enterprisestrip_test.go` | `TestStrip_UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys` | 新增 | `t.Errorf` 严格断言每个文件 × 每个删除键不存在 |
| `plugins/governance/enterprisestrip_test.go` | `TestStrip_MainGoConfigIsEnterpriseRemoved` | 新增 | `t.Errorf` 严格断言 3 个标识符在 main.go 中不存在 |
| `plugins/governance/customer_scope_test.go` | `TestCollectHierarchy_ScopedCustomerSkipsScalarTeamCustomer` | 改写 | `assert.True/False` 仍为强断言；删除 enterprise-scoped 子测试，保留 OSS-only 子测试 |
| `plugins/governance/customer_scope_test.go` | `TestEvaluateGovernanceRequest_ScopedCustomerSkipsScalarTeamCustomerEnforcement` | 改写 | `assert.Equal(DecisionBudgetExceeded, ...)` 强断言保留 |
| `plugins/governance/resolver_test.go` | `TestGovernancePlugin_EvaluateGovernanceRequest_DirectKeySatisfiesMandatoryAuth` | 改写 | `assert.Equal` 强断言保留 |
| `plugins/governance/resolver_test.go` | `TestGovernancePlugin_EvaluateGovernanceRequest_HeaderWithoutContextDoesNotSatisfyAuth` | 改写 | `assert.Equal` 强断言消息内容，删除 `isEnterprise: true` 字段赋值 |
| `plugins/governance/prerequesthookmcp_test.go` | 7 个 `LargePayload_*` 测试 | 改写 | `assert.Equal` 强断言保留；删除 `metadata.Model` 断言因 large-payload 路由分支已删除 |
| `plugins/governance/modelprovidergovernance_test.go` | `TestPostHook_TracksVirtualKeyUsageWhenUserIDPresent` 等 | 改写 | 删除 `BifrostContextKeyUserID` 上下文设置 + 删除末尾 `bifrost.GetStringFromContext` 验证；保留 VK 用量追踪断言 |
| `plugins/logging/operations_test.go` | `TestPostLLMHookCancelledStreamLogsCost` | 改写 | `want :=` 计算式调整以反映 OSS 中无 judge-call cost；保留 `1e-9` tolerance 的 `diff` 严格断言 |
| `plugins/logging/operations_test.go` | `TestMCPHooksPersistPluginLogs` | 改写 | `assertMCPLogGovernanceFields` 签名简化，保留 `assert.Fatal` 严格断言（OSS 必须为 nil） |
| `plugins/logging/operations_test.go` | `TestGuardrailDebugForLogReadsContextWithoutResponse` | 改写 | 翻转断言语义：OSS 中 `AppendGuardrailJudgeCallOnContext` 必须返回 false，`guardrailDebugForLog` 必须返回 nil |
| `plugins/logging/redaction_test.go` | `TestAttachLogRedactionDataCopiesContextValue` | 改写 | `require.NotNil` → `assert.Nil`（OSS 必须为 nil） |
| `plugins/logging/redaction_test.go` | `TestAttachLogRedactionDataClonesContextMaps` | 改写 | 改为断言 nil |
| `plugins/logging/redaction_test.go` | `TestAttachMCPLogRedactionDataCopiesContextValue` | 改写 | 改为断言 nil |

无弱断言（`toBeDefined()` / `toBeTruthy()` 等）引入。命名反映业务场景（"UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys"、"MainGoConfigIsEnterpriseRemoved"）。

happy path + error path 覆盖：
- `TestStrip_*` 既是 happy path（验证清理完成）也是 error path（t.Errorf 即 fail）
- `TestEvaluateGovernanceRequest_*` 覆盖 auth 失败 + auth 成功两个场景

## R-6 error_wrapping — 详细证据

对所有新增/删除行做错误处理扫描：

| 检查项 | 命令 | 结果 |
|--------|------|------|
| `errors.New` 直接抛无上下文 | `grep -E '^\+\s*errors\.New\('` | 0 行 |
| `errors.Is/As` 缺失导致 `==` 比较 | `grep -E '^\+\s*err *=='` | 0 行 |
| error 吞掉（catch 后只 log） | `grep -E '^\+\s*p\.logger\.Warn.*err.*return nil'` | 0 行 |
| `_ = xxx` 忽略 error | `grep -E '^\+\s*_ = '` | 0 行 |
| sentinel error 散落新增 | `grep -E '^\+\s*var Err\w+ = '` | 0 行 |

删除路径中保留的 error handling：
- `store.go` 中 `LocalGovernanceStore.CheckUserBudget` / `CheckUserRateLimit` 等 no-op 函数签名 `(ctx, userID, request, ...) (Decision, error)` 完全保留
- `logger.Warn("...: %v", err)` 模式在 `scheduleDeferredUsageUpdate` 中删除前已合规（保留 `err` 在格式化串中），现在整个函数删除

业务 sentinel error：`var ErrEnterprise*` / `var ErrVault*` 等企业版错误常量在 plugins/ 下搜索：

```
grep -rE 'ErrEnterprise|ErrVault|ErrScoped' plugins/  →  0 matches
```

无新增 sentinel error；现存 sentinel error 不在本变更范围。

## 通过项（简要）

- R-1 design_alignment: PASS（design.md plugins 阶段承诺 7/7 已实现；V-plugins-1 通过 build 验证）
- R-2 scope_creep: PASS（14 个文件全部在 plugins/ 下，跨 logging/otel/telemetry 改动属 enterprise strip 主题）
- R-3 pattern_consistency: PASS（纯删减 + 注释清理，无 panic/interface{}/err/goroutine 违规）
- R-4 file_location: PASS（全部路径合规）
- R-5 test_contract: PASS（新增 2 个强断言 regression test；既有测试断言保持严格度或翻转为正确语义）
- R-6 error_wrapping: PASS（无新增错误处理路径）

## 决策

`review_score = 100 ≥ pass_threshold = 85`，**disposition = completed**，进入 dev.plugins:verify 阶段。
P0 硬约束（本 track 无 p0 检查项）未触发。

## 验证快照

- `for d in plugins/*/; do (cd "$d" && go build ./...); done` → 12/12 OK
- `for d in plugins/*/; do (cd "$d" && go vet ./...); done` → 12/12 OK
- `cd plugins/governance && go test ./... -short -count=1` → PASS（governance + governance/complexity）
- `cd plugins/logging && go test ./... -short -count=1` → PASS
- V-plugins-1：`cd plugins/governance && go build ./...` → 退出码 0，无 undefined identifier