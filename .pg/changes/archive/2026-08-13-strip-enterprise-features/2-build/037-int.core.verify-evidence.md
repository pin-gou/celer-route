# int.core:verify Evidence

## Task: 29.1 Lint (go vet)
```
cd /home/ubuntu/workspace/my-gateway/core && go vet ./...
```
**Result**: PASS (no output, exit 0)

---

## Task: 29.2 Tests (go test ./... -short -count=1)
```
cd /home/ubuntu/workspace/my-gateway/core && go test ./... -short -count=1
```

### PASS packages (summary)
- github.com/maximhq/bifrost/core/internal/llmtests
- github.com/maximhq/bifrost/core/internal/mcptests (73.406s)
- github.com/maximhq/bifrost/core/mcp (7.951s)
- github.com/maximhq/bifrost/core/mcp/codemode/starlark (0.102s)
- github.com/maximhq/bifrost/core/mcp/credstore (0.027s)
- github.com/maximhq/bifrost/core/network (0.082s)
- All 20+ providers (anthropic, azure, bedrock, cerebras, cohere, deepseek, etc.)
- github.com/maximhq/bifrost/core/schemas (1.382s)

### FAIL packages (pre-existing, unrelated to enterprise-stripping)
1. `github.com/maximhq/bifrost/core` — TestOverheadEndToEndUnary
   - Reason: Flaky overhead test; fails on base branch too (9e93b94)
   - Error: `overhead = 233.333609ms is more than half of total 441.363082ms`
2. `github.com/maximhq/bifrost/core/providers/utils` — TestConfigureDialer_SSRFMultiIPAllFail
   - Reason: Flaky network test; fails on base branch too (9e93b94)
   - Error: `expected connection error for unroutable TEST-NET address`

**Pre-existing verification**:
```
# Checked base commit 9e93b94 (before any pg-build changes):
git checkout 9e93b94 -- core/ && go test -run TestOverheadEndToEndUnary ./core/... -short -count=1
→ FAIL (same error)
git checkout 9e93b94 -- core/ && go test -run TestConfigureDialer_SSRFMultiIPAllFail ./core/providers/utils/... -short -count=1
→ FAIL (same error)
```
**Conclusion**: Both failures are pre-existing flaky tests, NOT caused by strip-enterprise-features changes.

---

## Task: 29.3 Service Startup
```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --action restart_all_instances \
    --skill pg-build
```

**Output**:
```
Stopping ui-dev (PID 854949)...
{"ok": true, "exit_code": 0}
bifrost-api: not running (stale PID file)
{"ok": true, "exit_code": 0}
重新构建 bifrost-http...
Waiting for bifrost-api to be ready on port 9080...
[32mbifrost-api is ready on port 9080[0m]
{"ok": true, "exit_code": 0}
Waiting for ui-dev to be ready on port 3008...
[32mui-dev is ready on port 3008[0m]
{"ok": true, "exit_code": 0}
pg_http_health_check: probing http://localhost:9080/health (role=bifrost-api instance=bifrost-api-1)
pg_http_health_check: OK — http://localhost:9080/health returned HTTP 200
pg_http_health_check: probing http://localhost:3008/ (role=ui-dev instance=ui-dev-1)
pg_http_health_check: OK — http://localhost:3008/ returned HTTP 200
```
**Result**: PASS — both bifrost-api (port 9080) and ui-dev (port 3008) started and healthy.

---

## Task: 29.4 V-core-N Verification

### V-core-1: Go module build passes
```
cd /home/ubuntu/workspace/my-gateway/core && go build ./...
EXIT_CODE: 0
```
**Result**: PASS

### V-core-2: Go vet passes
```
cd /home/ubuntu/workspace/my-gateway/core && go vet ./...
(no output, exit 0)
```
**Result**: PASS

### V-core-3: Go unit tests pass
(excluding 2 pre-existing flaky tests — see Task 29.2 above)
**Result**: PASS (pre-existing failures confirmed)

### V-core-4: Real LLM provider inference — SKIPPED
**Reason**: design.md explicitly lists V-core-4 as `skipped` in define-summary.
Verification strategy: "scenario 不写; proposal.md '未做' 段列出"

---

## V-transports-2: Enterprise HTTP endpoints return 404

### Enterprise endpoints (must return 404):
```
/api/rbac             → 404 ✓
/api/scim             → 404 ✓
/api/alerting         → 404 ✓
/api/access-profiles  → 404 ✓
/api/business-units   → 404 ✓
```

### Normal endpoints (must return 200):
```
/health  → 200 ✓
/v1/models → 200 ✓
```
**Result**: PASS — all enterprise endpoints return 404, normal endpoints work.

---

## Spring Bean / Runtime Boot Check

The strip-enterprise-features change is a **deletion-only refactor** (no new @Service/@Component classes).
- No new Spring beans introduced
- No constructor changes
- No test doubles or stubs added

**Result**: Not applicable (N/A — no new components).
