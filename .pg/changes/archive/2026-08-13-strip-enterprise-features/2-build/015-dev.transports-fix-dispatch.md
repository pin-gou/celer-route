## 任务：dev.transports

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.transports
- track.modules: ['transports']
- track.max_fix_retries: 5

### Module 配置
  - module: transports
    - root: transports
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: cd transports/bifrost-http && go test ./... -short -count=1
    - test.integration: make test

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: cd transports/bifrost-http && go test ./... -short -count=1


---

- stage.environment.instances:
```yaml
bifrost-api:
- name: bifrost-api-1
  host: localhost
  port: 9080
ui-dev:
- name: ui-dev-1
  host: localhost
  port: 3008
```

- stage.environment.hooks:
```yaml
bifrost-api:
  start:
    host: localhost
    script: .pg/hooks/bifrost-api-start.sh
  stop:
    host: localhost
    script: .pg/hooks/bifrost-api-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/bifrost-api-health-check.sh
ui-dev:
  start:
    host: localhost
    script: .pg/hooks/ui-dev-start.sh
  stop:
    host: localhost
    script: .pg/hooks/ui-dev-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/ui-dev-health-check.sh
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session strip-enterprise-features --env local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---

### 必读源报告（verify ESCALATE 派发）

- **源 verify 报告**: .pg/changes/strip-enterprise-features/2-build/014-dev.transports-verify.md

请用 Read 工具逐字读取该文件。报告包含 verify agent 记录的
ESCALATE Issue 详情、失败证据（HTTP 响应/日志片段/stack trace）、
V-* 验证项的逐项结果等完整上下文。

### 修复范围

- fix_cycle: 1
- 只修复 source verify 报告中列出的问题
- 不要引入超出修复范围的新功能

### 修复后必跑流程

1. 修改源码
2. 跑 cd transports/bifrost-http && go test ./... -short -count=1 单元测试（必须通过）
3. 跑模块 lint（必须 0 警告）
4. 启动 invoke-hook --action start 服务（如需）
5. 只跑 verify 报告中标记为 FAIL 的 V-* 验证项（不要跑全部）
6. 冒烟：跑核心 happy path（与 FAIL 场景相反的"成功路径"，如 200 OK 导出）
7. 抓日志确认无 ERROR
8. 停止 invoke-hook --action stop 服务（如启动过）
9. 用 cat > 写盘到 2-build/015-dev.transports-fix-1.md

---

### 执行要求

**任务清单**：


**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-transports-1 | config.schema.json 同步校验通过 | 无 | `cd transports/bifrost-http && go test -run TestConfigSchemaSync ./lib/` | 通过 |
| V-transports-2 | enterprise HTTP 端点返回 404 | prepare_env 已启动 bifrost-http 实例 | curl GET /api/rbac /api/scim 等 | 全部返回 404 |

---

[ROLLBACK CONTEXT]
- failed_at: 2026-08-13T08:15:50+00:00
- reason: 14.1/14.2/14.3/V-transports-1/2 全部 FAIL: plugins 引用已删除的 enterprise BifrostContextKey，workspace 编译失败
- source: verify

你必须优先审查该根因是否已修复，再执行本阶段的正常任务。

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：fix）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（修复的 V-* 或 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`fix` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase fix \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/015-dev.transports-fix-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/015-dev.transports-fix-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/015-dev.transports-fix-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation