## 任务：int.scenario

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: int.scenario
- track.modules: ['core', 'framework', 'transports', 'cli', 'plugins', 'ui']
- track.max_fix_retries: 3

### Module 配置
  - module: core
    - root: core
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: go test ./... -short -count=1
    - test.integration: make test-core
  - module: framework
    - root: framework
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: go test ./... -short -count=1
    - test.integration: make test-framework
  - module: transports
    - root: transports
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: cd transports/bifrost-http && go test ./... -short -count=1
    - test.integration: make test
  - module: cli
    - root: cli
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: go test ./... -short -count=1
    - test.integration: make test-cli
  - module: plugins
    - root: plugins
    - language: go
    - build: for d in */; do (cd "$d" && go build ./...); done
    - lint: for d in */; do (cd "$d" && go vet ./...); done
    - test.unit: for d in */; do (cd "$d" && go test ./... -short -count=1); done
    - test.integration: make test-plugins
  - module: ui
    - root: ui
    - language: typescript
    - build: npm run build
    - lint: npm run lint
    - test.unit: npx vitest run

### Stage 配置
- stage.name: int
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: go test ./... -short -count=1 && go test ./... -short -count=1 && cd transports/bifrost-http && go test ./... -short -count=1 && go test ./... -short -count=1 && for d in */; do (cd "$d" && go test ./... -short -count=1); done && npx vitest run


---

- stage.environment.instances:
```yaml
bifrost-api:
- name: bifrost-api-1
  host: localhost
  port: 9080
ui-dev:
- name: ui-dev-1
  host: localhost
  port: 3008
```

- stage.environment.hooks:
```yaml
bifrost-api:
  start:
    host: localhost
    script: .pg/hooks/bifrost-api-start.sh
  stop:
    host: localhost
    script: .pg/hooks/bifrost-api-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/bifrost-api-health-check.sh
ui-dev:
  start:
    host: localhost
    script: .pg/hooks/ui-dev-start.sh
  stop:
    host: localhost
    script: .pg/hooks/ui-dev-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/ui-dev-health-check.sh
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session strip-enterprise-features --env local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---

### 场景执行（scenario-execute）

本 phase 读取 SSOT scenario-<track>.yaml，按顺序执行每个 Scenario，产出结构化 JSON 证据。

### SSOT：scenario-<track>.yaml

**禁止重写或修改 scenario-<track>.yaml**。scenario-<track>.yaml 由 pg-propose 阶段生成，是本 phase 的唯一输入。
如需修改场景，重跑 `pg-gen-scenario.py` 重新生成 skeleton。

### 必读源（每次启动顺序）

1. `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/scenario-scenario.yaml` —— 唯一 SSOT，禁止重写
2. `.pg/changes/<change-id>/env-description.yaml` —— per-change 环境描述（v6 新增）。
   - **直接消费**：scenario given 中引用的 `infra_services[name].instances[id].endpoint` / `business_systems[name].endpoints[url]` / `config_resources[name].location` 等路径直接读取，无需 `$` 占位符解析
   - **不允许**自行拼装资源 ID 或端口号 —— 所有连接信息来自此 YAML
   - **缺失**（描述文件中没有的资源）→ 立即失败并报告，不允许跳过或自造值
   - v1.0 之前版本使用的 `.pg/context/env-capability.yaml` 已废弃，不再读取

### scenario-<track>.yaml 路径

`/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/scenario-scenario.yaml`

### Scenario 文件结构

```yaml
scenarios:
  - scenario_id: S-<unique-name>          # 必须全局唯一（如 S-create-vm-happy-path）
    critical: true                        # true=禁止 SKIP；false=可记录 SKIPPED 后继续
    description: 一句话描述验证目标
    given:
      - <前置条件 1>
      - <前置条件 2>
    when:
      # API 类型（type=api 或省略时默认）
      - name: <动作名>
        type: api                         # 可选，默认 api
        method: <HTTP method | db query | ...>
        url: <endpoint>                    # 完整 URL 或 SQL
        body: {<payload>}                  # 可选
        expect_status: <int>               # 期望响应码
      # 浏览器类型（type=browser）
      - name: <动作名>
        type: browser
        action: <navigate | click | fill | wait | screenshot | assert | evaluate | assert_console | assert_network | press_key>
        url: <页面URL>                     # action=navigate 时必填
        selector: <CSS选择器>              # action=click/fill/wait/assert 时必填
        value: <输入值>                    # action=fill 时必填
        key: <按键>                        # action=press_key 时必填（如 "Enter"、"Control+A"）
        expression: <JS表达式>             # action=evaluate 或 assert 时必填
        condition: <断言条件>              # action=assert_console/assert_network 时必填
        timeout: 5000                      # 可选，等待超时（毫秒）
    then:
      # API 类型断言
      - status_code == <int>
      - response.<field> matches <regex>
      # 浏览器类型断言
      - dom: <selector> exists             # 元素在 DOM 中存在
      - dom: <selector> text == <value>    # 元素文本内容匹配
      - console: no errors                 # 控制台无错误
      - console: no warnings               # 控制台无警告
      - network: <urlPattern> status == <int>  # 网络请求状态码
      - screenshot: <path>                 # 截图对比（可选，记录证据用）
    and:                                   # cleanup 步骤（无论成功失败都跑）
      - name: <cleanup 名>
        action: <HTTP DELETE | db DELETE | ...>
    evidence:
      - <curl 输出文件路径>
      - <screenshot 文件路径>
      - <console 日志路径>
```

### 执行策略：按 type 分派

`when` 数组中的每个步骤按 `type` 字段（默认 `api`）分派执行方式：

| type | 执行方式 | 工具 |
|------|---------|------|
| `api` | 使用 curl 等 HTTP 工具发送请求 | 系统 curl 命令 |
| `browser` | 加载 `pg-browser-testing-with-devtools` SKILL，使用 Chrome DevTools MCP 工具执行浏览器交互 | Chrome DevTools MCP |

#### type=api 步骤

使用 `curl` 发送 HTTP 请求，检查 `expect_status` 并与后续 `then` 断言配合。

#### type=browser 步骤

在执行 browser 步骤前，**先加载 `pg-browser-testing-with-devtools` SKILL**：

```markdown
<skill name="pg-browser-testing-with-devtools">
```

然后使用 Chrome DevTools MCP 工具执行以下 action：

| action | 工具 | 必填字段 | 说明 |
|--------|------|---------|------|
| `navigate` | `navigate_page` | `url` | 导航到页面 |
| `click` | `click` | `selector` (uid) | 点击元素（先用 `take_snapshot` 拿到 uid） |
| `fill` | `fill` | `selector`, `value` | 填写输入框（先用 `take_snapshot` 拿到 uid） |
| `wait` | `wait_for` | `text` 或 `selector` | 等待元素/文本出现 |
| `screenshot` | `take_screenshot` | — | 截取当前页面截图 |
| `assert` | `evaluate_script` 或 `take_snapshot` | `expression` 或 `selector` | 断言 DOM 或 JS 表达式 |
| `evaluate` | `evaluate_script` | `expression` | 执行 JS 表达式 |
| `assert_console` | `list_console_messages` | `condition` | 断言控制台状态 |
| `assert_network` | `list_network_requests` | `condition` | 断言网络请求状态 |
| `press_key` | `press_key` | `key` | 按键/组合键 |

**执行流程**：
1. 对每个 browser step，先 `take_snapshot` 获取当前页面 DOM 树
2. 从 snapshot 中定位目标元素的 `uid`
3. 调用对应 Chrome DevTools 工具执行操作
4. 记录操作结果（截图、控制台日志、网络请求等）

### 浏览器 Token 处理

当 browser Scenario 需要登录态时，按以下优先级获取 token：

1. **given 阶段已有 API 调用**：如果 given 阶段包含登录 API 调用，从响应中提取 token，通过 `navigate_page` 的 `initScript` 参数在导航前注入
2. **直接使用已登录页面**：如果环境已包含登录态，直接导航到目标页面
3. **通过 browser 步骤登录**：使用 `navigate` → `fill` → `click` 的 browser 步骤组合完成登录，不依赖 API token

### 执行流程

1. 读取 scenario-<track>.yaml，校验每个 Scenario 含必要字段
   - 校验失败 → record(scenario-execute, "failed")（workflow_failed）
   - 文件本身缺失 → record(scenario-execute, "failed")

2. 按 scenario_id 排序：**先 critical=true，后 critical=false**

3. 串行执行每个 Scenario：

   - given 阶段：把环境调到 given 描述的状态（如调用 prepare-data API、设置 DB 行）
   - when 阶段：按 `type` 分派执行每个步骤，记录执行结果
     - type=api: 记录 request_id + 响应
     - type=browser: 记录截图、控制台日志、网络请求
   - then 阶段：逐项断言；任一 FAIL → 该 Scenario 标记 fail
   - and 阶段：无论成功失败都执行 cleanup（失败时也必须 cleanup，避免脏数据）

4. 关键规则：
   - **critical=true Scenario FAIL** → 立即停止后续 Scenario（剩余全部 SKIPPED），record(scenario-execute, "escalate")
   - **仅 critical=false Scenario FAIL** → 记录失败，继续后续 Scenario；全部跑完后视情况判定：
     - 若存在 critical=true 全 PASS 且 failure 数 < 30% → record(scenario-execute, "completed")，summary 列出跳过/失败的非 critical Scenario
     - 否则 record(scenario-execute, "escalate")

### 结构化 JSON 证据

每个 Scenario 必须产出 JSON 块：

```json
{{
  "scenario_id": "S-xxx",
  "critical": true,
  "status": "pass|fail|skip",
  "type": "api|browser|mixed",
  "steps": [
    {{"phase": "given", "result": "ok|skip"}},
    {{"phase": "when", "result": "ok", "step_type": "api", "request_id": "<id>", "response_status": 200}},
    {{"phase": "when", "result": "ok", "step_type": "browser", "action": "navigate", "url": "/login"}},
    {{"phase": "then", "result": "fail", "assertions": [{{"name": "status_code == 200", "actual": 500, "passed": false}}]}},
    {{"phase": "and", "result": "ok", "cleanup_status": 204}}
  ],
  "request_ids": ["<id1>", "<id2>"],
  "screenshot_paths": ["<path1>", "<path2>"],
  "timestamps": {{"start": "ISO8601", "end": "ISO8601"}},
  "cleanup_result": "ok"
}}
```

### 失败时 cleanup

当 Scenario 失败时，**必须**执行 `and` 表里的 cleanup，避免脏数据污染后续 Scenario。
cleanup 执行结果记录到 `cleanup_result` 字段。

### 返回契约

按 prompt 模板 sub_agent_contract 块落盘 result.json。
- escalate 时 `tasks_updated` 必须含失败的 scenario_id 列表
- completed 时 `outputs` 必须含每个 Scenario 的 JSON 证据路径
- **v1.1.0 (P1-2) 结构化清单契约**（reducer 强制校验，违反 → error action）：
  - `failed_scenarios`：JSON 数组字符串。escalate 时**必须非空**（失败 scenario_id 列表）；completed 时**必须为 `"[]"`**。
  - `skipped_scenarios`：JSON 数组字符串。无跳过时填 `"[]"`；有跳过时列出被 SKIPPED 的 scenario_id。
  - 调 `pg-build-result` 时用 `--failed-scenarios '["S-xxx"]' --skipped-scenarios '[]'` 传入。
  - 这两个字段缺失或 JSON 非法，reducer 直接拒绝该 record（error action），不推进 state。

### 写盘要求

完成后用 `cat >` 写盘到 `2-build/059-int.scenario-scenario-execute.md`，含：
- 每个 Scenario 的 PASS/FAIL/SKIP 判定
- 结构化 JSON 证据（per Scenario block）
- browser 场景的截图路径列表
- cleanup 报告
- critical=true 失败列表（若 escalate）

### 证据文件命名

每个 Scenario 必须产出结构化 JSON，存到 `2-build/059--evidence.json`：

- `059` 与本 phase 主报告 `059-int.scenario-scenario-execute.md` 共享同一 seq（来自 dispatch_file 注入）
- `` 来自 scenario-<track>.yaml 中 `scenarios[i].scenario_id` 字段
- scenario-<track>.yaml 的 `evidence` 字段只声明 scenario_id（如 `S-xxx-evidence.json`），**不**含 seq；agent 写盘时自动在前面拼 `059-` 形成最终路径
- **必须**加 seq 前缀，否则多次派遣（首次 execute / fix 后重跑 execute）会覆盖同 scenario 的历史 evidence
- 最终在 result.json 的 `evidence_paths` / `outputs` 中填实际落盘的绝对路径（含 `059-` 前缀）
- browser 场景的截图建议存到 `2-build/059--screenshot.png`


---

### 执行要求

**任务清单**：
## 51. int.scenario:scenario-execute - 真机场景执行

<!-- on_conditions_eval:
     stage=int (常驻, 无 on_conditions)
     track=scenario (常驻, 无 on_conditions)
-->

#### 步骤组 1：scenario-scenario.yaml 读取

- [ ] 51.1 确认 `.pg/changes/strip-enterprise-features/scenario-scenario.yaml` 存在且每个 Scenario 含 6 段（scenario_id / critical / given / when / then / evidence；and 可选）
- [ ] 51.2 校验 scenario_id 全局唯一、critical 字段为 bool

#### 步骤组 2：执行

- [ ] 51.3 按 scenario_id 排序：先 critical=true，后 critical=false
- [ ] 51.4 串行执行每个 Scenario 的 given → when → then → and（cleanup）
- [ ] 51.5 按 when[].type 分派执行方式：
  - type=api（默认）：使用 curl 等 HTTP 工具执行 API 请求
  - type=browser：加载 `pg-browser-testing-with-devtools` SKILL，使用 Chrome DevTools MCP 工具执行浏览器交互
- [ ] 51.6 产出结构化 JSON 证据到 `2-build/<report_seq>-<scenario_id>-evidence.json`（<report_seq> 与本 phase 主报告共享同一 seq，由 dispatch_file 注入；加 seq 前缀避免多次 execute 派遣覆盖同 scenario 的历史 evidence）
- [ ] 51.7 browser 场景截图存到 `2-build/<report_seq>-<scenario_id>-screenshot.png`
- [ ] 51.8 critical=true FAIL → 立即停止后续 Scenario，全部标记 SKIPPED → record(scenario-execute, "escalate")
- [ ] 51.9 全部通过 / scenario-execute agent 写盘报告到 `2-build/<seq>-scenario-execute.md`

**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-scenario-1 | 完整变更后端到端 build + lint 验证 | 全部 track 已完成 | `make build LOCAL=1 && cd ui && npm run build` | 退出码 0 |
| V-scenario-2 | helm template 渲染通过 | helm-cli 可用 | `helm template helm-charts/bifrost/` | 退出码 0，无渲染错误 |

## 变更类型判定

| track | 是否影响 | 理由 |
|-------|---------|------|
| core | ✅ | 删除 20+ enterprise-only BifrostContextKey、Vault hooks、SSEReaderFactory |
| framework | ✅ | 删除 FeatureFlags.EnterpriseOnly、ErrFlagEnterpriseOnly、SyncDelegate 接口 |
| transports | ✅ | 删除 enterprisePlugins 列表、LargePayloadHook、注释清理、config_test.go enterpriseSchemaPaths、handlers/utils_test.go enterprise 表名硬编码 |
| cli | ❌ | 无 enterprise 引用，无需修改 |
| plugins | ✅ | 删除 enterprise-only 注释、确认 governance 插件不引用被删键 |
| ui | ✅ | 删除 30+ 路由、100+ import 重写、删除 fallback 目录、删除 sidebar 条件、删除 selector |

**affected_tracks**：`[core, framework, transports, plugins, ui]`

**scenario_tracks_decision**：

- 跨 role 协作验证？**否** —— 这是纯 refactor，不引入新 API 端点，不涉及跨 service 协作
- 新 API 端点？**否** —— 仅删除，不新增
- 跨模块联调？**是** —— 涉及 core/framework/transports/plugins/ui 6 模块联合编译验证

`scenario_decisions`：仅 scenario track 启用（用于跨模块联调验证），其他都按 standard 处理。
`scenario_reason`：跨模块联调场景需要 scenario track 验证 core/framework/transports/plugins/ui 联合 build 通过；纯 refactor 无新 API 端点也无跨 service 协作。

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：scenario-execute）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | 选填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`scenario-execute` | （未知 phase，使用前请补 _CONTRACT_PHASE_POLICIES） |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase scenario-execute \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/059-int.scenario-scenario-execute-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/059-int.scenario-scenario-execute-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/059-int.scenario-scenario-execute-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation