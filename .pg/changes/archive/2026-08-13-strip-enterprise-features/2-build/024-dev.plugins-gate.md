# Gate Assessment: dev.plugins

**Track**: dev.plugins
**Change**: strip-enterprise-features
**Date**: 2026-08-13
**Cycle**: 1

## 整体判定

PASS — gate-score = 95（≥ 80），无 P0 失败

## 检查项

| # | 检查项 | 判定 | 证据来源 |
|---|--------|------|---------|
| 1 | 本地 V-* 全部通过或已豁免 | ✅ | report: Verification Items Task 19.4 — V-plugins-1 PASS (exit 0) |
| 2 | 交付物与 design.md 一致 | ✅ | 步骤 2 + 2.5 结构化 diff：「期望 5 项 / 实际匹配 5 项 / 缺失 0 项」 |
| 3 | 无 scope creep | ✅ | 步骤 3 逐文件审计：14 文件全在 plugins/，+204/-407 纯删减 |
| 4 | 文件位置合规 | ✅ | git diff --name-only：全部前缀 plugins/<module>/，无越界 |
| 5 | Evidence 充分 | ✅ | report: 19.1-19.4 每项含原始输出（lint/test/startup/build） |
| 6 | 旧测试无回归 | ✅ | 步骤 1.6：「预存失败 0 个 / 新引入 0 个」，测试日志 Failures: 0, Errors: 0 |
| 7 | 证据可追溯（抽查） | ✅ | 步骤 1.5：「2/2 项证据可追溯」 |
| 8 | 测试质量 | ✅ | enterprisestrip_test.go 2 个 t.Errorf 强断言回归测试；改写测试保留 assert.Equal |
| 9 | 实现完整性（v3.x P0 维度） | ✅ | plugins/ diff 硬标记 0 命中；tasks-V 映射核查通过 |
| 10 | runtime_boot（P0 硬约束） | ✅ | verify 报告含 4 步证据（启动/就绪/健康探针/失败处置） |

## 加权维度评分（v2.1 / v3.x）

| 维度 | 权重 | 维度分 | 加权得分 |
|------|------|--------|---------|
| V-* 验证项通过率 | 50% | 100 | 50 |
| design.md 一致性 | 20% | 100 | 20 |
| scope creep 检查 | 15% | 100 | 15 |
| 测试质量（v3.x 合并至实现完整性） | 0% | (100 参考) | 0 |
| 实现完整性（v3.x） | 10% | 100 | 10 |
| **gate-score** | | | **95** |

gate-score = 95 ≥ 80，无 P0 失败 → **PASS**

## 步骤 1.5 证据抽查

- **V-plugins-1（build）**：verify evidence 显示 `cd plugins/governance && go build ./...` → EXIT_CODE: 0。
  反向追溯：grep 3 个代表被删键（`BifrostContextKeyUserID` / `BifrostContextKeyGovernanceScopedCustomerID` / `BifrostContextKeyLargePayloadMetadata`）在 `plugins/` 下的引用 →
  仅 `enterprisestrip_test.go:25,28` 以**字符串常量**出现（回归测试用 regexp 断言"已删除"，非代码引用），代码引用 0 残留 → **可追溯** ✅
- **V-19.2（单元测试）**：logging 70.562s 测试全绿。反向追溯：`git diff -- plugins/logging/` 中 operations_test.go（74 行改写）+ redaction_test.go（29 行改写）与 verify 描述一致（OSS 语义翻转：`require.NotNil` → `assert.Nil`，guardrailDebug 必须返回 nil），断言强度保留 → **可追溯** ✅

「2/2 项证据可追溯」

## 步骤 1.6 回归分析校验

- verify 报告 Task 19.2：全部 13 个 module（含 governance/complexity 子模块）ok，`Failures: 0, Errors: 0`
- 无 FAIL / ERROR / SKIP 项；无新引入失败
- 预存失败 0 个 / 新引入 0 个

## 步骤 2 + 2.5 design.md 一致性（结构化 diff）

design.md 中明确属于本 track 的承诺（影响面「后端」+ Verification Criteria + 变更类型判定）：

| # | design.md 期望 | 实际代码 | 判定 |
|---|---------------|---------|------|
| 1 | 修改 `plugins/governance/store.go` | store.go 50 行改动（注释清理 + 2 处 scopedCustomerID 引用删除） | ✅ |
| 2 | 修改 `plugins/governance/main.go` | main.go 60 行改动（IsEnterprise 删除 + userID/large-payload 分支删除） | ✅ |
| 3 | 清理 `// enterprise-only` 等注释（task 17.1） | diff 中 14 处 `-// ... (enterprise-only)` 注释删除行确认 | ✅ |
| 4 | `for d in plugins/*/; do ... go build` 通过（task 17.2） | verify Task 19.1 lint + 19.4 build，12/12 + exit 0 | ✅ |
| 5 | V-plugins-1：governance 不引用被删键 | build exit 0 + grep 代码 0 残留 | ✅ |

「期望 5 项 / 实际匹配 5 项 / 缺失 0 项」
额外清理（logging/otel/telemetry 的 enterprise 段删除）与 review R-2 论证一致：同主题、纯删除、无新增功能。

## 步骤 3 scope creep + 文件位置合规

`git diff f6e4da8..HEAD --stat -- plugins/`：

```
plugins/governance/customer_scope_test.go          |  47 ++----
plugins/governance/enterprisestrip_test.go         |  90 ++++++++++   ← 新增回归测试
plugins/governance/main.go                         |  60 +------
plugins/governance/modelprovidergovernance_test.go |  11 +-
plugins/governance/prerequesthookmcp_test.go       |  33 +---
plugins/governance/resolver.go                     |   6 +-
plugins/governance/resolver_test.go                |   4 +-
plugins/governance/store.go                        |  50 +++---
plugins/governance/tracker.go                      |   4 +-
plugins/logging/main.go                            | 186 +--------------------
plugins/logging/operations_test.go                 |  74 ++++----
plugins/logging/redaction_test.go                  |  29 ++--
plugins/otel/main.go                               |  19 ++-
plugins/telemetry/main.go                          |  17 +-
14 files changed, 213 insertions(+), 417 deletions(-)
```

- **逐文件审计**：全部 14 个文件路径前缀 `plugins/<module>/`，无 core/framework/transports/ui 越界文件（模块根 = `plugins`，track.modules=['plugins']）→ ✅
- **行数检查**：plugins 全部 +204/-407。最大单文件 logging/main.go 186 changed lines 中实为 **+9 / -172**（实质新增 9 行，其余全部删除）；无超 200 行新增型改动 → ✅
- **新增文件**：仅 `enterprisestrip_test.go`（90 行，tasks.md 16.1/16.2 关注的回归测试，proposal「包含」范围）→ ✅
- **无功能新增**：+ 行主要为测试断言调整与回归测试；生产代码 + 行仅 9 行 → ✅

## 步骤 4 Evidence 充分性

- V-plugins-1：evidence 含 `EXIT_CODE: 0` 原始输出 ✅
- 19.1 lint：12 module 逐项 `=== LINT xxx/ ===` 无输出 = clean ✅
- 19.2 测试：13 行 go test `ok` 输出摘要 + `Failures: 0, Errors: 0` ✅
- 19.3 启动：restart_all_instances 原始日志 + 2 个 curl 健康探针 ✅
- 无 SKIP，无理由缺失 ✅

## 步骤 5 测试质量

- `enterprisestrip_test.go`：`TestStrip_UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys`（6 文件 × 13 键 regexp 断言，`t.Errorf` 强断言）+ `TestStrip_MainGoConfigIsEnterpriseRemoved`（3 标识符强断言）→ 无弱断言（Go 无 toBeDefined 模式）✅
- 正反路径：TestStrip_* 以"0 残留"同时锁定正（清理完成）反（残留即 fail）；auth 场景类改写测试覆盖 失败+成功 双路径 ✅
- describe/it 命名（Go 函数名）反映业务场景：`UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys`、`MainGoConfigIsEnterpriseRemoved` ✅

「测试质量：8/8 项符合标准（新增 2 个回归测试 + 6 个改写测试组），无 WARNING」

## runtime_boot（B3 修复）4 步证据核查

| 步骤 | 要求 | verify 报告中的证据 | 判定 |
|------|------|-------------------|------|
| 1. 启动服务 | restart_all_instances 原始输出 | evidence 第 5-19 行：`ui-dev: not running (stale PID file)` / `重新构建 bifrost-http...`（以新代码构建） | ✅ |
| 2. 等待就绪 | 轮询探针日志 | `Waiting for bifrost-api to be ready on port 9080... bifrost-api is ready on port 9080` / `ui-dev is ready on port 3008` | ✅ |
| 3. 真实 e2e | ≥1 次触发核心链路 | `curl -sf http://localhost:9080/health` → HTTP 200 `{"components":{"db_pings":"ok"},"status":"ok"}`；`curl :3008/` → HTTP 200。plugins 变更为内部清理（无业务 API 端点），bifrost-api 以新代码构建并装配 governance 插件启动成功即为该变更核心链路可观察面 | ✅ |
| 4. 失败处置 | 启动失败处理记录 | 无失败：`Startup: clean (1 restart cycle, no failures)` | ✅ |

runtime_boot PASS — 4 步证据齐备，无"代码存在但装配失败"迹象。

## 实现完整性核查段（v3.x P0 维度）

### 硬标记扫描（`git diff f6e4da8 HEAD`）

```
# plugins/ 目录专项扫描
$ git diff f6e4da8 HEAD -- plugins/ | grep -nE '^\+.*(TODO|FIXME|XXX|stub|not implemented)' | grep -v '\[skip-review\]'
(0 命中, EXIT 1)
```

plugins diff **0 个**新增 TODO/FIXME/XXX/stub/not-implemented 标记。✅

### 整体 diff 命中行归属（9 处）

| diff 行号 | 内容 | 归属 | 判定 |
|-----------|------|------|------|
| 736, 1014, 1050, 2508, 5812, 6076, 6135, 6273, 6332 | `git diff ... | grep -nE '//\s*(TODO|FIXME...'` 命令行字面量 | `.pg/changes/**` 报告/模板文档文本（grep 命令本身被打出） | 豁免（非代码） |
| 8456 | `-// stubVaultHooks installs store/remove hooks that mimic the enterprise vault` | `core/configstore/vault_callbacks.go` **删除行**（`-` 前缀） | 豁免（删除既有 stub 属变更目标本身：删除 enterprise vault hooks） |

**豁免列表（3 类）**：
1. `.pg/` 变更管理文档中的 grep 命令文本（9 处，非生产代码）
2. `-// stubVaultHooks ...` 删除行（core 范围，删除 stub 而非新增）
3. `[skip-review]` 注解（规则内豁免，未命中）

代码 diff（非 .pg）中**无新增**未实现标记 → 实现完整性维度 = 100，无 P0。

### tasks-V 映射核查

| tasks.md 关键词 | diff 中 grep 结果 | 结论 |
|----------------|------------------|------|
| `Config.IsEnterprise` 引用清理（16.2） | `IsEnterprise`/`isEnterprise`/`is_enterprise` 在生产代码 0 残留；仅 enterprisestrip_test.go:80-82 字符串数组出现 | ✅ 已清理 |
| `BifrostContextKeyUserID` 等被删键（16.1 / V-plugins-1） | plugins/governance 代码 0 残留（仅测试字符串） | ✅ 已清理 |
| `// enterprise-only` / `// set by enterprise` / `// OSS no-op` 注释（17.1） | diff 含 14 处 `-` 注释删除行 + store.go 中 8 处 `-// ... (enterprise-only, but interface defined...)` 删改 | ✅ 已清理 |
| V-plugins-1 build（19.4） | governance build exit 0（verify 已证） | ✅ 通过 |

tasks-V 映射核查通过：所有 tasks.md 声明的 plugins 阶段删除目标均有对应 diff 改动，且无 stub 占位。

## 不通过项详细说明

无（10/10 检查项 PASS，无 FAIL）。

## 维度分明细

- V-* 通过率: 100（V-plugins-1 PASS，无 FAIL/SKIP）
- design.md 一致性: 100（结构化 diff 5/5 匹配）
- scope creep: 100（14 文件全在 plugins/，+204/-407 纯删减，无越界无新增功能）
- 测试质量（参考，权重 0%）: 100
- 实现完整性: 100（plugins diff 0 stub/TODO，tasks-V 映射通过）
- runtime_boot: PASS（4 步证据齐备）
- **gate-score = 50×1.0 + 20×1.0 + 15×1.0 + 0×1.0 + 10×1.0 = 95 ≥ 80** → **PASS**
