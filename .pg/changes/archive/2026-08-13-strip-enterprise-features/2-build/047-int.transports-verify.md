# 047-int.transports:verify 验证报告

## 验证结果：PASS（MIXED — V-transports-1 预存 oauth drift，非本变更引入）

### 执行摘要

int.transports 集成验证完成。lint、build、测试（5 个子包全过）、服务健康探针、真实 e2e（14 个 enterprise 端点全部 404）均通过。V-transports-1 存在 pre-existing oauth schema drift（5 字段），经 fork base 三线确认与本变更无关，与 dev.transports gate（019，PASS 92.5）豁免口径一致。

---

## 39.1 Lint — PASS

- **命令**: `cd transports && go vet ./...`
- **结果**: exit 0，无输出
- **证据**: evidence 文件 39.1 段

## 39.2 测试 — PASS（子包全过；lib/schema_test 2 个 pre-existing 失败与本变更无关）

- **命令**: `cd transports/bifrost-http && go test ./... -short -count=1`

| 包 | 结果 | 耗时 |
|----|------|------|
| handlers | ✅ ok | 24.712s |
| integrations | ✅ ok | 0.279s |
| server | ✅ ok | 0.121s |
| websocket | ✅ ok | 1.544s |
| tests/realtime | ✅ ok | 0.004s |
| lib — TestConfigSchemaSync | ⚠️ FAIL（pre-existing oauth drift） | 0.047s |
| lib — TestConfigSchemaSyncTopLevel | ✅ PASS（enterprise 删除同步正确） | — |
| lib — 套件超时 | ⚠️ getbifrost.ai/datasheet 网络延迟（非代码回归） | 180s 超时 |
| schema_test — TestSchemaMCPClientConfigFields | ⚠️ FAIL（pre-existing oauth drift） | 1.4s |

**pre-existing 失败确认（fork base 三线）**：
1. `git show db1ef287:core/schemas/mcp.go | grep -c oauth` = 37，本分支 mcp.go oauth 计数相同 —— Go struct 基线即有 oauth 字段
2. `git show db1ef287:transports/config.schema.json | grep -c oauth_config_id` = 0，`oauth_authorize_url` = 0 —— schema 基线同样缺失；本分支相同
3. int.transports:dev diff（dbdf019）对 schema `oauth_*` 0 命中、对 config_test.go oauth 排除项 0 改动 —— 本变更未新增/未删除任何 oauth 字段
4. `TestSQLite_VirtualKey_WithProviderConfigs` 单独运行 PASS（8.3s），套件 180s 超时系 `modelcatalog/datasheet` 从 `getbifrost.ai/datasheet`（HTTP/2, ~1.6MB, 4.8s/次, 带 retry）拉取的间歇性网络延迟，非代码问题

## 39.3 服务启动 — PASS（已运行，未重启）

- bifrost-api（port 9080）：✅ LISTEN（PID 970811），`/health` → 200
- ui-dev（port 3008）：✅ LISTEN（PID 971147），`/` → 200

## 39.4 V-transports-N 验证项

### V-transports-1: config.schema.json 同步校验通过

**结果**: ⚠️ DEGRADED（pre-existing，非本变更引入）

**方法**: `cd transports/bifrost-http && go test -run TestConfigSchemaSync ./lib/`

**证据**:
```
=== RUN   TestConfigSchemaSync
    config_test.go:18249: Schema sync errors found (5 total):
        [mcp.client_configs] Field 'oauth_authorize_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_registration_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_resource' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_scopes' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_token_url' in schemas.MCPClientConfig but missing from schema
--- FAIL: TestConfigSchemaSync (0.00s)
=== RUN   TestConfigSchemaSyncTopLevel
    config_test.go:18303: Top-level sync validated: 20 properties match
--- PASS: TestConfigSchemaSyncTopLevel (0.00s)
```

**分析**:
- 5 个 `oauth_*` 字段在 Go struct `MCPClientConfig`（`core/schemas/mcp.go:454-458`）中存在但 `config.schema.json` 缺失 —— 与 dev.transports verify（018）同因
- **fork base（db1ef287）三线确证**：Go struct / schema / 本分支 oauth 计数均与 fork base 一致（详见 evidence 39.2 段），本变更 diff 对 oauth 0 改动 → drift 与本变更完全无关
- 本变更相关的 schema 同步（enterprise 字段删除）验证：`TestConfigSchemaSyncTopLevel` **PASS（20 属性匹配）**；config_test.go 的 `excludedSchemaFields`/`excludedGoFields` 调整（`is_access_profile_managed` 等）经 review（046）与本次运行确认正确
- 与 dev.transports gate（019，gate-score 92.5 PASS 同一豁免口径）一致：预存 drift 不阻断交付

### V-transports-2: enterprise HTTP 端点返回 404

**结果**: ✅ PASS

**方法**: curl 14 个 enterprise-only 端点

**证据**:
```
/api/rbac → 404         /api/scim → 404
/api/audit-logs → 404   /api/alerting → 404
/api/cluster → 404      /api/guardrails → 404
/api/circuit-breaker → 404  /api/mcp-tool-groups → 404
/api/mcp-auth-config → 404  /api/access-profiles → 404
/api/business-units → 404   /api/user-rankings → 404
/api/adaptive-routing → 404 /api/edge-control → 404
```
OSS 正常端点：`/api/providers` → 200、`/v1/models` → 200 ✅

---

## B3 运行时启动 + 健康探针（v3.x 强制项）

### Step 1: 启动服务
✅ 服务已在运行（bifrost-api 9080 / ui-dev 3008），符合"若当前未运行"条件，未重复重启。

### Step 2: 等待就绪（30 次 × 3s 轮询）
- bifrost-api port 9080 `/health` → **200** ✅
- ui-dev port 3008 `/` → **200** ✅

### Step 3: 真实 e2e
✅ 触发本次变更核心链路：14 个 enterprise-only HTTP 端点全部返回 404（删除端点后不再注册），OSS 端点 200 正常 —— 与设计契约 V-transports-2 预期一致。

### Step 4: 失败处置
不适用。无启动失败、无探针超时、e2e 符合预期。

### Spring Bean 装配自查（runtime_boot check 辅助项）
**N/A** — 本项目为 Go 工程，本变更为纯删除型 refactor，无新增 @Service/@Component 类、无多构造函数、无测试替身。

---

## define-summary 对账

| define-summary | 本验证结果 |
|----------------|-----------|
| verifiable: V-transports-1 | ⚠️ DEGRADED（pre-existing oauth drift，fork base 三线确证与本变更无关；TopLevel 同步 PASS） |
| degraded: V-transports-2 | ✅ PASS（14 端点全 404，实际完成 degraded 项验证） |

## 问题清单

无本变更引入的问题。仅 2 项 pre-existing（均已确证与本变更无关）：
1. `TestConfigSchemaSync` 5 个 oauth 字段 schema drift（fork base 即有，dev 阶段 018/019 已记录并豁免）
2. `lib/` 套件 180s 超时（getbifrost.ai/datasheet 网络延迟；单测 8.3s 通过）

---

**验证时间**: 2026-08-13
**Track**: int.transports
**Stage**: verify