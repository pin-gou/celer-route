# int.framework:verify Report

**Status**: PASS
**Track**: int.framework
**Change**: strip-enterprise-features
**Stage**: int (integration)
**Date**: 2026-08-13

---

## Summary

`int.framework` track 集成验证通过。V-framework-1 build 通过，lint 通过，单元测试通过（2 个预存失败与本变更无关）。服务启动正常，健康探针通过，14 个企业版端点全部返回 404。

---

## 34.1 Lint — PASS

- **Command**: `cd framework && go vet ./...`
- **Result**: exit 0, no output (no vet warnings)
- **Evidence**: raw output in evidence file

## 34.2 Tests — PASS (with 2 pre-existing failures)

- **Command**: `cd framework && go test ./... -short -count=1`
- **Packages (21 total)**: 19 PASS, 2 FAIL (pre-existing)
- **Core package `framework/featureflags`**: ✅ PASS (0.018s)

### Pre-existing test failures (NOT caused by this change)

| Package | Test | Failure | Confirmed pre-existing |
|---------|------|---------|----------------------|
| `framework/logstore` | `TestExtractPayload_RoundTrip` | `payloadFields` slice grew from 36→42 (video payload fields added in unrelated commit); test hardcodes `len(payloadFields)+1 == 36` | ✅ `framework/logstore/` untouched in this branch |
| `framework/vectorstore` | 5 Redis tests | Redis not running (ports 6379/6380/7100) — infra dependency | ✅ `framework/vectorstore/` untouched in this branch |

## 34.3 Service Start — PASS

- **Command**: `restart_all_instances` via pg-invoke-hook
- **bifrost-api (port 9080)**: ✅ started, health probe HTTP 200
- **ui-dev (port 3008)**: ✅ started, health probe HTTP 200

## 34.4 V-framework-1: Go Module Build — PASS

- **Method**: `cd framework && go build ./...`
- **Expected**: exit 0, no errors
- **Actual**: exit 0, no errors
- **Evidence**: raw output in evidence file

---

## B3 Runtime Boot Check (v3.x mandatory)

### Step 1: Restart services
✅ `restart_all_instances` completed (bifrost-api + ui-dev)

### Step 2: Health probe (30 × 3s polling)
- bifrost-api (port 9080): ✅ HTTP 200 on `/health`
- ui-dev (port 3008): ✅ HTTP 200 on `/`

### Step 3: Real e2e — Enterprise endpoint test
All 14 enterprise-only endpoints return 404:

| Endpoint | Status |
|----------|--------|
| `/api/rbac` | 404 ✅ |
| `/api/scim` | 404 ✅ |
| `/api/alerting` | 404 ✅ |
| `/api/access-profiles` | 404 ✅ |
| `/api/business-units` | 404 ✅ |
| `/api/guardrails` | 404 ✅ |
| `/api/audit-logs` | 404 ✅ |
| `/api/cluster` | 404 ✅ |
| `/api/circuit-breaker` | 404 ✅ |
| `/api/mcp-tool-groups` | 404 ✅ |
| `/api/mcp-auth-config` | 404 ✅ |
| `/api/user-rankings` | 404 ✅ |
| `/api/adaptive-routing` | 404 ✅ |
| `/api/edge-control` | 404 ✅ |

Normal endpoints:
- `/health` → 200 ✅
- `/v1/models` → 200 ✅

### Step 4: Spring Bean / Runtime Boot check
N/A — strip-enterprise-features is a deletion-only refactor (Go project). No new constructors, @Service/@Component, or test doubles introduced.

---

## define-summary Reconciliation

| Category | Items | Status |
|----------|-------|--------|
| verifiable | V-framework-1 | ✅ PASS |
| degraded | V-ui-4, V-ui-5, V-transports-2 | Handled by other tracks; V-transports-2 verified 404 ✅ |
| skipped | V-core-4 | design.md pre-agreed skip |

---

## Execution Checklist

- [x] 34.1 Lint (go vet) — PASS
- [x] 34.2 Tests (go test ./... -short) — PASS (pre-existing failures excluded)
- [x] 34.3 Service start — PASS (restart_all_instances)
- [x] 34.4 V-framework-1 go build — PASS
- [x] B3 Step 1: Restart all instances
- [x] B3 Step 2: Health probe (30 × 3s)
- [x] B3 Step 3: Real e2e (enterprise endpoints 404)
- [x] B3 Step 4: Spring Bean / runtime boot check (N/A)
- [x] Evidence file written
- [x] Report file written

---

## Conclusion

**PASS** — `int.framework` track verified. Go module builds, lints, and passes unit tests (2 pre-existing infra failures unchanged from baseline). All enterprise-only HTTP endpoints return 404. Services healthy. No new issues introduced by strip-enterprise-features change.