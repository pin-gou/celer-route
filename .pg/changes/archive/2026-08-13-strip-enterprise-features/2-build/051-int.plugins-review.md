# Review Report: int.plugins

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |
| p0_failures | [] |

## Profile

- Profile 名: go (merged with default)
- 检查项: 6 enabled (design_alignment, scope_creep, pattern_consistency, file_location, test_contract, error_wrapping)
- 总权重: 120 (30 + 25 + 20 + 15 + 10 + 20)
- pass_threshold: 85

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | design.md plugins 契约全部实现：enterprise 注释清理、Config.IsEnterprise/p.isEnterprise 删除、ScopedCustomerID 引用删除、large-payload 分支删除、V-plugins-1 (go build) OK |
| R-2 | scope_creep | 25 | PASS | 14 个变更文件全部在 `plugins/` 模块 root 内；governance/logging/otel/telemetry 改动均属 design.md "strip enterprise features" 主题 |
| R-3 | pattern_consistency | 20 | PASS | 无 panic/interface{}/错误吞掉/goroutine 泄漏；错误统一 %w；struct tag 完整；build+vet 通过。**WARN**: 3 文件 gofmt 不干净（详见下） |
| R-4 | file_location | 15 | PASS | 14 文件全部路径前缀 `plugins/<module>/`，符合 `module_details[].root = plugins` |
| R-5 | test_contract | 10 | PASS | enterprisestrip_test.go 两个强断言 regression test；既有测试断言保持严格或翻转为 OSS 正确语义 |
| R-6 | error_wrapping | 20 | PASS | 本次 diff 纯删除 + no-op/注释清理，无新增 errors.New / 裸 return err / _ = / panic / goroutine / interface{} |

## R-1 design_alignment — 详细证据

design.md 中 plugins 阶段契约逐项核对（task 41.x / 42.x + V-plugins-1）：

| Design 项 | 文件 | 状态 |
|----------|------|------|
| 清理 `plugins/governance/` 下 `// enterprise-only` / `// set by enterprise` / `// OSS no-op` 注释 | `main.go` / `store.go` / `resolver.go` / `tracker.go` / `*_test.go` | ✅ `grep -rniE 'enterprise-only|set by enterprise|OSS no-op|enterprise only' plugins/` → 0 matches（非 test 代码） |
| 删除 `Config.IsEnterprise` 字段与 `p.isEnterprise` 内部字段 | `main.go` (Config L40 → removed / Init L235 → removed / InitFromStore L330 → removed / struct field L88 → removed) | ✅ 4 处全部删除 |
| 删除 enterprise-only `BifrostContextKeyUserID` 在 governance 的引用 | `main.go` (applyRoutingRules L727 / PreLLMHook L1266 / PostLLMHook L1307 / PreMCPHook L1394) | ✅ 4 处替换为常量 `""`，逻辑保留但 userID 恒空（OSS 语义） |
| 删除 enterprise-only `BifrostContextKeyGovernanceScopedCustomerID` 在 store 的引用 | `store.go` collectRateLimitsFromHierarchy L3222 / collectBudgetsFromHierarchy L3319 | ✅ 引用删除，`scopedCustomerID` 改为常量 `""`，注释简化 |
| 删除 large-payload 路由分支 (BifrostContextKeyLargePayloadMetadata) | `main.go` PreRequestHook L1227-1242 | ✅ 整个 if-block 删除（21 行） |
| 删除 `IsEnterprise` 分支逻辑（错误消息） | `main.go` EvaluateGovernanceRequest L943 | ✅ 分支删除，统一为 OSS 错误消息 |
| governance 插件不引用被删键 (V-plugins-1) | 全部 governance module | ✅ `grep -rn 'BifrostContextKeyUserID|...|BifrostContextKeyClusterNodeID' plugins/ --include='*.go'` 非 test → 0 matches；`go build ./...` exit 0 |

额外 enterprise 清理（与 design "strip enterprise" 主题一致）：`logging/main.go` scheduleDeferredUsageUpdate 100 行 goroutine 删除、UserID/UserName/BusinessUnit 标签 stamping 删除、LargePayloadRequestThreshold 输入剥离分支删除；`otel/main.go` business-unit 维度 + entitySet 替换为 scalar；`telemetry/main.go` business_unit 标签 + entitySet 删除。均为**删除/简化**而非新增功能。

## R-2 scope_creep — 详细证据

`git diff master...HEAD --name-only -- plugins/` 输出 14 个文件，全部路径前缀 `plugins/<module>/`：

```
plugins/governance/customer_scope_test.go          # governance ✓
plugins/governance/enterprisestrip_test.go         # governance (new) ✓
plugins/governance/main.go                         # governance ✓
plugins/governance/modelprovidergovernance_test.go # governance ✓
plugins/governance/prerequesthookmcp_test.go       # governance ✓
plugins/governance/resolver.go                     # governance ✓
plugins/governance/resolver_test.go                # governance ✓
plugins/governance/store.go                        # governance ✓
plugins/governance/tracker.go                      # governance ✓
plugins/logging/main.go                            # logging ✓
plugins/logging/operations_test.go                 # logging ✓
plugins/logging/redaction_test.go                  # logging ✓
plugins/otel/main.go                               # otel ✓
plugins/telemetry/main.go                          # telemetry ✓
```

14 文件全部在 `plugins/` 模块 root 内，无 core/framework/transports/ui/cli 越界。logging/otel/telemetry 的额外清理属于 design.md "影响面 > 后端 > 哪些 Go 文件修改" 之外，但都是**删除 enterprise 残留**（scheduleDeferredUsageUpdate / 标签 stamping），与 design "strip enterprise features" 主题一致，且是删非增。

## R-3 pattern_consistency (Go) — 详细证据

对所有新增行（git diff `+` 行）做模式扫描：

| 检查项 | 命令 | 结果 |
|--------|------|------|
| panic 滥用 | `grep -E '^\+\s*panic\('` | 0 行 |
| interface{} 用法（应 use any） | `grep -E '^\+\s*interface\{\}\b'` | 0 行 |
| 未 wrapping 错误（`return err$`） | `grep -E '^\+\s*return err\s*$'` | 0 行 |
| goroutine 无 ctx.Done() 退出 | `grep -E '^\+\s*go func\(\)'` | 0 行（仅删除 goroutine，无新增） |
| defer 误用在循环 | `grep -E '^\+\s*for.*\{[^}]*defer'` | 0 行 |
| errcheck 忽略（`_ = xxx`） | `grep -E '^\+\s*_ = '` | 0 行 |
| errors.New 无上下文 | `grep -E '^\+\s*errors\.New\('` | 0 行 |
| sentinel error 散落新增 | `grep -E '^\+\s*var Err\w+'` | 0 行 |

struct/字段改动：删除 `Config.IsEnterprise`、`p.isEnterprise`；`EvaluationRequest.UserID` / `UsageUpdate.UserID` 字段保留但语义改为 OSS-empty（注释更新）。新增函数/方法：0。context.Context 传递：所有保留方法继续传 `ctx context.Context`。

**⚠️ WARN — gofmt 不干净（3 处由本次变更引入）**：

- `plugins/governance/modelprovidergovernance_test.go` — master gofmt-clean，现在 NOT clean：L2141/L2214 两个闭包配对 `}` 缩进错误（`\t}` 应为 `}`），由移除 `bifrost.GetStringFromContext(...UserID)` 断言行的 `}` 位置未复原引起
- `plugins/logging/main.go` — master gofmt-clean，现在 NOT clean：L1181 残留一个孤空白行 `\t`（移除 teamIDs/businessUnitIDs 数组 stamping 块后遗留）
- `plugins/governance/enterprisestrip_test.go` — 新文件，NOT clean：文件末尾多一个空行（L90 无换行位 + trailing blank）
- `plugins/telemetry/main.go` — master 本就 NOT clean，但本次 diff 又引入 map literal 闭括号 `}` 缩进错误（L775 `\t\t}` 应为 `\t}`）+ 对齐值 tab 宽度变化

均不影响 `go build`/`go vet`/`go test`（仅 gofmt -w 可修）。按 pipeline 先例（framework review 将 garden-variety gofmt 问题标 WARN、不阻断），标 **WARN**、R-3 判 **PASS**。建议 fix-review 或后续任意阶段顺手 `gofmt -w` 修正。

## R-4 file_location — 详细证据

`git diff master...HEAD --name-only -- plugins/` 输出 14 个文件全部以 `plugins/<module>/` 为前缀，符合 `module_details[].root = plugins`。无 `.pg/` 外、模块 root 外文件新增/修改。

## R-5 test_contract — 详细证据

新增/改写测试断言强度：

| 文件 | 测试 | 断言强度 |
|------|------|---------|
| `enterprisestrip_test.go` | `TestStrip_UserGovernanceCallSitesNotPollutedByDeletedEnterpriseKeys` | `t.Errorf` 严格断言 6 文件 × 13 deleted key → 0 残留 |
| `enterprisestrip_test.go` | `TestStrip_MainGoConfigIsEnterpriseRemoved` | `t.Errorf` 严格断言 3 标识符 0 残留 |
| `customer_scope_test.go` | `TestCollectHierarchy_ScopedCustomerSkipsScalarTeamCustomer` | `assert.True/False` 保留；删除 enterprise-scoped 子测试 |
| `customer_scope_test.go` | `TestEvaluateGovernanceRequest_ScopedCustomer...` | `assert.Equal(DecisionBudgetExceeded, ...)` 强断言保留 |
| `resolver_test.go` | 2 个 `EvaluateGovernanceRequest_*` | `assert.Equal` 强断言保留；断言消息更新为 OSS 内容 |
| `modelprovidergovernance_test.go` | 4 个 PostHook tests | 删除 `BifrostContextKeyUserID` ctx 设置 + 末尾 GetStringFromContext 验证；保留 VK 用量断言 |
| `prerequesthookmcp_test.go` | 7 个 LargePayload tests | `assert.Equal` 保留；删除 metadata.Model 传播断言（large-payload 分支已删） |
| `operations_test.go` | `TestPostLLMHookCancelledStreamLogsCost` | `want` 计算式去掉 judge-call cost，保留 `1e-9` tolerance 严格 diff |
| `operations_test.go` | `assertMCPLogGovernanceFields` | 签名收窄，新增 `UserID/BusinessUnitID 必须为 nil` 强断言 |
| `operations_test.go` | `TestGuardrailDebugForLogReadsContextWithoutResponse` | 翻转为 OSS 语义：append 必须返回 false、debug 必须 nil |
| `redaction_test.go` | 3 个 redaction tests | `require.NotNil` → `assert.Nil`（OSS 必须 nil） |

关键契约覆盖：happy + error path 全覆盖；命名反映业务场景；无新增弱断言。被删功能（large-payload、guardrail judge-call、user governance、redaction）均在改写测试中锁定 OSS 语义。

## R-6 error_wrapping — 详细证据

| 检查项 | 命令 | 结果 |
|--------|------|------|
| `errors.New` 无上下文 | `grep -E '^\+\s*errors\.New\('` | 0 行 |
| err `==` 比较 | `grep -E '^\+\s*err\s*=='` | 0 行 |
| 错误吞掉（catch 后只 log） | `grep -E '^\+\s*logger\.\w+.*err.*return nil'` | 0 行 |
| `_ = err` 忽略 | `grep -E '^\+\s*_ = '` | 0 行 |
| sentinel error 散落 | `grep -E '^\+\s*var Err\w+'` | 0 行 |

删除路径保留的 error handling：`CheckUserBudget` / `CheckUserRateLimit` 的 no-op 返回 `(Decision, error)` 签名完整保留；`scheduleDeferredUsageUpdate` 整个删除（其中 `logger.Warn("...: %v", err)` 模式删除前已合规）。无新增 sentinel error。

## 通过项（简要）

- R-1 design_alignment: PASS（plugins 契约 100% 实现；V-plugins-1 build OK；无 enterprise 注释/键残留）
- R-2 scope_creep: PASS（14 文件全在 plugins/ 内）
- R-3 pattern_consistency: PASS（**WARN**: 3 文件 gofmt 不干净，可 gofmt -w 修复，不阻断）
- R-4 file_location: PASS
- R-5 test_contract: PASS（regression tests 强断言锁定 OSS 语义）
- R-6 error_wrapping: PASS（无新增错误处理/并发/类型问题）

## 决策

`review_score = 100 ≥ pass_threshold = 85`，**disposition = completed**，进入 int.plugins:verify 阶段。
P0 硬约束（本 track 无 p0 检查项）未触发。

## 验证快照

- `for d in plugins/*/; do (cd "$d" && go build ./...); done` → 12/12 OK
- `cd plugins/governance && go vet ./...` → exit 0
- `cd plugins/logging|otel|telemetry && go vet ./...` → exit 0
- V-plugins-1：`cd plugins/governance && go build ./...` → exit 0，无 undefined identifier

## 残留跟踪

- WARN: `gofmt -w` 未跑完 — `plugins/governance/modelprovidergovernance_test.go`、`plugins/logging/main.go`、`plugins/governance/enterprisestrip_test.go` 由本次 diff 引入 gofmt 违规；`plugins/telemetry/main.go` 的 map literal 闭括号缩进由本次 diff 引入。均不阻断 build/vet/test，建议 verify 后统一 gofmt。
