## 任务：dev.transports

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.transports
- track.modules: ['transports']
- track.max_fix_retries: 5

### Module 配置
  - module: transports
    - root: transports
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: cd transports/bifrost-http && go test ./... -short -count=1
    - test.integration: make test

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: cd transports/bifrost-http && go test ./... -short -count=1


---

### 测试要求

TDD 红 Phase：本阶段只写测试代码，绝不创建或修改任何生产代码。
预期首次编译失败（红 phase 属性）。

- 只创建测试文件
- 不修改任何生产代码文件
- 可以修改测试配置文件（如 pom.xml、package.json 的测试依赖）

### 模块路径约束

禁止编辑以下路径的文件：
- 不在 ['transports'] 路径下的文件

---

### 执行要求

**任务清单**：
## 11. dev.transports:test - dev 测试先行

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=transports (常驻, 无 on_conditions)
-->

- [ ] 11.1 确认 `transports/bifrost-http/lib/config_test.go` 的 `TestConfigSchemaSync` 与 `enterpriseSchemaPaths` map 引用全部清理（`enterpriseSchemaPaths` map 整体删除）
- [ ] 11.2 确认 `transports/bifrost-http/handlers/utils_test.go` 中硬编码的 `enterprise_access_profiles`、`enterprise_users` 表名引用已改为通用 unique-constraint 文本
- [ ] 11.3 确认 `transports/bifrost-http/handlers/plugins_test.go` 中 `enterprise-governance` plugin 名称期望已删除
- [ ] 11.4 确认 `transports/bifrost-http/handlers/governanceroutes_test.go` 中 `"enterprise"` 覆盖期望已删除
- [ ] 11.5 跑 `cd transports/bifrost-http && go test -short ./lib/ ./handlers/` 确认全部测试通过

**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-transports-1 | config.schema.json 同步校验通过 | 无 | `cd transports/bifrost-http && go test -run TestConfigSchemaSync ./lib/` | 通过 |
| V-transports-2 | enterprise HTTP 端点返回 404 | prepare_env 已启动 bifrost-http 实例 | curl GET /api/rbac /api/scim 等 | 全部返回 404 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：test）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（覆盖的 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`test` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase test \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/011-dev.transports-test-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/011-dev.transports-test-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/011-dev.transports-test-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation