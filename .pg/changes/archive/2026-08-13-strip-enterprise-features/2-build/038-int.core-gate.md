# Gate Assessment: int.core

**Track**: int.core
**Change**: strip-enterprise-features
**Stage**: int (integration)
**Date**: 2026-08-13
**Cycle**: 1

## 整体判定

**PASS** — gate-score: 100/100, 无 P0 失败

## 加权评分明细

| 维度 | 权重 | 分数 | 加权得分 | P0 失败条件触发? |
|------|:----:|:----:|:--------:|:----------------:|
| V-* 验证项通过率 | 50% | 100 | 50.0 | ❌ 无 V-* FAIL |
| design.md 一致性 | 20% | 100 | 20.0 | ❌ 核心 API/DTO 改动全部实现 |
| scope creep 检查 | 15% | 100 | 15.0 | ❌ 无越界修改 |
| 实现完整性 (v3.x) | 10% | 100 | 10.0 | ❌ diff 无 TODO/stub；tasks-V 全部映射 |
| 测试质量 | 0% (合并到实现完整性) | 100 | — | ❌ 强断言 strip 测试全覆盖 |
| **gate-score** | **100%** | — | **100.0** | — |

> **runtime_boot (B3 修复)**: P0 硬约束 — PASS（verify 报告含 4 步完整证据：启动/就绪/e2e/失败处置；Go 删除型重构无新组件，失败处置段 N/A 合理）

## 检查项

| # | 检查项 | 判定 | 证据来源 |
|---|--------|:----:|---------|
| 1 | 本地 V-* 全部通过或已豁免 | ✅ | verify report: 037-int.core-verify.md |
| 2 | 交付物与 design.md 一致 | ✅ | codegraph + grep 验证：26 个 enterprise-only key 删除、vault hooks 删除、SSEReaderFactory 删除、注释清理 |
| 3 | 无 scope creep | ✅ | git diff --stat 仅 core/ 23 文件修改，全部在 core/ 模块 root 内 |
| 4 | 文件位置合规：所有变更文件在本 track 模块根内 | ✅ | 23 个 core/ 文件路径前缀均为 `core/` |
| 5 | Evidence 充分 | ✅ | 每个 V-* 有原始输出（go build/curl/测试日志）；V-core-4 SKIP 有 design.md 豁免理由 |
| 6 | 旧测试无回归（含新引入分析） | ✅ | 2 个 FAIL 为预存 flakiness，在 base commit 验证失败，变更文件不涉及 bifrost_test.go / dialer_test.go |
| 7 | 证据可追溯（抽查 2 项） | ✅ | V-core-1: `go build ./...` EXIT_CODE 0 现场验证；V-transports-2: curl 返回 404 现场验证 |
| 8 | 实现完整性 (v3.x) | ✅ | diff 硬标记扫描 0 命中；tasks-V 全部映射 |
| 9 | runtime_boot (B3) | ✅ | verify 报告含 4 步：启动（restart_all_instances 原始输出）、就绪（health probe 日志）、e2e（curl 404/200）、失败处置（N/A — 删除型 Go 重构无新组件，services boot 已验证） |

## 不通过项详细说明

无 — 所有检查项 PASS。

## 各维度详细评估

### 维度 1: V-* 验证项通过率 (50%) — 100 分

| V-* | 状态 | 判定 | 说明 |
|-----|:----:|:----:|------|
| V-core-1 | Go module build passes | ✅ | `go build ./...` EXIT_CODE 0 |
| V-core-2 | Go vet passes | ✅ | `go vet ./...` EXIT_CODE 0 |
| V-core-3 | Go unit tests pass | ✅ | 2 个预存 flakiness 已确认（TestOverheadEndToEndUnary 时序敏感，单跑 PASS；TestConfigureDialer_SSRFMultiIPAllFail 环境依赖）；变更文件不涉及这两个测试 |
| V-core-4 | Real LLM provider inference | ⏭️ SKIP | design.md 预约定为 skipped；proposal.md "未做" 段列出 |
| V-transports-2 | Enterprise HTTP endpoints return 404 | ✅ | curl 现场验证：/api/rbac → 404, /api/scim → 404, /api/alerting → 404, /health → 200, /v1/models → 200 |

### 维度 2: design.md 一致性 (20%) — 100 分

design.md 中属于 `core/` 模块的 4 条后端契约全部实现：

| design.md 契约 | 实现位置 | 验证结果 |
|----------------|----------|:--------:|
| 27.1 删除 20+ enterprise-only BifrostContextKey | `core/schemas/bifrost.go` (-28 行) | ✅ grep 确认 0 命中（BifrostContextKeyClusterNodeID, SSEReaderFactory, LargePayloadReader 等全部删除） |
| 27.2 删除 4 个 vault hook + VaultStoreWriteEnabled | `core/schemas/vault.go` (-219 行) | ✅ grep 确认 0 命中（VaultResolveHook, VaultStoreWriteEnabled 等全部删除） |
| 27.3 删除 SSEReaderFactory 注入逻辑 | `core/providers/utils/sse.go` (-34 行) | ✅ grep 确认 0 命中（SSEReaderFactory 标识符在生产代码中不存在） |
| 27.4 清理 enterprise 注释 | `core/` 多个文件 | ✅ review report R-4 确认所有 `// enterprise-only` / `// set by enterprise` 注释清理 |

### 维度 3: scope creep 检查 (15%) — 100 分

- `git diff --name-only` 从 bootstrap 基准：仅 `core/` 模块的 23 个文件变更
- 所有文件以 `core/` 为前缀，在 `module_details[].root = "core"` 范围内
- 无 framework/transports/plugins/ui 越界文件（其他模块由各自 track 负责）
- 变更文件列表与 tasks.md 27.x 声明完全一致

### 维度 4: 实现完整性 (v3.x) (10%) — 100 分

**硬标记扫描**：
```bash
git diff bootstrap..HEAD | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException'
```
→ **0 命中** ✅

**tasks-V 映射核查**（人工提取 tasks.md 27.x 关键 API 关键词，在 diff 中 grep）：

| tasks.md 条目 | 关键标识符 | 生产代码状态 | 测试代码状态 |
|--------------|-----------|:----------:|:----------:|
| 27.1 删除 enterprise-only BifrostContextKey | BifrostContextKeyClusterNodeID, BifrostContextKeySSEReaderFactory, BifrostContextKeyLargePayloadReader, BifrostContextKeyGovernanceBusinessUnitID, ... | ✅ 全部删除（grep 0 命中） | ✅ 仅 enterprisestrip_test.go 引用（regex 断言已删除） |
| 27.2 删除 vault hooks + VaultStoreWriteEnabled | VaultResolveHook, VaultStoreWriteEnabled, VaultPrefixHook | ✅ 全部删除（grep 0 命中） | ✅ 仅 vaultstrip_test.go 引用（regex 断言已删除） |
| 27.3 删除 SSEReaderFactory 注入 | SSEReaderFactory | ✅ 全部删除（grep 0 命中） | ✅ 仅 ssestrip_test.go 引用（regex 断言已删除） |
| 27.4 清理 enterprise 注释 | "// enterprise-only", "// set by enterprise" | ✅ review report 确认 | — |
| 27.5 go build + vet 通过 | go build, go vet | ✅ 均 EXIT_CODE 0 | — |

**豁免列表**: 无（0 命中 TODO/FIXME/stub）

### 维度 5: runtime_boot (B3 修复) — P0 硬约束通过的 4 步证据

| 步骤 | 证据 | 原始输出/日志片段 |
|:----:|------|:----------------:|
| 1️⃣ 启动服务 | `restart_all_instances` hook 执行 | `"Stopping ui-dev (PID 854949)... {\"ok\": true, \"exit_code\": 0}"` |
| 2️⃣ 等待就绪 | Health probe 30×3s 轮询 | `"bifrost-api is ready on port 9080"` / `"ui-dev is ready on port 3008"` |
| 3️⃣ 真实 e2e | Enterprise 端点 curl + 正常端点 | `/api/rbac → 404` / `/health → 200` / `/v1/models → 200`（现场验证） |
| 4️⃣ 失败处置 | N/A — 删除型 Go 重构，无新 @Service/@Component 类 | 无新组件需要装配；services boot 已通过步骤 1-3 验证 |

### 维度 6: 回归分析校验

| 测试 | 包 | 本变更是否涉及 | 预存确认 |
|------|------|:------------:|:--------:|
| TestOverheadEndToEndUnary | core (bifrost_test.go) | ❌ 不在变更文件集 | ✅ 时序 flaky，单跑 PASS，全套 FAIL |
| TestConfigureDialer_SSRFMultiIPAllFail | core/providers/utils (dialer_test.go) | ❌ 不在变更文件集 | ✅ 环境依赖 flaky（TEST-NET 地址） |

**预存失败**: 2 个 / **新引入**: 0 个 ✅

### 维度 7: 证据抽查

抽检 2 项 V-* 并现场追溯：

| 抽检项 | 证据引用 | 现场追溯 | 判定 |
|--------|---------|:--------:|:----:|
| V-core-1: Go build passes | `EXIT_CODE: 0` | `cd core && go build ./...` → EXIT_CODE 0 | ✅ |
| V-transports-2: Enterprise 404 | `/api/rbac → 404` | `curl http://localhost:9080/api/rbac` → 404，`/health` → 200 | ✅ |

**2/2 项证据可追溯** ✅

### 维度 8: 测试质量

| 检查项 | 结果 |
|--------|:----:|
| 非条件断言（如 toBeTruthy） | ✅ 无 — 全部使用 regex + t.Fatalf + assert.False/Equal 强断言 |
| 正反两条路径覆盖 | ✅ 每个 strip 测试验证删除（正）+ 预期行为（反） |
| describe/it 命名反映业务场景 | ✅ TestEnterpriseContextKeysRemovedFromSource, TestVaultHookGlobalsRemoved, TestSSEReaderFactoryInjectionRemoved 等 |

**测试质量**: 3/3 项符合标准 ✅

## 实现完整性核查段 (v3.x)

### 1. 硬标记扫描结果

```
$ git diff bootstrap..HEAD -- 'core/**' | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException'
(无输出)
```

**判定**: 0 命中 ✅

### 2. 豁免列表

无（0 命中，无需豁免）

### 3. tasks-V 映射核查结论

全部 5 个 tasks.md 27.x 条目 (`27.1` 删除 context keys, `27.2` 删除 vault hooks, `27.3` 删除 SSEReaderFactory, `27.4` 清理注释, `27.5` build+vet 验证) 均有对应的 diff 更改和测试覆盖。**5/5 完整映射** ✅

## 结论

**PASS** — `int.core` track 门控审查通过。gate-score: **100/100**（≥ 80），无 P0 失败。实现完整性维度 0 命中 TODO/stub，tasks-V 全部映射。所有 V-* 通过或合理豁免。design.md 4 条后端契约全部实现。无 scope creep。2 个预存 flaky 测试已确认不在变更文件中。runtime_boot 4 步证据完整（启动/就绪/e2e/失败处置）。证据可追溯，测试质量合格。
