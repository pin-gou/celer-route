# dev.ui:verify — strip-enterprise-features

**Status: FAIL** (escalate — pre-existing typecheck regression blocks the track)

## Verdict

- V-ui-1: **PASS** — 0 @enterprise import references
- V-ui-2: **PASS** — vite build succeeds, `out/` produced
- V-ui-3: **FAIL** — `npm run typecheck` errors in `lib/store/apis/configApi.ts`
- V-ui-4: **PASS** — all 12 enterprise route directories absent
- V-ui-5: **PASS** — `IS_ENTERPRISE` fully removed from sidebar
- Unit tests: **DEGRADED** — 1 pre-existing test infrastructure failure in `columns.test.ts` (214/214 real tests pass)

## V-ui-1 — No @enterprise import references

**Method:** `grep -rn '@enterprise' ui/ | grep -v node_modules | grep -v RbacResource` + dedicated vitest suite

**Result:** PASS — 0 matches. The dedicated `lib/enterprise-stripping.test.ts` (4 tests, all green) confirms no live `@enterprise` imports remain in `.ts/.tsx` source files.

Evidence: see `030-dev.ui-verify-evidence.md`

---

## V-ui-2 — UI build passes

**Method:** `cd ui && npm run build`

**Result:** PASS — Vite build completes in 2.09s, `out/` directory produced with all assets, `index.html`, and fonts.

Evidence: see `030-dev.ui-verify-evidence.md`

---

## V-ui-3 — UI lint + typecheck passes

**Method:** `cd ui && npm run lint && npm run typecheck`

### lint
**Result:** PASS — `eslint --format stylish` exits 0 with 436 warnings (pre-existing, not from this diff), 0 errors.

### typecheck
**Result:** FAIL — `tsc --noEmit` exits non-zero.

```
lib/store/apis/configApi.ts(71,4): error TS2322: Type '(_arg: void, { signal }: BaseQueryApi) => Promise<...>' is not assignable to type '...'.
  Types of property 'then' are incompatible.
    Type '{ error: { status: "TIMEOUT_ERROR"; error: string; data: { error: { message: string; }; }; }; data?: undefined; }' is not assignable to type '{ error: FetchBaseQueryError; data?: undefined; meta?: FetchBaseQueryMeta | undefined; }'.
      Type '{ status: "TIMEOUT_ERROR"; error: string; data: { error: { message: string; }; }; }' is not assignable to type '{ status: "TIMEOUT_ERROR"; data?: undefined; error: string; }'.
        Types of property 'data' are incompatible.
          Type '{ error: { message: string; }; }' is not assignable to type 'undefined'.
```

**Root cause analysis:** `configApi.ts` is NOT in the strip-enterprise-features git diff. This is a pre-existing regression in the base repository. The `TIMEOUT_ERROR` branch in the `LatestRelease` endpoint has a shape `{ status: "TIMEOUT_ERROR", error: string, data: { error: { message: string } } }` that violates RTK's `FetchBaseQueryError` contract (which expects `data` to be `undefined` for `TIMEOUT_ERROR`). The `npm run build` step succeeds because Vite's esbuild doesn't enforce full TypeScript type checking — it uses `tsconfig.json` but skips some strict checks.

Evidence: see `030-dev.ui-verify-evidence.md`

---

## V-ui-4 — Enterprise routes do not exist

**Method:** `for dir in rbac scim audit-logs alerting cluster guardrails circuit-breaker mcp-tool-groups mcp-auth-config access-profiles business-units user-rankings; do ... done`

**Result:** PASS — all 12 directories are absent from `ui/app/workspace/`.

Evidence: see `030-dev.ui-verify-evidence.md`

---

## V-ui-5 — Sidebar has no IS_ENTERPRISE menu items

**Method:** `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx`

**Result:** PASS — 0 matches.

Evidence: see `030-dev.ui-verify-evidence.md`

---

## Unit test summary

**Method:** `npx vitest run`

| Metric | Value |
|--------|-------|
| Test Files | 1 failed, 9 passed (10 total) |
| Tests | 214 passed (214 total) |
| Duration | 819ms |

**Degradation note:** `columns.test.ts` fails with a Vite transform error (JSX parsing failure on `columns.tsx`). This is a pre-existing vitest infrastructure issue — neither `columns.tsx` nor `columns.test.ts` is in the strip-enterprise-features diff. The 214 actual test assertions all pass.

---

## Summary

The strip-enterprise-features change passes 4 of 5 V-ui checks cleanly. The single blocking failure is a pre-existing TypeScript type error in `lib/store/apis/configApi.ts` (RTK `TIMEOUT_ERROR` shape mismatch), unrelated to this change. This type error must be fixed before the track can advance.

| V-* | Status | Note |
|-----|--------|------|
| V-ui-1 | PASS | 0 @enterprise refs |
| V-ui-2 | PASS | build succeeds |
| V-ui-3 | FAIL | pre-existing TS error in configApi.ts |
| V-ui-4 | PASS | all enterprise routes removed |
| V-ui-5 | PASS | IS_ENTERPRISE fully purged |

**Return:** `status: "escalate"` — code bug (pre-existing type regression blocks track advance).
