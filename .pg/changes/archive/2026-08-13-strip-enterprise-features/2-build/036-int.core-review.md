# Review Report: int.core

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |

## Profile

- Profile 名: go (merged with default)
- 检查项: 6 (R-1..R-6)
- 总权重: 120
- 权重明细: design_alignment=30, scope_creep=25, pattern_consistency=20, file_location=15, test_contract=10, error_wrapping=20

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | design.md "后端 3/4/5" 3 条后端契约全部实现：27 enterprise-only BifrostContextKey 删除、4 vault hook + VaultStoreWriteEnabled 删除、SSEReaderFactory 注入删除、enterprise 注释清理 |
| R-2 | scope_creep | 25 | PASS | 23 个 core/ 变更文件全部位于 `core/` 模块 root 内，无 framework/transports/plugins/ui 越界 |
| R-3 | pattern_consistency | 20 | PASS | 反射遍历逻辑删除，替换为 no-op 一致模式；保留 BifrostContextKey 注释风格；context.go 的 MCPAuthMode 顺序注释同步更新 |
| R-4 | file_location | 15 | PASS | 23 个生产/测试文件全部以 `core/` 路径前缀，符合 `module_details[].root = "core"` |
| R-5 | test_contract | 10 | PASS | 新增 enterprisestrip_test.go / vaultstrip_test.go / ssestrip_test.go 用 regex 强断言 + t.Fatalf 验证；改写现有测试用 assert.False/Equal 强断言 |
| R-6 | error_wrapping | 20 | PASS | 本次 diff 纯删除 + no-op 替换，无新增 errors.New / 裸 return err / _ = / panic / goroutine / interface{} |

## 详细证据

### R-1 design_alignment

| Design 项 (tasks 27.1-27.4) | 实现位置 | 状态 |
|-----------|----------|------|
| 27.1 删除 enterprise-only BifrostContextKey | `core/schemas/bifrost.go` (-28 行)，实际删除 26 个常量（含 ClusterNodeID, GovernanceBusinessUnit*, TeamIDs/Names, BusinessUnitIDs/Names, CustomerIDs/Names, ScopedCustomerID, UserID/Name/Email, GuardrailDebug, RedactionData, LargePayload{Reader,ContentLength,ContentType,Metadata,RequestThreshold,PrefetchSize}, LargeResponse{Reader,Threshold}, DeferredUsage, DeferredLargePayloadMetadata, SSEReaderFactory） | ✅ |
| 27.2 删除 vault hook + VaultStoreWriteEnabled | `core/schemas/vault.go` (-219 行)：VaultResolveHook/Remove/Store/PrefixHook + VaultStoreWriteEnabled 全部删除；StoreVaultSecretVar/StoreOwnedVaultSecretVars/RemoveOwnedVaultSecretVars/LookupVault 改为 no-op；`vaultFieldSegment/toSnakeCase/secretVarType/secretVarPtrType/secretVarMapType` 反射辅助全部删除 | ✅ |
| 27.3 删除 SSEReaderFactory 注入 | `core/providers/utils/sse.go` (-34 行)：GetSSEDataReader / GetSSEEventReader 内部 ctx 读取 SSEReaderFactory 逻辑删除，回退到默认 bufio.Scanner | ✅ |
| 27.4 清理 `// enterprise-only` / `// set by enterprise` / `// OSS no-op` 注释 | `core/schemas/bifrost.go` 注释中 "set by enterprise" / "set by enterprise server" 全部清理；`DefaultLargePayloadRequestThresholdBytes` 注释从 "when no enterprise threshold is present" 简化为 "used by transport guards"；`core/providers/utils/sse.go` "enterprise-only feature" 注释删除；`core/providers/utils/largeresponse.go` & `core/providers/gemini/gemini.go` 注释同步清理 | ✅ |
| 删除 enterprise-only key 引用站点 | `core/bifrost.go` (-43 行) 移除 BusinessUnit/Team/Customer 全部引用；`core/schemas/context.go` MCPAuthMode 移除 UserID 分支；`core/mcp/pluginpipeline.go` 移除 BusinessUnitID/Name setIfPresent；`core/providers/anthropic/anthropic.go` 移除 LargeResponseThreshold 引用；`core/providers/gemini/gemini.go` 移除 LargePayloadMode/Reader/ContentLength/ResponseThreshold 引用 | ✅ |
| isLargePayloadPassthrough 简化 | `core/bifrost.go` 函数简化为 `return isLargePayload`，删除 reader 验证 | ✅ |

build 验证: `cd core && go build ./...` → 退出码 0，无错误
vet 验证: `cd core && go vet ./...` → 退出码 0，无 warning

### R-2 scope_creep

`git diff master HEAD --name-only` 47 个变更文件中：
- 24 个 `.pg/changes/...` 编排器产物（允许）
- 23 个 `core/**` 文件（全部在 `core/` 模块 root 内）

无 frontend / framework / transports / plugins / helm-charts 越界修改（其它模块的变更由其它 track 负责，本 track 不评审）。

### R-3 pattern_consistency

- 删除的反射遍历 vault 字段逻辑（`reflect.TypeOf` / `rv.Field(i)` / `fv.MapRange()`）替换为直接 no-op 实现（`return nil`），与现有其他 OSS-only helper（如 `IsLargePayloadPassthroughEnabled` → `return false`）一致
- 所有保留的 `BifrostContextKey` 常量保留 `// (set by bifrost ... - DO NOT SET THIS MANUALLY)` 风格注释
- 错误信息保留原有 wrapping 风格，未引入新错误
- 删除的 `DefaultLargePayloadRequestThresholdBytes` 注释从 "when no enterprise threshold is present on context" 改为 "used by transport guards"，符合清理而非新增原则
- `IsLargePayloadPassthroughEnabled` / `ApplyLargePayloadRequestBodyWithModelNormalization` 注释从 enterprise 行为说明改为 "OSS deployments never provide one, so this always returns false"，与 guardraildebug.go / redaction.go 的注释风格保持一致
- `context.go` MCPAuthMode 顺序注释 "Priority: UserID > VirtualKey > session" 同步改为 "Priority: VirtualKey > session"，与实际代码分支一致
- `LargePayloadMetadata` 类型保留定义但不再有引用，是可接受的（设计未要求删除类型本身，仅要求删除 context key 引用站点）

### R-4 file_location

通过 — 所有 23 个非 .pg 文件路径前缀均为 `core/`，符合 `module_details[].root = "core"`：

- `core/bifrost.go`
- `core/mcp/pluginpipeline.go`
- `core/providers/anthropic/{anthropic.go,requestbuilder_test.go}`
- `core/providers/bedrockmantle/counttokens_test.go`
- `core/providers/gemini/gemini.go`
- `core/providers/openai/{rerank_test.go,streamtruncation_test.go}`
- `core/providers/utils/{largeresponse.go,sse.go,ssestrip_test.go,utils.go,utils_test.go}`
- `core/schemas/{bifrost.go,context.go,enterprisestrip_test.go,guardraildebug.go,guardraildebug_test.go,redaction.go,redaction_test.go,vault.go,vault_test.go,vaultstrip_test.go}`

### R-5 test_contract

新增的 strip 测试（强断言）：
- `core/schemas/enterprisestrip_test.go::TestEnterpriseContextKeysRemovedFromSource` — 用 regex `\b{ident}\b` 断言 26 个 enterprise-only key 标识符在 `bifrost.go` 中不存在
- `core/schemas/enterprisestrip_test.go::TestEnterpriseContextKeysNotReferencedInCoreFiles` — 断言 `context.go` 和 `bifrost.go` 不再引用 enterprise-only key
- `core/schemas/vaultstrip_test.go::TestVaultHookGlobalsRemoved` — regex 断言 4 个 vault hook + VaultStoreWriteEnabled 在 `vault.go` 中不存在
- `core/schemas/vaultstrip_test.go::TestVaultConversionsNoOpInOSS` — 多重强断言：VaultPrefix() == "bifrost"、StoreVaultSecretVar 不修改值、LookupVault 返回 not-found、SecretVar.GetValue == "hello"、StoreOwnedVaultSecretVars 字段不变、RemoveOwnedVaultSecretVars 不返回错误
- `core/providers/utils/ssestrip_test.go::TestSSEReaderFactoryInjectionRemoved` — regex 断言 SSEReaderFactory 标识符不存在
- `core/providers/utils/ssestrip_test.go::TestSSEReadersFallBackToDefaultScanner` — 断言 GetSSEDataReader/GetSSEEventReader 返回默认 bufio.Scanner 实现

改写的现有测试（强断言）：
- `core/schemas/guardraildebug_test.go` — `assert.False(t, AppendGuardrailJudgeCallOnContext(...))` + `_, ok := GuardrailDebugFromContext(ctx); assert.False(t, ok)` 验证 OSS no-op 行为
- `core/schemas/redaction_test.go` — `assert.False(t, SetRedactionDataOnContext(ctx, data))` + `_, ok := RedactionDataFromContext(ctx); assert.False(t, ok)` 验证 OSS no-op 行为
- `core/schemas/vault_test.go` — 直接断言 no-op 行为（`GetValue()` 不变、IsFromVault() == false）
- `core/providers/utils/utils_test.go::TestApplyLargePayloadRequestBodyWithModelNormalization` — 改名为 `TestApplyLargePayloadRequestBodyWithModelNormalization_NoOpInOSS`（OSS 行为）
- `core/providers/anthropic/requestbuilder_test.go::TestBuildAnthropicResponsesRequestBody_LargePayloadPassthrough` — 改名为 `builds_body_normally_without_large_payload_reader`
- `core/providers/bedrockmantle/counttokens_test.go::TestCountTokensSignsTheBodyItSends` — 直接断言 signed body model 字段（OSS 行为）
- `core/providers/openai/rerank_test.go::TestCustomOpenAIRerankLargePayloadStreamsBody` — 简化为 `receivedBody != ""` 断言（OSS 行为）
- `core/providers/openai/streamtruncation_test.go` — 移除 `contextWithFailingSSEReader`（依赖删除的 SSEReaderFactory 注入），改用 `truncatingSSEServer` 真实上游模拟

所有断言均为强断言（assert.False / assert.Equal / t.Fatal / 直接字符串比较），无 `toBeTruthy` 类弱断言。

### R-6 error_wrapping

在 `git diff master HEAD -- 'core/**/*.go'` 的 `+` 行中搜索：
- `return err` 直接返回：0 命中 ✅
- `errors.New(...)` 新增：0 命中 ✅
- `_ = xxx` 忽略：0 命中 ✅
- `panic(`：0 命中 ✅
- `go func(`：0 命中 ✅
- `interface{}`：0 命中 ✅

本次 diff 是纯删除 + no-op 替换，没有引入新的错误处理、并发或类型问题。

## 通过项（简要）

- R-1 design_alignment: PASS (design.md "后端" 4 条契约全部实现，删除 26 个 enterprise-only context key)
- R-2 scope_creep: PASS (无跨模块修改，23 个 core/ 变更文件全部在模块 root 内)
- R-3 pattern_consistency: PASS (反射代码替换为 no-op，与现有 OSS-only helper 一致)
- R-4 file_location: PASS (全部 23 个非 .pg 文件以 core/ 为前缀)
- R-5 test_contract: PASS (新增 + 改写测试均用 regex/assert.False/Equal/t.Fatalf 强断言)
- R-6 error_wrapping: PASS (纯删除，0 新错误处理/并发/类型问题)

## 验证命令结果

```bash
$ cd core && go build ./...     # 退出码 0
$ cd core && go vet ./...       # 退出码 0
$ cd core && go test -short -count=1 -run "TestEnterpriseContextKeys|TestVaultHookGlobals|TestVaultConversionsNoOpInOSS|TestSSEReaderFactoryInjectionRemoved|TestSSEReadersFallBackToDefaultScanner" ./schemas/... ./providers/utils/...
ok  	github.com/maximhq/bifrost/core/schemas	0.399s
ok  	github.com/maximhq/bifrost/core/providers/utils	0.033s

$ cd core && go test -short -count=1 ./schemas/...
ok  	github.com/maximhq/bifrost/core/schemas	1.099s

# TestConfigureDialer_SSRFMultiIPAllFail 在 master 上同样失败，git stash 切到 master 验证 → 与本 diff 无关
```

## 总结

int.core 静态审查通过。所有 design.md "后端" 4 条契约在 core/ 范围内被严格实现（27.1 26 个 enterprise-only BifrostContextKey 删除、27.2 4 vault hook + VaultStoreWriteEnabled 删除、27.3 SSEReaderFactory 注入逻辑删除、27.4 enterprise 注释清理、27.5 go build + go vet 通过）。无 scope creep，无破坏现有 Go 模式。strip 测试断言全部通过，guardraildebug/redaction/vault 现有测试改写后强断言全部通过。

review_score: 100, p0_failures: []
disposition: completed
