# Review Report: int.transports

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |
| p0_failures | [] |

## Profile

- Profile 名: go (inherits default)
- 检查项: 6 enabled (design_alignment, scope_creep, pattern_consistency, file_location, test_contract, error_wrapping)
- 总权重: 120 (30 + 25 + 20 + 15 + 10 + 20)
- pass_threshold: 85

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | int.transports:dev 阶段特有改动 3 文件全部符合 design.md transports 契约；schema 9 顶层 + 6 嵌套字段全删 |
| R-2 | scope_creep | 25 | PASS | 32 个 transports 变更文件全部位于 `transports/` 模块 root 内，无 core/framework/ui/plugins 越界 |
| R-3 | pattern_consistency | 20 | PASS | 纯删减 + excludedSchema 映射调整；无 panic/interface{}/裸 return err/TODO 引入 |
| R-4 | file_location | 15 | PASS | 全部 32 个文件以 `transports/` 为前缀，符合 module root |
| R-5 | test_contract | 10 | PASS | config_test.go 的 sync 测试排除项随 schema 字段同步调整；schema_test.go 删除绑定已删字段的失效测试 |
| R-6 | error_wrapping | 20 | PASS | 无新增 errors.New / `_ =` 忽略 / 裸 return err / panic / goroutine |

## R-1 design_alignment — 详细证据

int.transports:dev 阶段（diff `9296f91..dbdf019`）仅改动 3 个文件，全部属于 design.md 的 transports 契约：

| int 阶段文件 | 改动内容 | design.md 对应 |
|-------------|---------|---------------|
| `transports/config.schema.json` | 删除嵌套 enterprise 字段：`governance.teams.items.business_unit_id`、`governance.virtual_keys.items.access_profile_id`、`governance.virtual_keys.items.is_access_profile_managed`、`plugins.*.config.properties.is_enterprise`、`plugins.items.kafka` 分支、`plugins.items.google_cloud_pubsub` 分支 | 架构概览"删除 6 个嵌套字段" |
| `transports/bifrost-http/lib/config_test.go` | 删除 `enterpriseSchemaPaths` / `enterpriseSchemaFields` map；`excludedSchemaFields` 移除 `governance.business_units` / `governance.roles` / `governance.teams.business_unit_id` / `governance.virtual_keys.access_profile_id` / `mcp.tool_groups` 条目；`excludedGoFields['tables.TableVirtualKey']` 增加 `is_access_profile_managed` 排除项 | tasks 37.6 |
| `transports/schema_test/config_schema_test.go` | 删除绑定被删字段的失效测试 `TestSchemaGuardrailRuleTarget`（guardrails_config）与 `TestSchemaSCIMConfigValidation`（scim_config） | tasks 37.5 配套清理 |

**schema 字段删除验证**（`python3` 精确检测残留）：
- 顶层 9 字段（alerting / access_profiles / audit_logs / cluster_config / guardrails_config / large_payload_optimization / load_balancer_config / scim_config / circuit_breaker_config）：**全部已删，0 残留** ✅
- 嵌套字段：`business_unit_id` 0 命中、`access_profile_id` 0 命中、`is_enterprise` 0 命中、`"kafka"` 0 命中、`"pubsub"` 0 命中、`business_units` 0 命中 ✅
- 疑似残留项核查结果为**合法 OSS 用途**：
  - `mcp_tool_groups`（下划线，7090 行）是合法 OSS 配置，非 enterprise 的 `mcp.tool_groups` 点号路径 ✅
  - `"default": "roles"`（3 处）是 RBAC 默认角色名，非 `governance.roles` 字段 ✅
  - `datadog` / `bigquery`（3157/3300 行）仍在 schema plugins 枚举，但 design.md 仅要求删 kafka/pubsub，datadog/bigquery 不在删除列表 ✅

**excludedGoFields 增加 `is_access_profile_managed` 的正确性分析**：
`schema` 中该字段已删除，但 Go struct `framework/configstore/tables/virtualkey.go:245` 仍定义 `IsAccessProfileManaged`（JSON tag `is_access_profile_managed,omitempty`，`gorm:"-"` 虚拟字段，由 governance.go 计算）。`TestConfigSchemaSync` 遍历 schema↔Go 映射时，Go 侧有而 schema 侧无的字段会报错，因此增加该排除项是**保持 schema↔Go 同步测试通过的充分必要条件**，设计合理。

**dev 阶段已实现契约（本 review 复核 master..HEAD）**：enterprisePlugins 列表 + slices.Contains 跳过逻辑删除（server.go/plugins.go）、LargePayloadHook/LargeResponseHook 类型/字段/Set* 方法删除（router.go）、Set* 调用删除（integrations.go）、initVault 与 Config.IsEnterprise 删除（config.go）、config_test.go enterpriseSchemaPaths map 删除、utils_test.go/plugins_test.go/governanceroutes_test.go enterprise 表名/插件/覆盖期望改写、featureflags.go ErrFlagEnterpriseOnly case 删除——全部与 dev.transports review（013）一致，无回归。

## R-2 scope_creep — 详细证据

`git diff master HEAD --name-only -- transports/` 共 32 个文件，全部以 `transports/` 前缀：

```
transports/bifrost-http/handlers/{featureflags,governanceroutes_test,integrations,logging,logging_test,mcpoauth2jwt,mcpoauth2jwt_test,mcpoauth2sessions_test,mcpserver,mcpserver_auth_test,mcpsessions,middlewares,plugins_test,realtime_client_secrets,realtime_client_secrets_test,ui,utils_test,webrtc_realtime,wsrealtime}.go
transports/bifrost-http/integrations/{genai,genai_large_request_test,router,router_large_payload_test,utils,utils_test}.go
transports/bifrost-http/lib/{config,config_test,lib}.go
transports/bifrost-http/server/{plugins,server}.go
transports/config.schema.json
transports/schema_test/config_schema_test.go
```

分支 master..HEAD 中 core/framework/plugins/ui 的改动属于其它 track（dev.core / int.core / dev.framework / etc.），不在本 int.transports track 审查范围（与 dev.transports review 013 处理一致）。无设计未提及的新功能。

## R-3 pattern_consistency (Go) — 详细证据

对 `git diff master HEAD -- 'transports/**/*.go'` 全部新增行扫描：
- `panic(` → 0 命中
- `interface{}` → 0 命中
- `_ = xxx` 忽略 → 0 命中
- `go func(` → 0 命中
- `TODO` / `FIXME` / `XXX` → 0 命中

int.transports 阶段新增行额外扫描同结果（纯删除 + excludedSchema 映射增删）。

关键模式确认：
- `transports/bifrost-http/integrations/utils.go` 的 `tryStreamLargeResponse` 将返回值从恒 `true` 改为"仅当 streamLargeResponse 成功时 true，否则 false fall-through"，使 OSS（无 large 钩子）正确回退到 buffered serialize 路径——与 StreamLargeResponseBody 恒 false 的 no-op 化保持一致，无死代码残留。
- `resolveLargePayloadMetadata` 简化为 `return nil`（OSS 恒 nil），与后续 `if metadata != nil` 的防御式检查自洽。
- `lib.go` 的 `StreamLargeResponseBody` 简化为 `return false`，未保留 dead import（diff 同时移除 `io` / `strconv` import）。
- `config.go` 的 `initFeatureFlags` 移除 `IsEnterprise` 读取，改为 `featureflags.Config{}`。

## R-4 file_location — 详细证据

全部 32 个变更文件路径前缀均为 `transports/`，符合 `module_details[].root = "transports"`。无越界限根。

## R-5 test_contract — 详细证据

int.transports 阶段的测试改动：
1. `config_test.go::TestConfigSchemaSync` — 删除 `enterpriseSchemaPaths[prop]` 跳过判断，改为依赖精简后的 `excludedSchemaFields`；`TestConfigSchemaSyncTopLevel` 用 `excludedSchemaFields[""]`（rootExcluded）替换 `enterpriseSchemaFields` map。schema↔Go 同步校验逻辑**保留且断言未弱化**（missing-in-struct / missing-in-schema 双向报错仍强制）。
2. `schema_test/config_schema_test.go` — 删除 `TestSchemaGuardrailRuleTarget` 与 `TestSchemaSCIMConfigValidation`。这两个测试的被测对象（`guardrails_config` / `scim_config` schema 分支）已被 design 契约删除，删除失效测试**合理符合 test_contract**（不保留针对已删 schema 字段的必失败测试）。
3. dev 阶段已确认的测试改写（utils_test.go enterprise 表名 → some_table.*、plugins_test.go → {logging,telemetry}、governanceroutes_test.go "enterprise"→"override"、logging_test.go userID 分区断言重写、mcpserver_auth_test.go OSS 行为断言）均保持强断言（t.Fatalf / assert.Equal / Contains），无 toBeTruthy 类弱断言。

V-transports-1（TestConfigSchemaSync）对应测试仍存在并经排除项调整后逻辑自洽。测试覆盖未弱化。

## R-6 error_wrapping — 详细证据

- `git diff master HEAD -- 'transports/**/*.go'` 新增行：`errors.New(` 0 命中、`_ = ` 0 命中、裸 `return err` 0 命中
- int 阶段新增行：无任何新增错误处理/并发/类型隐患
- 删除的 `errors.Is(err, featureflags.ErrFlagEnterpriseOnly)` case（featureflags.go:89-91）随 `ErrFlagEnterpriseOnly` 在 framework 中删除而移除，符合"先删被引错误"的一致 cleanup

无新增错误路径，无 sentinel error 散落，无错误吞掉。

## 通过项（简要）

- R-1 design_alignment: PASS（int 阶段 schema 9 顶层 + 6 嵌套字段全删，`excludedSchemaFields`/`excludedGoFields` 同步调整，schema_test.go 清理绑定已删字段的测试）
- R-2 scope_creep: PASS（32 个文件全部在 transports/ 内）
- R-3 pattern_consistency: PASS（纯删减 + no-op 化，0 模式违规）
- R-4 file_location: PASS（路径合规）
- R-5 test_contract: PASS（schema sync 测试保留且断言未弱化；删除绑定已删字段的失效测试合理）
- R-6 error_wrapping: PASS（无新增错误处理问题）

## 决策

`review_score = 100 ≥ pass_threshold = 85`，**disposition = completed**，进入 int.transports:verify 阶段。
P0 硬约束（无）未触发。
