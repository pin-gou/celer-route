# dev.transports verify evidence
# Change: strip-enterprise-features
# Track: dev.transports
# Date: 2026-08-13

## Step 1: Service startup (bifrost-api)

$ python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role bifrost-api --instance bifrost-api-1 \
    --action start --skill pg-build

Output:
重新构建 bifrost-http...
Waiting for bifrost-api to be ready on port 9080...
[32mbifrost-api is ready on port 9080[0m
{"ok": true, "exit_code": 0}

## Step 2: Health check (bifrost-api)

$ curl -s -f http://localhost:9080/health
(output not captured, but startup succeeded)

## Step 3: lint (go vet)

$ cd transports/bifrost-http && go vet ./...

(no output = success)
EXIT_CODE: 0

## Step 4: unit tests (go test -short)

$ cd transports/bifrost-http && go test ./... -short -count=1

Output:
?   	github.com/maximhq/bifrost/transports/bifrost-http	[no test files]
ok  	github.com/maximhq/bifrost/transports/bifrost-http/handlers	21.677s
ok  	github.com/maximhq/bifrost/transports/bifrost-http/integrations	0.275s

(TIMEOUT on full suite but sub-packages passed)
Note: The timeout was on the full suite run. Sub-packages handlers and integrations both PASS.

## Step 5: V-transports-1 (config.schema.json sync)

$ cd transports/bifrost-http && go test -run TestConfigSchemaSync ./lib/

Output:
--- FAIL: TestConfigSchemaSync (0.01s)
    config_test.go:18265: Schema sync errors found (5 total):
        [mcp.client_configs] Field 'oauth_authorize_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_registration_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_resource' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_scopes' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_token_url' in schemas.MCPClientConfig but missing from schema
FAIL
FAIL	github.com/maximhq/bifrost/transports/bifrost-http/lib	0.076s
EXIT_CODE: 1

## Step 6: V-transports-2 (enterprise endpoints return 404)

$ for path in /api/rbac /api/scim /api/audit-logs /api/alerting /api/cluster /api/guardrails /api/circuit-breaker /api/access-profiles; do
  status=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:9080$path")
  echo "GET $path → HTTP $status"
done

Output:
GET /api/rbac → HTTP 404
GET /api/scim → HTTP 404
GET /api/audit-logs → HTTP 404
GET /api/alerting → HTTP 404
GET /api/cluster → HTTP 404
GET /api/guardrails → HTTP 404
GET /api/circuit-breaker → HTTP 404
GET /api/access-profiles → HTTP 404

## Step 7: git status (branch verification)

$ git branch --show-current
feat/pg/strip-enterprise-features

## Step 8: UI dev server check

$ netstat -tlnp | grep 3008
tcp        0      0 127.0.0.1:3008          0.0.0.0:*               LISTEN      797039/node

UI dev server running on port 3008.
