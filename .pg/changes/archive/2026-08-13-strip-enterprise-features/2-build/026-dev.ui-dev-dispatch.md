## 任务：dev.ui

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.ui
- track.modules: ['ui']
- track.max_fix_retries: 5

### Module 配置
  - module: ui
    - root: ui
    - language: typescript
    - build: npm run build
    - lint: npm run lint
    - test.unit: npx vitest run

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: npx vitest run


---

- stage.environment.instances:
```yaml
bifrost-api:
- name: bifrost-api-1
  host: localhost
  port: 9080
ui-dev:
- name: ui-dev-1
  host: localhost
  port: 3008
```

- stage.environment.hooks:
```yaml
bifrost-api:
  start:
    host: localhost
    script: .pg/hooks/bifrost-api-start.sh
  stop:
    host: localhost
    script: .pg/hooks/bifrost-api-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/bifrost-api-health-check.sh
ui-dev:
  start:
    host: localhost
    script: .pg/hooks/ui-dev-start.sh
  stop:
    host: localhost
    script: .pg/hooks/ui-dev-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/ui-dev-health-check.sh
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session strip-enterprise-features --env local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---

### 实现要求

本阶段需要根据 design.md 实现功能代码。遵循以下原则：
- 只创建和修改生产代码
- 不删除已有测试
- 确保测试仍然通过
- 如果现有测试需要修改以适配新实现，那也是允许的

### 模块路径约束

禁止编辑以下路径的文件：
- 不在 ['ui'] 路径下的文件

### 完善清单

- [ ] 所有 checkstyle / lint 检查 0 警告
- [ ] 所有单元测试 PASS
- [ ] 集成测试 PASS（如环境可用）

---

### 执行要求

**任务清单**：
## 22. dev.ui:dev - 实现开发

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=ui (常驻, 无 on_conditions)
-->

- [ ] 22.1 删除 `ui/app/_fallbacks/enterprise/` 整个目录（24 个 components 子目录 + lib 全部）
- [ ] 22.2 删除 `tsconfig.json` 中 `paths."@enterprise/*"` 与 `paths."@schemas/*"` 配置
- [ ] 22.3 删除 `vite.config.mts` 中 `isEnterpriseBuild` 检测、`alias` 段中的 `@enterprise` 与 `@schemas`、`BIFROST_IS_ENTERPRISE` 环境变量注入
- [ ] 22.4 删除 `ui/app/workspace/{rbac,scim,audit-logs,alerting,cluster,guardrails,circuit-breaker,mcp-tool-groups,mcp-auth-config,access-profiles,business-units,users,teams,customers,prompt-deployments,user-rankings,adaptive-routing,edge-control,config/license,config/branding,config/proxy}` 路由的 `page.tsx` 与 `layout.tsx`
- [ ] 22.5 把 `ui/app/_fallbacks/enterprise/components/api-keys/apiKeysIndexView.tsx` 搬到 `ui/app/workspace/config/api-keys/views/apiKeysView.tsx`，调整 import 路径
- [ ] 22.6 把 `ui/app/_fallbacks/enterprise/components/login/loginView.tsx` 搬到 `ui/app/login/views/loginView.tsx`，调整 import 路径
- [ ] 22.7 删除 `ui/components/entitySelectors/customerSelector.tsx` 与 `teamSelector.tsx`
- [ ] 22.8 创建 `ui/lib/rbac.ts`，定义 `useRbac()` 返回 true、`RbacResource` / `RbacOperation` enum（仅 OSS 用到的子集）
- [ ] 22.9 重写 100+ 个 `@enterprise/lib`、`@enterprise/components`、`@enterprise/types` import
- [ ] 22.10 修改 `ui/app/workspace/virtual-keys/views/virtualKeySheet.tsx` 与 `virtualKeysTable.tsx`，移除 customer/team picker，移除 `CustomerSelector` / `TeamSelector` 引用
- [ ] 22.11 修改 `ui/app/workspace/routing-rules/views/routingRuleSheet.tsx`、`routingRuleInfoSheet.tsx`、`tree/views/node/rfRuleNode.tsx`，scope 收窄为 `virtual_key / global`，删除 customer/team/user scope 分支
- [ ] 22.12 删除 `ui/components/sidebar.tsx` 中 Proxy、Branding、License Info 三个 IS_ENTERPRISE 条件菜单项
- [ ] 22.13 删除 `ui/lib/constants/config.ts` 中 `IS_ENTERPRISE` 常量定义
- [ ] 22.14 清理 `ui/lib/store/store.ts`、`ui/lib/store/slices/index.ts`、`ui/lib/store/apis/baseApi.ts`、`ui/app/clientLayout.tsx` 中 enterprise 引用
- [ ] 22.15 删除 `ui/lib/registries/userPicker.tsx` 与 `modelLimitScopes.tsx` 中 enterprise fallback 注册项
- [ ] 22.16 删除 `ui/app/_fallbacks/enterprise/components/views/contactUsView.tsx`（不再被任何组件引用）
- [ ] 22.17 跑 `cd ui && npm run build` 验证通过
- [ ] 22.18 跑 `cd ui && npm run lint && npm run typecheck` 验证通过

**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-ui-1 | 无 @enterprise 引用残留 | ui/ 目录就绪 | `grep -r '@enterprise' ui/ \| wc -l` | 命中数为 0（除 rbac.ts 中的 RbacResource 字面定义） |
| V-ui-2 | UI build 通过 | node_modules 已安装 | `cd ui && npm run build` | 退出码 0，产出 out/ |
| V-ui-3 | UI lint 与 typecheck 通过 | node_modules 已安装 | `cd ui && npm run lint && npm run typecheck` | 退出码 0，无错误 |
| V-ui-4 | enterprise 路由不存在 | ui/ 目录就绪 | `ls ui/app/workspace/{rbac,scim,audit-logs,alerting,cluster,guardrails,circuit-breaker,mcp-tool-groups,mcp-auth-config,access-profiles,business-units,user-rankings}` | 所有目录不存在 |
| V-ui-5 | 侧边栏无 enterprise 菜单项 | ui/ 目录就绪 | `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` | 命中数为 0 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：dev）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（实现的 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`dev` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase dev \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/026-dev.ui-dev-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/026-dev.ui-dev-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/026-dev.ui-dev-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation