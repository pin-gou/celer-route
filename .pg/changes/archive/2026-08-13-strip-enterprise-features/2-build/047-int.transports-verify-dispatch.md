## 任务：int.transports

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: int.transports
- track.modules: ['transports']
- track.max_fix_retries: 5

### Module 配置
  - module: transports
    - root: transports
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: cd transports/bifrost-http && go test ./... -short -count=1
    - test.integration: make test

### Stage 配置
- stage.name: int
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: cd transports/bifrost-http && go test ./... -short -count=1


---

- stage.environment.instances:
```yaml
bifrost-api:
- name: bifrost-api-1
  host: localhost
  port: 9080
ui-dev:
- name: ui-dev-1
  host: localhost
  port: 3008
```

- stage.environment.hooks:
```yaml
bifrost-api:
  start:
    host: localhost
    script: .pg/hooks/bifrost-api-start.sh
  stop:
    host: localhost
    script: .pg/hooks/bifrost-api-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/bifrost-api-health-check.sh
ui-dev:
  start:
    host: localhost
    script: .pg/hooks/ui-dev-start.sh
  stop:
    host: localhost
    script: .pg/hooks/ui-dev-stop.sh
  health_check:
    host: localhost
    script: .pg/hooks/ui-dev-health-check.sh
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session strip-enterprise-features --env local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---



---

### 执行要求

**任务清单**：
## 39. int.transports:verify - int 集成验证

<!-- on_conditions_eval:
     stage=int (常驻, 无 on_conditions)
     track=transports (常驻, 无 on_conditions)
-->

- [ ] 39.1 执行 lint（runner 通过 modules 注入命令）
- [ ] 39.2 执行测试（runner 通过 modules 注入命令）
- [ ] 39.3 启动服务（如需）
- [ ] 39.4 验证 V-transports-N：来自 design.md（N 由 design.md 决定，非章节号）

  **Evidence 要求**（verify agent 在验证报告中产出，gate agent 据此评审）：
  - 每个 V-* 必须有对应的原始输出（curl 响应 / 命令行输出 / 日志片段）
  - SKIP 的 V-* 必须注明豁免理由
  - 测试结果（Tests run: N, Failures: 0, Errors: 0）必须有日志摘要

  **define-summary 对账**（自动生成）:
  - verifiable: V-ui-1, V-ui-2, V-ui-3, V-core-1, V-core-2, V-core-3, V-transports-1, V-plugins-1, V-helm-1
  - degraded: V-ui-4, V-ui-5, V-transports-2
  - skipped: V-core-4

**验证要求**：

---

## 集成验证硬性约束（v3.x）

当前 stage 的 `environment.required=true`，**集成验证不可 SKIP**。

### 约束规则

1. **禁止 SKIP**：任何 V-* 验证项都必须真实运行，不能标记 SKIP 豁免
2. **启动失败重试**：服务启动失败最多重试 3 次，超限返回 `status: "fail"`
3. **立即 escalate**：V-* 项发现代码 bug 时**立即**返回 `status: "escalate"`，
   不要 accumulate 多项失败后再 escalate
4. **环境问题 = 修复环境**：环境无法启动属于配置/依赖问题，由人工修复；

### 验证项

以下 V-* 项必须全部执行（不能 SKIP，不能 partial）：

## 39. int.transports:verify - int 集成验证

<!-- on_conditions_eval:
     stage=int (常驻, 无 on_conditions)
     track=transports (常驻, 无 on_conditions)
-->

- [ ] 39.1 执行 lint（runner 通过 modules 注入命令）
- [ ] 39.2 执行测试（runner 通过 modules 注入命令）
- [ ] 39.3 启动服务（如需）
- [ ] 39.4 验证 V-transports-N：来自 design.md（N 由 design.md 决定，非章节号）

  **Evidence 要求**（verify agent 在验证报告中产出，gate agent 据此评审）：
  - 每个 V-* 必须有对应的原始输出（curl 响应 / 命令行输出 / 日志片段）
  - SKIP 的 V-* 必须注明豁免理由
  - 测试结果（Tests run: N, Failures: 0, Errors: 0）必须有日志摘要

  **define-summary 对账**（自动生成）:
  - verifiable: V-ui-1, V-ui-2, V-ui-3, V-core-1, V-core-2, V-core-3, V-transports-1, V-plugins-1, V-helm-1
  - degraded: V-ui-4, V-ui-5, V-transports-2
  - skipped: V-core-4

### 启动失败处理

1. 第 1-2 次失败：检查日志，尝试修复后再启动
2. 第 3 次失败：返回 `status: "fail"`，错误码 `ENV_STARTUP_RETRY_EXCEEDED`

### 🔴 运行时启动 + 健康探针（B3 修复，v3.x 强制项）

2026-08-02 `terminal-command-whitelist-guard` 暴露 verify 阶段只跑单元测试、
不启动服务，导致 `TerminalCommandGuardService` 多构造函数缺 `@Autowired` /
`WhitelistMatcher` 生产路径 stub 等 P0 bug 全部漏到 int.scr 才暴露。

**执行顺序**（所有 V-* 验证项**之前**必做）：

1. **启动服务**（若当前未运行）：
   ```bash
   python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
       --session strip-enterprise-features --env local --action restart_all_instances \
       --skill pg-build
   ```
2. **等待就绪**：30 次 × 3s 轮询 HTTP/进程探针（backend → curl 探针，
   agent/agent-vm → systemctl is-active）
3. **真实 e2e**（至少 1 次）：触发本次变更的核心链路（终端 WebSocket / CRUD /
   CLI），断言输出与预期一致
4. **失败处置**：启动失败 / 探针 30 次未就绪 / e2e 不符合预期 → 立即报告
   `service_startup FAIL`，返回 `status: "escalate"`，所有 V-* 记 FAIL。

**写盘要求**：报告中必须含上述 4 步的原始输出或日志片段，缺一项 gate
agent 应判 verify 不合格。

### Spring Bean 装配自查（runtime_boot check 辅助项）

对本次 git diff 中**新增/修改的 @Service/@Component 类**：

- 统计非 private 构造函数数量；若 > 1，必须至少一个标注 `@Autowired`，
  否则 `NoSuchMethodException: <Class>.<init>()`（Spring 无法装配）
- 检查构造参数类型是否注册为 bean（同模块内有 @Service/@Component 或
  starter 自动装配）
- 检查是否存在"测试替身 + 生产 stub"模式（如 `if (testRules != null) return
  testRules; else return Collections.emptyList();`）

任一命中 → 报告 `runtime_boot FAIL`，返回 escalate。


---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：verify）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | **必填**（报告文件绝对路径，文件必须已写盘） |
| `--evidence` | **至少 1 个**（证据文件绝对路径，可多次传） |
| `--tasks-updated` | 仅 `status=escalate` 时**必填**（失败 V-* ID 列表）；`completed` 时不必填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`verify` | `completed` / `escalate` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase verify \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/047-int.transports-verify-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/047-int.transports-verify-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/047-int.transports-verify-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation