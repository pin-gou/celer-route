# int.core:verify Report

**Status**: PASS
**Track**: int.core
**Change**: strip-enterprise-features
**Stage**: int (integration)
**Date**: 2026-08-13

---

## Summary

`int.core` track 集成验证通过。V-core-1/2/3 全部 PASS，V-core-4 按 design.md 豁免跳过，V-transports-2 企业端点全部返回 404，core 服务启动健康探针正常。发现 2 个测试失败均为**预存 flakiness**，在 base commit (9e93b94) 上验证同样失败，与本变更无关。

---

## V-* Results

### ✅ V-core-1: Go module build passes
- **Method**: `cd core && go build ./...`
- **Expected**: exit 0
- **Actual**: exit 0, no errors
- **Evidence**: EXIT_CODE: 0

### ✅ V-core-2: Go vet passes
- **Method**: `cd core && go vet ./...`
- **Expected**: exit 0, no warnings
- **Actual**: exit 0, no output
- **Evidence**: no vet warnings

### ✅ V-core-3: Go unit tests pass
- **Method**: `cd core && go test ./... -short -count=1`
- **Expected**: all tests pass
- **Actual**: 2 pre-existing failures (see below), all others pass
- **Evidence**: TestOverheadEndToEndUnary and TestConfigureDialer_SSRFMultiIPAllFail FAIL; confirmed pre-existing by running on base commit 9e93b94

#### Pre-existing test failures (NOT caused by this change)
| Test | Package | Failure | Base commit verified |
|------|---------|---------|----------------------|
| TestOverheadEndToEndUnary | core | overhead=233ms > half of 441ms total | ✅ FAILS on 9e93b94 |
| TestConfigureDialer_SSRFMultiIPAllFail | core/providers/utils | unroutable TEST-NET connection | ✅ FAILS on 9e93b94 |

### ⏭️ V-core-4: Real LLM provider inference — SKIPPED
- **define-summary**: `skipped`
- **Reason**: design.md explicitly lists this as skipped; proposal.md "未做" section covers it
- **No verification performed**

### ✅ V-transports-2: Enterprise HTTP endpoints return 404
- **Method**: `curl` against running bifrost-api on port 9080
- **Enterprise endpoints tested**:
  - `/api/rbac` → 404 ✓
  - `/api/scim` → 404 ✓
  - `/api/alerting` → 404 ✓
  - `/api/access-profiles` → 404 ✓
  - `/api/business-units` → 404 ✓
- **Normal endpoints sanity check**:
  - `/health` → 200 ✓
  - `/v1/models` → 200 ✓

---

## Service Startup Verification (Runtime Boot Check)

### Step 1: Start services
```bash
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session strip-enterprise-features --env local --action restart_all_instances --skill pg-build
```
**Result**: ✅ bifrost-api and ui-dev both started successfully

### Step 2: Health probe (30 × 3s polling)
- `bifrost-api` (port 9080): ✅ HTTP 200 on `/health`
- `ui-dev` (port 3008): ✅ HTTP 200 on `/`

### Step 3: Core functionality e2e
- Enterprise endpoint strip: ✅ all return 404
- Normal endpoints: ✅ /health and /v1/models return 200

### Step 4: Spring Bean / Runtime Boot
- **N/A**: strip-enterprise-features is a deletion-only refactor. No new `@Service`/`@Component` classes added. No constructor changes. No test doubles or stub patterns introduced.

---

## define-summary Reconciliation

| Category | Items | Status |
|----------|-------|--------|
| verifiable | V-core-1, V-core-2, V-core-3, V-transports-1, V-plugins-1, V-helm-1 | ✅ verified |
| degraded | V-ui-4, V-ui-5, V-transports-2 | ✅ V-transports-2 PASS, V-ui-4/5 handled by ui track |
| skipped | V-core-4 | ⏭️ design.md pre-agreed skip |

---

## Execution Checklist

- [x] 29.1 Lint (go vet)
- [x] 29.2 Tests (go test ./... -short)
- [x] 29.3 Start services (restart_all_instances)
- [x] 29.4 V-core-N verification
- [x] Health probe (30 × 3s polling)
- [x] Enterprise endpoint curl tests
- [x] Spring Bean / runtime boot check (N/A — no new components)
- [x] Evidence file written
- [x] Report file written

---

## Conclusion

**PASS** — `int.core` track verified. Core module builds, lints, and passes unit tests (pre-existing flaky failures excluded). Enterprise-only HTTP endpoints return 404. Services healthy. No new issues introduced by strip-enterprise-features change.
