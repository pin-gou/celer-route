## scenario-fix Report (fix_cycle=3)

### fix_cycle
3

### 失败的 Scenario
S-bifrost-api-no-enterprise-endpoints-registered (critical=false)
S-bifrost-api-routing-rules-list (critical=false)

### 根因分类
design_drift

### 根因审查结论（rollback context 要求：优先审查根因是否已修复）

**已修复。** 在 escalate 阶段（auto-record 提交 c7565b6）中，scenario-scenario.yaml 已修正：

1. **S-bifrost-api-no-enterprise-endpoints-registered**: 
   - teams/customers 期望从 404 修正为 200（OSS 保留功能，与 proposal 一致）
   - users/alerting 保持 404（enterprise-only 已剥离）
   - then 断言从"全部端点返回 404"改为"enterprise-only 端点返回 404 / OSS 保留端点返回 200"

2. **S-bifrost-api-routing-rules-list**:
   - URL 从 `/api/routing-rules` 修正为 `/api/governance/routing-rules`
   - 期望状态保持 200

### 根因分析（三线确认）

1. **代码未变**：本变更为纯删除型 refactor，teams/customers 路由为 OSS 保留功能（proposal 明确保留），routing-rules 路由正确注册于 `/api/governance/` 前缀下。代码行为完全符合 proposal 设计意图。

2. **API 契约实测验证**：
   - `GET /api/governance/users` → 404 ✅
   - `GET /api/alerting/rules` → 404 ✅
   - `GET /api/governance/teams` → 200 ✅
   - `GET /api/governance/customers` → 200 ✅
   - `GET /api/governance/routing-rules` → 200 ✅，body 不含 business_unit_id
   - `GET /api/routing-rules` → 404 ✅（确认旧路径不再绑定）

3. **scenario-scenario.yaml diff 验证**：c7565b6 提交修正了 30 行，上述断言修正全部到位。

### 判定
两个失败 Scenario 均为 scenario-assertion 设计缺陷（scenario 文件假设与实际 proposal 契约不一致），非代码缺陷。scenario-scenario.yaml 已在 escalate 阶段由编排器修正（c7565b6），YAML 内容与实测 API 行为完全一致。重跑 scenario-execute 应全部通过。

### 修改的文件
无（design_drift 路径，不改代码；scenario-scenario.yaml 修正已在 escalate 提交 c7565b6 完成，属于 SSOT 维护方，非本 agent 修改）

### 单元测试证据
N/A — 不改代码，未跑测试

### 修复策略
design_drift：不改源码，record `--fix-root-cause design_drift` + `--design-drift` JSON（decision=ACCEPT）。scenario-scenario.yaml 已由 orchetrator 在 escalate 阶段修正（c7565b6），重跑 scenario-execute 后该场景应全部通过。
