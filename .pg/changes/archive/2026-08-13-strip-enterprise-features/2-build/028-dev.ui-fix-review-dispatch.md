## 任务：dev.ui

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: dev.ui
- track.modules: ['ui']
- track.max_fix_retries: 5

### Module 配置
  - module: ui
    - root: ui
    - language: typescript
    - build: npm run build
    - lint: npm run lint
    - test.unit: npx vitest run

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: npx vitest run


---

### 必读源报告（review ESCALATE 派发）

- **源 review 报告**: .pg/changes/strip-enterprise-features/2-build/027-dev.ui-review.md

请用 Read 工具逐字读取该文件。报告包含 review agent 记录的
ESCALATE Issue 详情（R-* 项）、每项检查的证据、score 等完整上下文。

### design.md / tasks.md 缺陷检测（v2.7+）

在执行代码修复前，先评估 source review 报告中每个 R-* 项的根因：

- 若根因位于 **设计层**（design.md / tasks.md 文档错误—如 proto 字段编号
  与实际占用冲突、API 契约与技术约束矛盾、tasks.md 引用与 design.md 不匹配等）：
  - **不要尝试修改代码**（代码实现是正确的）
  - **不要修改 design.md**（fix-review 无权修改）
  - 返回 JSON 时标注：
    - `status: failed`
    - `design_md_fault: true`
    - `design_md_fault_location: "<file>:<line>"`（精确指向错误位置）
    - `summary` 描述根因
  - reducer 会立即终止 workflow，由人工重跑 `/2-pg-propose` 修正 design.md

- 若根因在代码层 → 正常走以下修复流程

### 修复范围

- fix_cycle: 1
- 只修复 source review 报告中列出的 R-* 项
- 不要引入超出修复范围的新功能
- 不要修改 design.md（设计层 bug 需 escalate，见上节）

### 修复后必跑流程

1. 修改源码
2. 跑 npx vitest run 单元测试（必须通过）
3. 跑模块 lint（必须 0 警告）
4. 只跑 review 报告中标记为 FAIL 的检查项（不要跑全部）
5. 重新计算 review_score（仅基于 FAIL 已修复的检查项）
6. 用 cat > 写盘到 2-build/028-dev.ui-fix-1.md

---

### 执行要求

**任务清单**：


**验证要求**：
| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-ui-1 | 无 @enterprise 引用残留 | ui/ 目录就绪 | `grep -r '@enterprise' ui/ \| wc -l` | 命中数为 0（除 rbac.ts 中的 RbacResource 字面定义） |
| V-ui-2 | UI build 通过 | node_modules 已安装 | `cd ui && npm run build` | 退出码 0，产出 out/ |
| V-ui-3 | UI lint 与 typecheck 通过 | node_modules 已安装 | `cd ui && npm run lint && npm run typecheck` | 退出码 0，无错误 |
| V-ui-4 | enterprise 路由不存在 | ui/ 目录就绪 | `ls ui/app/workspace/{rbac,scim,audit-logs,alerting,cluster,guardrails,circuit-breaker,mcp-tool-groups,mcp-auth-config,access-profiles,business-units,user-rankings}` | 所有目录不存在 |
| V-ui-5 | 侧边栏无 enterprise 菜单项 | ui/ 目录就绪 | `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` | 命中数为 0 |

---

[ROLLBACK CONTEXT]
- failed_at: 2026-08-13T12:09:51+00:00
- reason: review_score: 65.2 (<85), FAIL R-1 design_alignment(22.11 路由规则未完成), R-5 test_contract(21.4 断言错误)
- source: review

你必须优先审查该根因是否已修复，再执行本阶段的正常任务。

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：fix-review）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | 选填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`fix-review` | （未知 phase，使用前请补 _CONTRACT_PHASE_POLICIES） |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase fix-review \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/028-dev.ui-fix-review-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/028-dev.ui-fix-review-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/028-dev.ui-fix-review-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation