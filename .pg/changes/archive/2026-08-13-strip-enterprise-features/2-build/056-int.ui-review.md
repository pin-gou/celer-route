# Review Report: int.ui

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |

## Profile

- Profile 名: vue3 (effective, inherited from `default` + `vue3:component_props`)
- 检查项: 6 (design_alignment / scope_creep / pattern_consistency / file_location / test_contract / component_props)
- 总权重: 30 + 25 + 20 + 15 + 10 + 15 = 115
- 评分计算: (30+25+20+15+10+15) / 115 * 100 = 100
- P0 硬约束: 无（profile.yaml 未标记 `p0: true` 项）

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | V-ui-1..5 全部满足：grep `@enterprise` ui/ 零命中（rbac.ts 字面定义除外）；`npm run build` exit 0；`npm run lint` 0 errors；12 个 enterprise workspace 目录全部删除；`sidebar.tsx` `IS_ENTERPRISE` 零引用 |
| R-2 | scope_creep | 25 | PASS | 全部 231 个 ui/ 变更均在 `module_details[ui].root=ui` 范围内；`routingRuleSheet.tsx`、`rfRuleNode.tsx`、`routingRuleInfoSheet.tsx`、`routingRules.ts` 的 scope 收窄严格匹配 design.md 第 288-293 行约定；`virtualKeySheet.tsx`/`virtualKeysTable.tsx` 移除 `CustomerSelector`/`TeamSelector` 与 tasks.md 47.10 一致；2 个搬出文件（`loginView`、`apiKeysView`）符合 design.md 245-246 路径 |
| R-3 | pattern_consistency | 20 | PASS | `ui/lib/rbac.ts` 与 design.md 第 257-276 行模板完全对齐（`RbacResource` 枚举、`RbacOperation` 枚举、`useRbac()` 返回 true）；额外提供 `RbacProvider`/`useRbacContext` 满足 ~90 个调用方既有调用；`trialsExpiryBanner.tsx` 清理后变 no-op（`TRIAL_EXPIRY` 引用全清）|
| R-4 | file_location | 15 | PASS | 全部 D/M/R/A 文件均位于 `ui/` 根下；唯一被删除的目录 `ui/app/_fallbacks/enterprise/`（57 文件）已 `test.existsSync === false` 验证；唯一被搬出的 `loginView`/`apiKeysView` 目标路径与 design.md 245-246 完全一致；无跨模块修改 |
| R-5 | test_contract | 10 | PASS | `ui/lib/enterprise-stripping.test.ts`（4 个 test / 4 个 describe）覆盖 design.md V-ui-1/V-ui-4/V-ui-5；本机运行 `npx vitest run lib/enterprise-stripping.test.ts` → 4 passed (4)；断言均为强断言（`toEqual([])` / `existsSync === false` / `not.toContain`），无 `toBeTruthy` 弱断言；显式排除 `rbac.ts` 与测试自身的合法字面量 |
| R-6 | component_props | 15 | PASS | N/A — 项目为 React（非 Vue 3），`component_props` 规则文档中关于 `defineProps<T>()` / `<script setup>` / `v-model` / `defineEmits` 的检查项在本仓库没有适用表面；视为空集通过。注：dispatch 注入的 vue3 profile 与项目栈不匹配，但 profile 自动派发是基于 `language: typescript`，符合 `default→vue3` 合并路径；通过 `task` 替代（`useRbac` 90+ 调用方 + 组件 props 类型均与 React 既有模式一致）|

## FAIL 项详细

无。

## 通过项（简要）

- R-1 design_alignment: 5/5 design 承诺（V-ui-1..5）均已实现且本机验证通过
- R-2 scope_creep: 全部 231 个 ui/ 变更均在 track root 内；routing-rules/virtual-keys/sidebar/registries/store 的窄化与 design 章节一一对应
- R-3 pattern_consistency: `rbac.ts` 严格符合 design 模板；`useRbac` 签名保持 `(_resource?, _operation?): boolean` 兼容既有 90+ 调用方
- R-4 file_location: 全部 D/M/R/A 均在 `ui/` 下；被删目录与搬出路径与 design 一致
- R-5 test_contract: 4/4 vitest 测试通过；断言强度符合规约
- R-6 component_props: Vue 3 规则在本 React 仓库无适用表面（profile 自动派发决策）

## 备注

- 本 review 仅覆盖 `int.ui` track（int 环境 = 已有 master baseline 上的集成阶段），对应 tasks.md ## 47 int.ui:dev 与 ## 48 int.ui:review 段。`int.ui:test` 已在 ## 46 阶段完成（见 `054-int.ui-test-result.json`），其产物 `ui/lib/enterprise-stripping.test.ts` 已被本 review 纳入 test_contract 评估。
- vue3 profile 中的 `component_props` 检查项是为 Vue 3 单文件组件设计的；本仓库为 React + Vite，技术栈不匹配。建议后续在 `.pg/code-review/code-review.yaml` 增加 `react` profile 或将自动派发从 `language: typescript` 改为多栈（react/vue）双 profile，但本次不影响 review 结论。
- design.md 第 22 行 `RoutingRule.scope: "global" | "team" | "customer" | "virtual_key" | "user"` 数据契约未做收窄（仅 enum/options 收窄），这是有意为之——OSS 仍可读取历史规则并展示其 metadata，scope 字段为 API 响应解析所需；与 design.md 第 288-293 行"UI 选项收窄"契约不矛盾。
- `RbacResource` 枚举包含 design 模板未列出的项（`MCPLogs` / `Customer` / `Team` / `GuardrailsProviders` 等），但这些是 OSS 现有路由资源的实际使用面（被 ~90 个 layout/tsx 引用），未超出 design 边界。
