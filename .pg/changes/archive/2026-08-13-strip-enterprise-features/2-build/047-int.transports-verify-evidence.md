# int.transports verify evidence
# Change: strip-enterprise-features
# Track: int.transports
# Date: 2026-08-13

## B3 Step 1-2: Health probe (services already running)

birost-api health:
$ curl -s -o /dev/null -w "%{http_code}" http://localhost:9080/health
200

ui-dev health:
$ curl -s -o /dev/null -w "%{http_code}" http://localhost:3008/
200

Ports:
$ netstat -tlnp | grep -E '9080|3008'
tcp 0 0 127.0.0.1:9080  LISTEN 970811/bifrost-http
tcp 0 0 127.0.0.1:3008  LISTEN 971147/node

## B3 Step 3: Real e2e — enterprise endpoints → 404

for endpoint in /api/rbac /api/scim /api/audit-logs /api/alerting /api/cluster /api/guardrails /api/circuit-breaker /api/mcp-tool-groups /api/mcp-auth-config /api/access-profiles /api/business-units /api/user-rankings /api/adaptive-routing /api/edge-control; do
  code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:9080$endpoint")
  echo "$endpoint → $code"
done

Output:
/api/rbac → 404
/api/scim → 404
/api/audit-logs → 404
/api/alerting → 404
/api/cluster → 404
/api/guardrails → 404
/api/circuit-breaker → 404
/api/mcp-tool-groups → 404
/api/mcp-auth-config → 404
/api/access-profiles → 404
/api/business-units → 404
/api/user-rankings → 404
/api/adaptive-routing → 404
/api/edge-control → 404

OSS endpoints still work:
GET /api/providers → 200
GET /v1/models → 200

## 39.1 Lint (go vet)

$ cd transports && go vet ./...
exit 0, no output

## 39.2 Tests (go test -short)

### handlers
$ cd transports/bifrost-http && go test ./handlers/... -short -count=1
ok  github.com/maximhq/bifrost/transports/bifrost-http/handlers  24.712s

### integrations
ok  github.com/maximhq/bifrost/transports/bifrost-http/integrations  0.279s

### server
$ go test ./server/... -short -count=1 -timeout 120s
ok  github.com/maximhq/bifrost/transports/bifrost-http/server  0.121s

### websocket
$ go test ./websocket/... -short -count=1 -timeout 120s
ok  github.com/maximhq/bifrost/transports/bifrost-http/websocket  1.544s

### tests/realtime
$ go test ./tests/... -short -count=1 -timeout 90s
ok  github.com/maximhq/bifrost/transports/bifrost-http/tests/realtime  0.004s

### lib — TestConfigSchemaSync (V-transports-1)
$ go test -run TestConfigSchemaSync ./lib/ -v -count=1 -timeout 60s
=== RUN   TestConfigSchemaSync
    config_test.go:18249: Schema sync errors found (5 total):
        [mcp.client_configs] Field 'oauth_authorize_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_registration_url' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_resource' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_scopes' in schemas.MCPClientConfig but missing from schema
        [mcp.client_configs] Field 'oauth_token_url' in schemas.MCPClientConfig but missing from schema
--- FAIL: TestConfigSchemaSync (0.00s)
=== RUN   TestConfigSchemaSyncTopLevel
    config_test.go:18303: Top-level sync validated: 20 properties match
--- PASS: TestConfigSchemaSyncTopLevel (0.00s)
FAIL
FAIL  github.com/maximhq/bifrost/transports/bifrost-http/lib  0.047s

Analysis: Pre-existing at fork base (db1ef287). The 5 oauth_* fields exist in
Go struct core/schemas/mcp.go but not in config.schema.json — identical at fork
base. strip-enterprise-features changes do not touch any oauth code. The
TopLevel sync (enterprise field deletions) PASSES. Enterprise-only exclusion
adjustments (is_access_profile_managed, excluded schema fields) are all correct.

### lib — suite timeout (network latency to getbifrost.ai/datasheet)
$ go test ./lib/... -short -count=1 -timeout 180s
FAIL  github.com/maximhq/bifrost/transports/bifrost-http/lib  180.071s
Note: TestSQLite_VirtualKey_WithProviderConfigs passes individually in 8.3s
(go test -run TestSQLite_VirtualKey_WithProviderConfigs ./lib/ -timeout 300s → PASS 8.378s).
The suite timeout is due to modelcatalog/datasheet fetch from getbifrost.ai
(HTTP/2, ~1.6MB, 4.8s/pull, with retries) — intermittent network latency, no
code regression.

### schema_test — TestSchemaMCPClientConfigFields (pre-existing oauth drift)
$ go test -run TestSchemaMCPClientConfigFields -v -count=1 -timeout 60s
=== FAIL: TestSchemaMCPClientConfigFields/mcp_client_config_has_oauth_config_id (0.00s)
    config_schema_test.go:685: $defs/mcp_client_config is missing 'oauth_config_id'
                                 property — MCPClientConfig Go struct defines this field

Analysis: Same root cause as TestConfigSchemaSync. oauth_config_id exists in
Go struct MCPClientConfig (core/schemas/mcp.go) but not in config.schema.json.
Identical at fork base (db1ef287 had 0 hits for oauth_config_id in schema).
strip-enterprise-features did not touch schema OAuth fields.

## 39.3 Services: Already running (ports 9080, 3008), health probes 200

## 39.4 V-transports verification

### V-transports-1: config.schema.json 同步校验通过
Result: ⚠️ DEGRADED (pre-existing oauth schema drift, unchanged from fork base)
- TestConfigSchemaSync: FAIL (5 pre-existing oauth fields)
- TestConfigSchemaSyncTopLevel: PASS (enterprise field deletions correct)
- Enterprise exclusion adjustments in config_test.go confirmed correct

### V-transports-2: enterprise HTTP 端点返回 404
Result: ✅ PASS — all 14 enterprise endpoints return HTTP 404

## B3 Step 4: Spring Bean / runtime boot check
N/A — Go project, deletion-only refactor. No new constructors, @Service,
@Component, or test doubles introduced.

## define-summary reconciliation
- verifiable: V-transports-1 → DEGRADED (pre-existing oauth drift, not caused by this change)
- verifiable: V-transports-2 → PASS
- degraded: V-transports-2 → PASS (verified 404)