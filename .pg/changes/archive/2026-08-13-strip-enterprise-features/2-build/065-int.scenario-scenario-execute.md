# int.scenario: scenario-execute 报告

## 元信息
- **变更**: strip-enterprise-features
- **track**: int.scenario
- **phase**: scenario-execute
- **report_seq**: 065
- **时间**: 2026-08-14T02:00+08:00
- **本轮性质**: fix_cycle=3 (design_drift) 修复后重跑

## SSOT: scenario-scenario.yaml 校验
- 文件存在: ✅ (`/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/scenario-scenario.yaml`)
- Scenario 总数: 7
- scenario_id 全局唯一: ✅
- 每段含 6 段（scenario_id / critical / given / when / then / evidence; and 可选）: ✅
- critical 字段为 bool: ✅
- when.type 全部为 api（无 browser 场景）: ✅
- 环境占位符全部可在 env-description.yaml 解析:
  - `{env.business_systems[name=bifrost-api].endpoints[name=health].url}` → http://localhost:9080/health ✅
  - `{env.business_systems[name=bifrost-api].endpoints[name=api].url}` → http://localhost:9080 ✅
  - `{env.data_resources[name=config-db].state.status}` → seeded ✅

## 环境状态
- 健康检查 http://localhost:9080/health → 200 ✅
- config-db seeded（.pg/hooks/local/data/config.db 存在）✅
- 本次执行到 S-bifrost-api-version-after-strip 时断言正则已由 c7565b6 修正为 `"[vV]?[0-9]+\.[0-9]+\.[0-9]+"`，实测 body `"v1.0.0"` 匹配 ✅

## 执行顺序（按 critical + scenario_id 字典序）

1. **S-bifrost-api-version-after-strip** (critical=true, 字典序 v < c? no——按字典序 a<v, 实际顺序见下)
2. ...

> 实际排序：critical=true 组内按 scenario_id 字典序 = config-still-works, rbac-route-404, version-after-strip（'c'<'r'<'v'）

1. **S-bifrost-api-config-still-works** (critical=true) → **PASS**
2. **S-bifrost-api-rbac-route-404** (critical=true) → **PASS**
3. **S-bifrost-api-version-after-strip** (critical=true) → **PASS**
4. S-bifrost-api-no-enterprise-endpoints-registered (critical=false) → PASS
5. S-bifrost-api-prometheus-metrics (critical=false) → PASS
6. S-bifrost-api-routing-rules-list (critical=false) → PASS
7. S-bifrost-api-virtual-keys-list (critical=false) → PASS

**结果: 7/7 PASS, 0 FAIL, 0 SKIPPED**

---

## Scenario 详细结果

### Scenario 1: S-bifrost-api-version-after-strip (critical=true) — PASS

**描述**: 验证 enterprise 剥离后 bifrost-http 仍能响应 GET /api/version 端点（健康检查）

**Given**: 健康检查通过 (200)；enterprise 钩子已从源代码删除

**When**: GET http://localhost:9080/api/version → 200 OK，body = `"v1.0.0"`

**Then 断言**:
| 断言 | 实际 | 结果 |
|------|------|------|
| status_code == 200 | 200 | ✅ PASS |
| response.body matches `"[vV]?[0-9]+\.[0-9]+\.[0-9]+"` | `"v1.0.0"` | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-version-after-strip-evidence.json`

---

### Scenario 2: S-bifrost-api-config-still-works (critical=true) — PASS

**描述**: 验证 /api/config 端点返回 200 且响应结构完整（OSS 路径未受影响）

**When**: GET http://localhost:9080/api/config → 200 OK（1614 字节）

**Then 断言**:
| 断言 | 实际 | 结果 |
|------|------|------|
| status_code == 200 | 200 | ✅ PASS |
| response.body matches `"client_config"` | body 含 `"client_config"` key | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-config-still-works-evidence.json`

---

### Scenario 3: S-bifrost-api-rbac-route-404 (critical=true) — PASS

**描述**: 验证 enterprise-only /api/rbac 端点返回 404（OSS handler 从未注册）

**When + Then**:
| 端点 | 实际状态 | 期望 | 结果 |
|------|---------|------|------|
| GET /api/rbac | 404 | 404 | ✅ PASS |
| GET /api/scim | 404 | 404 | ✅ PASS |
| GET /api/audit-logs | 404 | 404 | ✅ PASS |
| GET /api/access-profiles | 404 | 404 | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-rbac-route-404-evidence.json`

---

### Scenario 4: S-bifrost-api-no-enterprise-endpoints-registered (critical=false) — PASS

**描述**: 验证 enterprise-only 端点（users/alerting）剥离、OSS 保留端点（teams/customers）行为符合 OSS 契约

**When + Then**:
| 端点 | 实际状态 | 期望 | 结果 |
|------|---------|------|------|
| GET /api/governance/users (enterprise-only) | 404 | 404 | ✅ PASS |
| GET /api/alerting/rules (enterprise-only) | 404 | 404 | ✅ PASS |
| GET /api/governance/teams (OSS 保留) | 200 | 200 | ✅ PASS |
| GET /api/governance/customers (OSS 保留) | 200 | 200 | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-no-enterprise-endpoints-registered-evidence.json`

---

### Scenario 5: S-bifrost-api-prometheus-metrics (critical=false) — PASS

**描述**: 验证 /metrics 端点仍可用（OSS Prometheus 集成未受影响）

**When**: GET http://localhost:9080/metrics → 200 OK（Prometheus 文本格式，`# HELP go_gc_duration_seconds ...`）

**Then 断言**:
| 断言 | 实际 | 结果 |
|------|------|------|
| status_code == 200 | 200 | ✅ PASS |
| response.body matches `# HELP` | body 以 `# HELP` 开头 | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-prometheus-metrics-evidence.json`

---

### Scenario 6: S-bifrost-api-routing-rules-list (critical=false) — PASS

**描述**: 验证 OSS Routing Rules 路由能正常返回列表（scope 收窄为 virtual_key/global）

**When**: GET http://localhost:9080/api/governance/routing-rules → 200 OK（5 条规则）

**Then 断言**:
| 断言 | 实际 | 结果 |
|------|------|------|
| status_code == 200 | 200 | ✅ PASS |
| response.body 不含 business_unit_id | 未出现 `business_unit_id`（scope 为 global） | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-routing-rules-list-evidence.json`

---

### Scenario 7: S-bifrost-api-virtual-keys-list (critical=false) — PASS

**描述**: 验证 OSS Virtual Keys 路由能正常返回列表（customer/team 字段已移除但 VK 基础功能保留）

**When**: GET http://localhost:9080/api/governance/virtual-keys → 200 OK

**Then 断言**:
| 断言 | 实际 | 结果 |
|------|------|------|
| status_code == 200 | 200 | ✅ PASS |
| response.body 不含 access_profile_id | 未出现 `access_profile_id` | ✅ PASS |

**证据**: `2-build/065-S-bifrost-api-virtual-keys-list-evidence.json`

---

## 结构化 JSON 证据（per Scenario block）

### S-bifrost-api-version-after-strip (critical=true, PASS)
```json
{
  "scenario_id": "S-bifrost-api-version-after-strip",
  "critical": true,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "Health check 200; enterprise hooks removed from source"},
    {"phase": "when", "result": "ok", "step_type": "api", "request_id": "", "response_status": 200, "url": "http://localhost:9080/api/version"},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "status_code == 200", "actual": 200, "passed": true},
      {"name": "response.body matches '\"[vV]?[0-9]+\\.[0-9]+\\.[0-9]+\"'", "actual": "\"v1.0.0\"", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:06+08:00", "end": "2026-08-14T02:00:07+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-config-still-works (critical=true, PASS)
```json
{
  "scenario_id": "S-bifrost-api-config-still-works",
  "critical": true,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "Health check http://localhost:9080/health → 200; enterprise hooks removed from source"},
    {"phase": "when", "result": "ok", "step_type": "api", "request_id": "", "response_status": 200, "url": "http://localhost:9080/api/config"},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "status_code == 200", "actual": 200, "passed": true},
      {"name": "response.body matches 'client_config'", "actual": "body contains 'client_config' key (1614 bytes)", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:00+08:00", "end": "2026-08-14T02:00:01+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-rbac-route-404 (critical=true, PASS)
```json
{
  "scenario_id": "S-bifrost-api-rbac-route-404",
  "critical": true,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "Health check passed; enterprise-only routes (rbac/scim/audit-logs/access-profiles) deleted from handlers"},
    {"phase": "when", "result": "ok", "step_type": "api", "sub_steps": [
      {"name": "GET /api/rbac", "response_status": 404},
      {"name": "GET /api/scim", "response_status": 404},
      {"name": "GET /api/audit-logs", "response_status": 404},
      {"name": "GET /api/access-profiles", "response_status": 404}
    ]},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "/api/rbac → 404", "actual": 404, "passed": true},
      {"name": "/api/scim → 404", "actual": 404, "passed": true},
      {"name": "/api/audit-logs → 404", "actual": 404, "passed": true},
      {"name": "/api/access-profiles → 404", "actual": 404, "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:02+08:00", "end": "2026-08-14T02:00:06+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-no-enterprise-endpoints-registered (critical=false, PASS)
```json
{
  "scenario_id": "S-bifrost-api-no-enterprise-endpoints-registered",
  "critical": false,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "Health check passed"},
    {"phase": "when", "result": "ok", "step_type": "api", "sub_steps": [
      {"name": "GET /api/governance/users (enterprise-only)", "response_status": 404},
      {"name": "GET /api/alerting/rules (enterprise-only)", "response_status": 404},
      {"name": "GET /api/governance/teams (OSS retained)", "response_status": 200},
      {"name": "GET /api/governance/customers (OSS retained)", "response_status": 200}
    ]},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "enterprise-only endpoints (users/alerting) → 404", "actual": "users=404, alerting=404", "passed": true},
      {"name": "OSS retained endpoints (teams/customers) → 200", "actual": "teams=200, customers=200", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:08+08:00", "end": "2026-08-14T02:00:14+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-prometheus-metrics (critical=false, PASS)
```json
{
  "scenario_id": "S-bifrost-api-prometheus-metrics",
  "critical": false,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "Health check passed"},
    {"phase": "when", "result": "ok", "step_type": "api", "request_id": "", "response_status": 200, "url": "http://localhost:9080/metrics"},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "status_code == 200", "actual": 200, "passed": true},
      {"name": "response.body matches '# HELP'", "actual": "body starts with '# HELP go_gc_duration_seconds...'", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:14+08:00", "end": "2026-08-14T02:00:15+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-routing-rules-list (critical=false, PASS)
```json
{
  "scenario_id": "S-bifrost-api-routing-rules-list",
  "critical": false,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "config-db status: seeded"},
    {"phase": "when", "result": "ok", "step_type": "api", "request_id": "", "response_status": 200, "url": "http://localhost:9080/api/governance/routing-rules"},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "status_code == 200", "actual": 200, "passed": true},
      {"name": "response.body 不含 business_unit_id", "actual": "business_unit_id not found in body", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:15+08:00", "end": "2026-08-14T02:00:16+08:00"},
  "cleanup_result": "n/a"
}
```

### S-bifrost-api-virtual-keys-list (critical=false, PASS)
```json
{
  "scenario_id": "S-bifrost-api-virtual-keys-list",
  "critical": false,
  "status": "pass",
  "type": "api",
  "steps": [
    {"phase": "given", "result": "ok", "detail": "config-db status: seeded"},
    {"phase": "when", "result": "ok", "step_type": "api", "request_id": "", "response_status": 200, "url": "http://localhost:9080/api/governance/virtual-keys"},
    {"phase": "then", "result": "ok", "assertions": [
      {"name": "status_code == 200", "actual": 200, "passed": true},
      {"name": "response.body 不含 access_profile_id", "actual": "access_profile_id not found in body", "passed": true}
    ]}
  ],
  "request_ids": [""],
  "timestamps": {"start": "2026-08-14T02:00:16+08:00", "end": "2026-08-14T02:00:17+08:00"},
  "cleanup_result": "n/a"
}
```

---

## Cleanup 报告

本 track 无 `and`（cleanup）段——所有 Scenario 均为只读 GET 请求（version/config/rbac/metrics/governance 列表），未写入任何数据，无需清理。
- cleanup_result: n/a（全部 7 个 Scenario）
- 无 cleanup 失败

## Critical=true 失败列表

无。3/3 critical=true Scenario 全部 PASS，未触发 escalate。

## 最终判定

**complete**

- 7/7 Scenario PASS（3 critical=true + 4 critical=false）
- 0 FAIL / 0 SKIPPED
- 本轮为 fix_cycle=3 (design_drift) 修复后重跑，此前 design_drift 的两个断言已在 c7565b6 修正（version 正则、teams/customers 200、routing-rules 路径），本轮全部验证通过。
