## 任务：int.core

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: int.core
- track.modules: ['core']
- track.max_fix_retries: 5

### Module 配置
  - module: core
    - root: core
    - language: go
    - build: go build ./...
    - lint: go vet ./...
    - test.unit: go test ./... -short -count=1
    - test.integration: make test-core

### Stage 配置
- stage.name: int
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: go test ./... -short -count=1


---

### 测试要求

TDD 红 Phase：本阶段只写测试代码，绝不创建或修改任何生产代码。
预期首次编译失败（红 phase 属性）。

- 只创建测试文件
- 不修改任何生产代码文件
- 可以修改测试配置文件（如 pom.xml、package.json 的测试依赖）

### 模块路径约束

禁止编辑以下路径的文件：
- 不在 ['core'] 路径下的文件

---

### 执行要求

**任务清单**：
## 26. int.core:test - int 测试先行

<!-- on_conditions_eval:
     stage=int (常驻, 无 on_conditions)
     track=core (常驻, 无 on_conditions)
-->

- [ ] 26.1 在 `core/schemas/` 与 `core/bifrost.go` 编写 Go 单元测试，确认 enterprise-only BifrostContextKey 已从源代码中删除，且未在 `core/schemas/context.go`、`core/inference.go`、`core/bifrost.go` 中被引用

**验证要求**：

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：test）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（覆盖的 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`test` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase test \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/034-int.core-test-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/034-int.core-test-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/034-int.core-test-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation