# Gate Assessment: int.framework

**Track**: int.framework
**Change**: strip-enterprise-features
**Stage**: int (integration)
**Date**: 2026-08-13
**Cycle**: 1

## 整体判定

PASS — gate-score: 100, 无 P0 失败

## 检查项

| # | 检查项 | 判定 | 证据来源 | 维度分 |
|---|--------|------|---------|--------|
| 1 | V-* 验证项通过率 | ✅ | design.md V-framework-1: go build PASS; lint PASS; tests 19/21 PASS (2 pre-existing); B3 all 4 steps PASS | 100/100 |
| 2 | design.md 一致性 | ✅ | 框架变更: EnterpriseOnly 字段删除 ✓, ErrFlagEnterpriseOnly 删除 ✓, SyncDelegate 接口删除 ✓, registry.go enterprise 分支删除 ✓, Config.IsEnterprise 删除 ✓, vault_callbacks 清空 ✓ | 100/100 |
| 3 | scope creep 检查 | ✅ | 所有 framework/ 变更文件属 enterprise 清理范围: vault_callbacks (vault hook 删除), tables/key/virtualkey (vault store 调用删除), oauth2 (BifrostContextKeyUserID 引用删除), modelcatalog/datasheet (同), featureflags (SyncDelegate/EnterpriseOnly) | 100/100 |
| 4 | 测试质量 | ✅ | enterprisestrip_test.go 5 个测试函数, 使用 regex 断言符号删除; featureflags_test.go 更新 strip 后行为测试; 非条件断言无 Warning | 100/100 |
| 5 | 实现完整性 (v3.x P0) | ✅ | git diff master 无 TODO/FIXME/stub/not implemented 或 panic("not implemented") 新增; V-framework-1 (go build) 已通过 | 100/100 |
| 6 | runtime_boot (B3 P0) | ✅ | verify 报告含 B3 4 步证据: (1) restart_all_instances ✓ (2) health probe 30×3s ✓ (3) 14 enterprise 端点 404 ✓ (4) 运行时 boot 检查 (N/A) ✓ | 100/100 |
| 7 | 证据可追溯 (抽查) | ✅ | 2/2 项可追溯: (1) V-framework-1 build evidence 含 exit 0 原始输出 (2) enterprise-only 删除在 diff 中可追溯 | 100/100 |
| 8 | 回归分析校验 | ✅ | 2 个预存失败(logstore TestExtractPayload_RoundTrip, vectorstore Redis tests) 均在未修改包中, 新引入 FAIL = 0 | 100/100 |

## 加权评分明细

| 维度 | 权重 | 维度分 | 加权得分 |
|------|------|--------|---------|
| V-* 验证项通过率 | 50% | 100 | 50.0 |
| design.md 一致性 | 20% | 100 | 20.0 |
| scope creep 检查 | 15% | 100 | 15.0 |
| 测试质量 | 10% | 100 | 10.0 |
| 实现完整性 (v3.x) | 10% | 100 | 10.0 |
| **gate-score** | **100%** | | **100.0** |

## 实现完整性核查段 (v3.x P0)

### 硬标记扫描

```
$ git diff master | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException' | grep -v '\[skip-review\]'
```

**结果**: 仅 pipeline.events/snapshot.json 中的 grep 命令文本和历史 diff 中的 `-// stubVaultHooks installs store/remove hooks that mimic the enterprise vault` 删除行命中。

**豁免列表**:
- `-// stubVaultHooks ...` 删除行 (core/configstore/vault_callbacks.go) — 属变更目标本身: 删除 enterprise vault hooks, 非新增 stub

**判定**: ✅ PASS — 0 个新增 TODO/FIXME/stub

### tasks-V 映射核查

int.framework 仅涉及 V-framework-1 (Go module build 通过)。已在 verify 阶段验证且证据完整。

## 不通过项详细说明

无 — 所有检查项通过。

## Evidence 抽查详情

### 抽查 1: V-framework-1 Go build
- **证据位置**: `042-int.framework-verify-evidence.md:54-60`
- **原始输出**: `$ cd framework && go build ./...` EXIT_CODE: 0
- **追溯**: framework/featureflags/ 差异文件均已通过 `go build ./...` 编译验证

### 抽查 2: EnterpriseOnly 字段删除
- **证据位置**: design.md framework/featureflags 段
- **追溯**: git diff master 确认 `EnterpriseOnly bool` 字段从 FlagDef 结构体删除, `ErrFlagEnterpriseOnly` 错误常量删除, `SyncDelegate` 接口删除
- **代码位置**: `framework/featureflags/featureflags.go` 和 `registry.go`

## 回归分析校验

| 预存失败 | 包 | 确认未修改 |
|----------|-----|----------|
| TestExtractPayload_RoundTrip (payloadFields 36→42) | framework/logstore/ | ✅ `git diff master -- framework/logstore/` 为空 |
| 5 Redis 测试 (redis not running) | framework/vectorstore/ | ✅ `git diff master -- framework/vectorstore/` 为空 |

**新引入 FAIL**: 0 个
