# 063-int.scenario-scenario-execute — 场景执行报告

## 摘要

| 项 | 值 |
|----|-----|
| Track | int.scenario |
| Phase | scenario-execute |
| 时间 | 2026-08-13T22:09:15+00:00 → 2026-08-13T22:11:13+00:00 |
| 总 Scenario | 7 |
| ✅ PASS | 5 |
| ❌ FAIL | 2 |
| ⏭️ SKIPPED | 0 |
| 最终判定 | escalate |

## 执行顺序

### critical=true（3 个）

#### 1. ✅ S-bifrost-api-config-still-works
- **描述**: 验证 enterprise 剥离后 /api/config 端点仍正常
- **when**: GET /api/config → 200 ✅
- **then**: status_code == 200 ✅, response.body 含 "client_config" ✅
- **证据**: `2-build/063-bifrost-api-config-still-works-evidence.json`

#### 2. ✅ S-bifrost-api-rbac-route-404
- **描述**: 验证 enterprise-only 端点返回 404
- **when**: 
  - GET /api/rbac → 404 ✅
  - GET /api/scim → 404 ✅
  - GET /api/audit-logs → 404 ✅
  - GET /api/access-profiles → 404 ✅
- **then**: 全部 4 个端点返回 404 ✅
- **证据**: `2-build/063-bifrost-api-rbac-route-404-evidence.json`

#### 3. ✅ S-bifrost-api-version-after-strip
- **描述**: 验证 /api/version 端点仍正常
- **when**: GET /api/version → 200 ✅
- **then**: status_code == 200 ✅, body="v1.0.0" matches 版本 regex ✅
- **证据**: `2-build/063-bifrost-api-version-after-strip-evidence.json`

### critical=false（4 个）

#### 4. ❌ S-bifrost-api-no-enterprise-endpoints-registered
- **描述**: 验证 enterprise 端点全部返回 404
- **when**: 
  - GET /api/governance/teams → 200 ❌（期望 404）
  - GET /api/governance/customers → 200 ❌（期望 404）
  - GET /api/governance/users → 404 ✅
  - GET /api/alerting/rules → 404 ✅
- **then**: 2/4 端点失败
- **根因**: **非代码缺陷**。proposal 明确保留 Team/Customer 基础 CRUD 为 OSS 功能（"修改 plugins/governance/ 中的非 enterprise 业务逻辑（VK、Team、Customer 基础 CRUD...保留）"）。scenario 文件假设全部企业版端点应返回 404，但 teams/customers API 是 OSS 保留路由，预期返回 200。
- **证据**: `2-build/063-bifrost-api-no-enterprise-endpoints-registered-evidence.json`

#### 5. ✅ S-bifrost-api-prometheus-metrics
- **描述**: 验证 /metrics 端点仍可用
- **when**: GET /metrics → 200 ✅
- **then**: body 含 "# HELP" ✅
- **证据**: `2-build/063-bifrost-api-prometheus-metrics-evidence.json`

#### 6. ❌ S-bifrost-api-routing-rules-list
- **描述**: 验证 Routing Rules 路由正常返回列表
- **when**: GET /api/routing-rules → 404 ❌（期望 200）
- **then**: status_code == 200 FAIL
- **根因**: **非代码缺陷**。scenario 文件 URL 写错：实际路由为 `/api/governance/routing-rules`（含 `/governance` 前缀），而非 `/api/routing-rules`。用正确路径验证：`GET /api/governance/routing-rules` → 200 ✅，body 含 5 条规则且不含 `business_unit_id` ✅。
- **证据**: `2-build/063-bifrost-api-routing-rules-list-evidence.json`

#### 7. ✅ S-bifrost-api-virtual-keys-list
- **描述**: 验证 Virtual Keys 路由正常返回列表
- **when**: GET /api/governance/virtual-keys → 200 ✅
- **then**: body 不含 access_profile_id ✅
- **证据**: `2-build/063-bifrost-api-virtual-keys-list-evidence.json`

## Cleanup 报告

所有 Scenario 均为 GET 只读请求，无 cleanup 步骤（and 段为空），无需清理。

## 失败列表

| Scenario ID | Critical | 失败原因 | 是否为代码缺陷 |
|------------|----------|---------|--------------|
| S-bifrost-api-no-enterprise-endpoints-registered | false | teams/customers 是 OSS 保留功能（proposal 明确保留），返回 200 正确 | ❌ scenario 文件假设有误 |
| S-bifrost-api-routing-rules-list | false | URL 路径应写为 `/api/governance/routing-rules`（缺少 `/governance` 前缀） | ❌ scenario 文件路径错误 |

## 最终判定

**escalate**（critical=false 失败率 2/4 = 50% ≥ 30%）

**重要说明**：两个失败 Scenario 均**非代码回归**，而是 scenario 文件自身假设与 proposal 设计意图不符：
- teams/customers 路由是 OSS 保留功能（proposal line 56 明确保留）
- routing-rules 正确路径为 `/api/governance/routing-rules`（代码行为正确）

建议：修正 scenario-<track>.yaml 中这两个 Scenario 的 then 断言，而非修改生产代码。
