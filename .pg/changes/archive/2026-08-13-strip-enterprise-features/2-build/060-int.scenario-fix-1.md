## scenario-fix Report

### fix_cycle
1

### 失败的 Scenario
S-bifrost-api-config-still-works (critical=true)

### 根因分类
design_drift

### 根因分析

**Scenario 断言**: `response.body matches '"config":'`
**实际响应**: `{"client_config":{...},"framework_config":{...},"auth_config":{...},"proxy_config":{...},...}`
**失败原因**: 响应 body 的 JSON 顶层 key 为 `client_config`（带 `client_` 前缀），而非裸 `config`。子串 `"config":` 不在任何实际 key 中出现（`"client_config":` 中 `config` 前是 `_` 而非 `"`）。

**三线确认**:
1. `git diff origin/master feat/pg/strip-enterprise-features -- transports/bifrost-http/handlers/config.go` → **空**（本变更未修改 config handler）
2. 当前运行实例实测 `/api/config` → `{"client_config":{...},"framework_config":{...},"auth_config":{...},"proxy_config":{...},...}`，无裸 `config` key
3. 设计文档（design.md）未声明 `/api/config` 响应应包含 `config` key

**判定**: 本变更为纯删除型 refactor，未更改 `/api/config` handler；scenario 断言 `"config":` 与实际 API 契约（`client_config`）不一致，属于设计文档（scenario）契约偏移，非代码缺陷。

### 修改的文件
无（design_drift 路径，不改代码）

### 单元测试证据
N/A — 不改代码，未跑测试

### 修复策略
design_drift 路径：不改代码，由编排器终止 workflow_failed。需人工重跑 pg-propose 修正 scenario-scenario.yaml 中 `S-bifrost-api-config-still-works` 的 `then` 断言，将 `"config":` 改为 `"client_config":`（或更宽松的匹配模式如 `"client_config"`）。
