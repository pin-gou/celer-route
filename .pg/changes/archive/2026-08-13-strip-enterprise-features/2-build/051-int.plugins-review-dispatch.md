## 任务：int.plugins

### 变更名称
strip-enterprise-features

### Track 配置
- track.id: int.plugins
- track.modules: ['plugins']
- track.max_fix_retries: 5

### Module 配置
  - module: plugins
    - root: plugins
    - language: go
    - build: for d in */; do (cd "$d" && go build ./...); done
    - lint: for d in */; do (cd "$d" && go vet ./...); done
    - test.unit: for d in */; do (cd "$d" && go test ./... -short -count=1); done
    - test.integration: make test-plugins

### Stage 配置
- stage.name: int
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: for d in */; do (cd "$d" && go test ./... -short -count=1); done


---

### 阶段定位

本阶段是 **review**（静态代码审查），处于 `dev → review → verify` 流程的中间位置。
关注的是代码**静态属性**是否符合 design.md + 编码规约，与 verify 阶段的"运行时行为"互补。

### 必读源文档

- **design.md**: `.pg/changes/strip-enterprise-features/design.md` — 期望实现的 API/DTO/数据结构/契约
- **tasks.md**: `.pg/changes/strip-enterprise-features/tasks.md` — 任务清单与验收标准
- **profile 配置**: `.pg/code-review/code-review.yaml` — 检查项 + 权重 + threshold
- **profile 规则**: `.pg/code-review/<profile>/<check>.md` — 每项检查的执行细则

### ⚠️ 必读：本 track 的具体规则文档（已注入）

下面是从 `.pg/code-review/<effective_profile>/` 加载的每条 enabled check 的完整规则。
**必须**对每条规则**完整执行**（逐项核对"FAIL 判定"段），不能跳过。

#### error_wrapping

## Go 错误处理检查

### FAIL 判定

- **错误信息缺上下文**：`return errors.New("failed")` 不带变量值/位置
  - 正确：`return fmt.Errorf("read config %s: %w", path, err)`
- **errors.Is/As 缺失**：需要判别特定错误时用 `==` 比较或字符串匹配
  - 正确：`if errors.Is(err, sql.ErrNoRows) { ... }`
- **错误吞掉**：catch 后只 log 不返回上层
  - 正确：log 后 `return fmt.Errorf("xxx: %w", err)`
- **sentinel error 散落**：业务错误定义在多处，应集中在 `errors.go`

### 通过条件

- 错误信息含上下文（变量值、操作名、关键参数）
- 使用 `errors.Is` / `errors.As` 而非字符串比较
- 错误逐层 wrapping 而非吞掉
- 业务 sentinel error 集中定义

#### pattern_consistency

## Go 模式一致性补充检查

继承自 `default/pattern_consistency.md`，本文件只列**额外**检查项。

### FAIL 判定

- **错误未 wrapping**：`return err` 直接返回，丢失上下文；应 `return fmt.Errorf("xxx: %w", err)`
- **panic 滥用**：业务逻辑用 `panic()` 而非 `return err`
- **未使用 context.Context**：长操作 / I/O 未传 `context.Context`
- **goroutine 无退出语义**：`go func() { ... }()` 没接 ctx.Done() 信号
- **struct tag 缺失**：json/db 序列化字段缺少 tag
- **interface{} 用法**：用了 `interface{}` 而不是 `any`（Go 1.18+）
- **defer 误用**：在循环里 `defer close(ch)` 会延迟到函数结束而非循环结束
- **errcheck 忽略**：未处理 error（`_ = xxx` 而不写明原因）

### 通过条件

- 错误统一用 `fmt.Errorf("...: %w", err)` wrapping
- 业务逻辑不 panic
- 长操作都传 context
- goroutine 监听 ctx.Done()
- struct tag 完整


### 🔴 P0 硬约束清单

本 track 中标记为 `p0: true` 的 check（即使 score 达标，FAIL 也强制 escalate）：

（无）

**summary 中 p0_failures 必须使用 check 名称**（如 `implementation_completeness`），
reducer 直接匹配 check 名（不依赖 R-N 编号）：

- ❌ `p0_failures: [R-6]` — reducer 无法识别
- ✅ `p0_failures: [implementation_completeness]` — reducer 识别为 P0 硬约束

### 审查范围

- 仅检查 dev agent 在 `git diff feat/pg/strip-enterprise-features` 中的新增/修改文件
- 不重跑测试（那是 verify 阶段的工作）
- 不修改源码

### 审查执行协议（v3.x 强化）

1. 读上列每条 check 的 markdown 规则文档
2. 找到该 check 对应的 R-* ID（顺序：`R-1`=`design_alignment` / `R-2`=`scope_creep` / `R-3`=`pattern_consistency` / `R-4`=`file_location` / `R-5`=`test_contract` / `R-6`=`implementation_completeness` / 等等，按 enabled check 顺序递增）
3. 逐项 grep `git diff feat/pg/strip-enterprise-features` 验证
4. 标记 PASS / FAIL / WARN，把 evidence 写入报告
5. 计算 `review_score`（加权平均，仅含 PASS 的 check）

### 评分协议（必填）

summary 必须包含：
- `review_score: <0-100>` — 加权平均分
- `p0_failures: [check_name, ...]` — P0 失败项列表（**使用 check 名称，不是 R-N 编号**）

### 输出报告

- 报告路径: `.pg/changes/strip-enterprise-features/2-build/051-int.plugins-review.md`
- 报告内含每项检查的 PASS/FAIL 判定与证据
- **必须**对 `implementation_completeness` check 单独列出 TODO/stub/task-V 缺失的证据行

### 触发 fix 循环

- score < pass_threshold → 一律 escalate 走 fix-review 循环（代码不会自愈，review 重试无意义）
- score ≥ pass_threshold → completed → 进入 verify
- **⚠️ P0 implementation_completeness FAIL**：即使 score ≥ pass_threshold，reducer 也会强制走 fix-review 循环（这是 v3.x 的硬约束，绕过 review_score 的兜底）

---

### 执行要求

**任务清单**：
## 43. int.plugins:review - 静态代码审查

<!-- on_conditions_eval:
     stage=int (常驻, 无 on_conditions)
     track=plugins (常驻, 无 on_conditions)
-->

- [ ] 43.1 review agent 读 design.md + tasks.md + .pg/code-review/code-review.yaml 细则
- [ ] 43.2 review agent 对 git diff feat/pg/strip-enterprise-features 做静态审查
- [ ] 43.3 review agent 输出 review_score + p0_failures 到本 section 对应的 review 报告（路径由 dispatch 注入）
- [ ] 43.4 score < pass_threshold → escalate 至 fix-review；score ≥ pass_threshold → completed → 进入 verify

**验证要求**：

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：review）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | 选填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`review` | （未知 phase，使用前请补 _CONTRACT_PHASE_POLICIES） |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase review \
    --output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/051-int.plugins-review-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `/home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/051-int.plugins-review-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path /home/ubuntu/workspace/my-gateway/.pg/changes/strip-enterprise-features/2-build/051-int.plugins-review-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation