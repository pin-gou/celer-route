# dev.transports:verify 验证报告

## 执行摘要
**状态**: FAIL

**关键问题**: V-transports-2 发现 enterprise HTTP 端点未正确删除，所有端点返回 200 而非预期的 404。

---

## 14.1 Lint 检查 ✅

**命令**: `cd transports/bifrost-http && go vet ./...`

**结果**: PASS (exit code 0)

---

## 14.2 测试检查

**命令**: `cd transports/bifrost-http && go test -run TestConfigSchemaSync ./lib/ -v -count=1`

**结果**: FAIL

**详情**: V-transports-1 TestConfigSchemaSync 测试失败，报告 5 个 MCP oauth 字段缺失：
- `oauth_authorize_url`
- `oauth_registration_url`
- `oauth_resource`
- `oauth_scopes`
- `oauth_token_url`

**分析**: 此问题为**预先存在的** schema 同步问题，与本变更 (`strip-enterprise-features`) 无直接关联。

---

## 14.3 服务启动状态

**服务**: bifrost-api on port 9080
- 状态: **UP** (HTTP 200)
- 健康检查: ✅ 通过

---

## 14.4 V-transports-2: Enterprise HTTP 端点验证 ❌ **CRITICAL FAILURE**

**验证方法**: `curl -s -o /dev/null -w "%{http_code}" http://localhost:9080/api/<endpoint>`

**预期结果**: 所有 enterprise 端点返回 404

**实际结果**:

| 端点 | 预期 | 实际 | 状态 |
|------|------|------|------|
| /api/rbac | 404 | 200 | ❌ FAIL |
| /api/scim | 404 | 200 | ❌ FAIL |
| /api/audit-logs | 404 | 200 | ❌ FAIL |
| /api/alerting | 404 | 200 | ❌ FAIL |
| /api/cluster | 404 | 200 | ❌ FAIL |
| /api/guardrails | 404 | 200 | ❌ FAIL |
| /api/circuit-breaker | 404 | 200 | ❌ FAIL |
| /api/access-profiles | 404 | 200 | ❌ FAIL |
| /api/scim/v2/Users | 404 | 200 | ❌ FAIL |

**结论**: Enterprise HTTP 端点**未被删除**，仍可访问。这与 `strip-enterprise-features` 变更目标相悖。

---

## Issue 列表

### Issue-1: Enterprise HTTP 端点未删除

**Verification Step**: V-transports-2

**Expected**: 所有 enterprise 端点（/api/rbac, /api/scim, /api/audit-logs 等）应返回 404

**Actual**: 所有端点返回 200，说明路由仍注册在服务器中

**Affected Tasks**: 
- design.md 中 "transports 路由删除" 要求
- V-transports-2 验证失败

---

## 服务启动日志

```
bifrost-api: no PID file, skip
重新构建 bifrost-http...
Waiting for bifrost-api to be ready on port 9080...
✅ bifrost-api is ready on port 9080
Waiting for ui-dev to be ready on port 3008...
✅ ui-dev is ready on port 3008
pg_http_health_check: probing http://localhost:9080/health
pg_http_health_check: OK — http://localhost:9080/health returned HTTP 200
```

---

## Define-Summary 对账

| 定义类别 | V-* 项 | 验证结果 |
|----------|--------|----------|
| verifiable | V-transports-1 | FAIL (pre-existing) |
| verifiable | V-transports-2 | FAIL (enterprise endpoints still accessible) |
| degraded | V-transports-2 | N/A (直接 FAIL) |
