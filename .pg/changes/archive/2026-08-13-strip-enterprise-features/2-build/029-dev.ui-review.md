# Review Report: dev.ui (cycle-1 re-review)

## Score

| Metric | Value |
|--------|-------|
| review_score | 100.0 |
| pass_threshold | 85 |
| disposition | completed |

> **Rollback context**：本报告是 review-cycle cycle 1。上一轮 review（`027-dev.ui-review.md`）escalate 标识的根因 R-1（design_alignment：routing-rules scope 收窄未完成）与 R-5（test_contract：21.4 断言错误），已由 fix-review cycle 1（`028-dev.ui-fix-1.md`）修复。本报告逐项核验两个 FAIL 是否已修复，并重跑全量 6 项 enabled 检查。

## Profile

- Profile 名: vue3 (track language=typescript → 自动派发 vue3 profile，inherit default)
- Enabled checks (deduped by union):
  - design_alignment (weight=30) [default]
  - scope_creep (weight=25) [default]
  - pattern_consistency (weight=20) [vue3 addendum overrides default]
  - file_location (weight=15) [default]
  - test_contract (weight=10) [default]
  - component_props (weight=15) [vue3-specific]
- Total weight: 30 + 25 + 20 + 15 + 10 + 15 = 115
- Pass threshold: 85 (vue3 profile)
- P0 硬约束: 无（本 track 无 `p0: true` 的 check）

## ROLLBACK 根因核验（上一轮 FAIL 是否已修复）

### R-1 design_alignment — 已修复 ✅
上一轮指出 3 处 routing-rules scope 收窄遗漏，逐一核验当前代码：

| 遗漏点 | 修复后现实 | 证据 |
|--------|-----------|------|
| 遗漏 1: `routingRuleInfoSheet.tsx` 仍引用 `useGetTeamQuery`/`useGetCustomerQuery` | 已删除 team/customer 分支，仅保留 `useGetVirtualKeyQuery`；`useScopeName` 只处理 `virtual_key` | `git diff master..HEAD` 显示 import 行删除 `useGetCustomerQuery, useGetTeamQuery, useGetVirtualKeyQuery` → `useGetVirtualKeyQuery`；useMemo 依赖数组精简为 `[scope, scopeId, vkData]` |
| 遗漏 2: `rfRuleNode.tsx` 用通用 `rule.scope !== "global" && rule.scope_id` | 已收窄为 `rule.scope === "virtual_key" && rule.scope_id` | `git diff` 显示 hover popover scope_id 条件行改为 `rule.scope === "virtual_key"` |
| 遗漏 3: `tree/views/constants.ts` SCOPE_CONFIG/SCOPE_ORDER 仍含 team/customer | 已删除 team/customer 条目；`SCOPE_ORDER = ["virtual_key", "global"]` | `git diff` 显示 SCOPE_CONFIG 移除 team/customer 键，SCOPE_ORDER 由 4 项收窄为 2 项 |
| 附带：`routingRuleSheet.tsx` 退化三元 + user scope | 已简化为常量 "Virtual Key"；`UserPicker`/`@enterprise/lib` import 全部清理 | `git diff` 显示 `toast.error("Virtual Key is required")`，删除 `SelectItem value="user"`、`getUserPicker()`、`@enterprise/lib/registrations/userPicker` |

静态核验（grep 佐证）：
- `grep 'useGetTeamQuery\|useGetCustomerQuery' ui/app/workspace/routing-rules/` → 0 命中
- `grep 'scope.*==.*"team"\|scope.*==.*"customer"' ui/app/workspace/routing-rules/` → 0 命中

### R-5 test_contract — 已修复 ✅
| 问题 | 修复后现实 |
|------|-----------|
| 21.4 断言 `line.includes("false")`（fake 断言，必然失败） | 重写为 `expect(content).not.toContain("IS_ENTERPRISE")`，与 V-ui-5 design 契约对齐 |
| 21.1 测试自指（测试文件自身含 `@enterprise` 字符串被计入） | 增加 `if (file.endsWith("/lib/enterprise-stripping.test.ts")) continue;` 自排除 |

`ui/lib/enterprise-stripping.test.ts` 当前 4 个 describe（21.1–21.4）断言与 design.md V-ui-1/V-ui-4/V-ui-5 契约一致，无逻辑瑕疵。

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|-----|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | 上一轮 3 处 routing-rules 遗漏已全部修复；全量 design 契约（见下）逐项核对通过 |
| R-2 | scope_creep | 25 | PASS | 所有 230 个变更文件均在 `ui/` 根下（`grep -cv '^ui/'` 输出 0，无 backend 越界） |
| R-3 | pattern_consistency | 20 | PASS | (default) React/TSX 接口显式、命名稳定；(vue3 addendum) `<script setup>`/`defineComponent`/`useProTable`/直接 fetch 全部空集（无 `.vue` 文件、API 走 RTK Query） |
| R-4 | file_location | 15 | PASS | 全部变更在 module.ui.root=`ui/` 内；新增 `ui/lib/rbac.ts`、`ui/lib/enterprise-stripping.test.ts` 均在 `ui/lib/` 下 |
| R-5 | test_contract | 10 | PASS | 21.4 断言重写为 `IS_ENTERPRISE` absence（对齐 V-ui-5）、21.1 自排除修复；4/4 断言与 design 契约一致 |
| R-6 | component_props | 15 | PASS | Vue 3 `defineProps<{...}>` 检查：ui/ 下 0 个 `.vue` 文件，Vue-specific 项空集，VACUOUSLY PASS |

### Score 计算

```
PASS weight = design_alignment(30) + scope_creep(25) + pattern_consistency(20)
            + file_location(15) + test_contract(10) + component_props(15) = 115
Total enabled weight = 115
review_score = 115 / 115 × 100 = 100.0
```

上一轮 65.2 → 本轮 100.0（+34.8），6/6 通过，≥ pass_threshold 85。

## 与 design.md 关键契约对照（design_alignment 全量）

| design.md 项 | 实现 | 证据 |
|--------------|------|------|
| 删除 `ui/app/_fallbacks/enterprise/` 整个目录 | ✓ | `git diff --name-status`：117 个 deleted 文件均位于该目录；`ls ui/app/_fallbacks/enterprise/` → No such file |
| `ui/lib/rbac.ts` useRbac()=true + RbacResource/RbacOperation enum | ✓ | `ui/lib/rbac.ts:45` useRbac 返回 `true`；RbacResource 31 项 + RbacOperation 7 项；含 RbacProvider/useRbacContext no-op |
| 删除 customerSelector & teamSelector | ✓ | `grep 'TeamSelector\|CustomerSelector' ui/` → 0 命中；`ls ui/components/entitySelectors/` 无该二件 |
| 删除 sidebar 三项 IS_ENTERPRISE 菜单 | ✓ | `grep -n 'IS_ENTERPRISE' ui/components/sidebar.tsx` → 0 命中；Proxy/Branding/License title 无残留 |
| 删除 `ui/lib/constants/config.ts` IS_ENTERPRISE 常量 | ✓ | `grep -rn 'IS_ENTERPRISE' ui/lib/constants/` → 0 命中 |
| 删除 tsconfig `@enterprise/*` `@schemas/*` paths | ✓ | `git diff` tsconfig.json 仅保留 `"@/*"` |
| 删除 vite.config `@enterprise` alias + `BIFROST_IS_ENTERPRISE` + preserveSymlinks | ✓ | `git diff` vite.config.mts 删除 fs/isEnterpriseBuild/alias 三项/双环境变量/preserveSymlinks |
| 删除 12+ enterprise 路由（rbac/scim/audit-logs/alerting/cluster/guardrails/circuit-breaker/mcp-tool-groups/mcp-auth-config/access-profiles/business-units/user-rankings） | ✓ | `ls ui/app/workspace/<each>` → 全部不存在 |
| `RoutingRuleScope` 收窄为 virtual_key/global | ✓ | `ui/lib/types/routingRules.ts:90-93` enum 仅 Global + VirtualKey；`ROUTING_RULE_SCOPES` 2 项 |
| VirtualKeys sheet 移除 customer/team picker | ✓ | `git diff` virtualKeySheet.tsx 删除 CustomerSelector/TeamSelector import 与相关逻辑，useRbac 改从 `@/lib/rbac` |
| RoutingRules sheet scope 收窄（sheet + info-sheet + tree 三件套） | ✓ | routingRuleSheet/InfoSheet/rfRuleNode/constants 四处全部收窄并清理 |
| api-keys/login 组件搬出 fallback | ✓ | `ui/app/workspace/config/api-keys/views/apiKeysView.tsx`、`ui/app/login/views/loginView.tsx` 存在，import 均为 `@/` |
| ContactUsView 删除 | ✓ | `grep 'ContactUsView' ui/` → 0 命中 |
| 重写 100+ `@enterprise/*` import | ✓ | `grep -rn '@enterprise' ui/` 仅在测试文件自身（已自排除），源文件 0 残留 |
| store/clientLayout/registries 清理 | ✓ | baseApi.ts 保留纯说明性注释；userPicker.tsx/modelLimitScopes.tsx 为 no-op 注册器工具（OSS 无注册） |

## 通过项（简要）

- **R-2 scope_creep**：`git diff --name-only master..HEAD` 中 ui/ 变更文件 230 个全部在 `ui/` 下；无跨模块越界（core/framework/transports 等变更属其它 track）。
- **R-3 pattern_consistency**：(default) 注解/类型风格统一；(vue3 addendum) 项目为 React + RTK Query，`.vue`/Composition API/useProTable 项空集。
- **R-4 file_location**：新增两个文件均位于 `ui/lib/`（rbac.ts、enterprise-stripping.test.ts）。
- **R-6 component_props**：ui/ 下无 `.vue` 文件，Vue-specific 检查空集，VACUOUSLY PASS。

## 备注

- 本阶段为静态 review，不重跑 `npm run build` / `npm run lint` / `npx vitest run`（那属 verify 阶段）。fix-review 报告已记录 vitest 214 用例 0 失败、lint exit 0、build 成功。
- fix-review 报告提到的 `config.ts:71` TS2322 与 `columns.test.ts` vitest 解析失败为基线遗留（两文件不在本变更 diff 内），已由 fix-review agent 记录，非本变更引入。
- P0 硬约束：无。disposition = completed → 进入 verify。

---

## tasks.md 状态
- 23.1 ✅ review agent 读设计/tasks/profile 细则
- 23.2 ✅ review agent 对 git diff 做静态审查
- 23.3 ✅ review agent 输出 review_score(100) + p0_failures([]) 到本报告
- 23.4 ✅ score 100 ≥ pass_threshold 85 → completed → 进入 verify
