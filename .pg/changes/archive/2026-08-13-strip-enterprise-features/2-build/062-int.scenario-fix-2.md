## scenario-fix Report (fix_cycle=2)

### fix_cycle
2

### 失败的 Scenario
S-bifrost-api-version-after-strip (critical=true)

### 根因分类
design_drift

### 根因审查结论（rollback context 要求：优先审查根因是否已修复）

**已修复。** scenario-scenario.yaml 中 S-bifrost-api-version-after-strip 的断言
已在 escalate 提交 265e8d5 中由 `'"version":\s*".+"'` 修正为
`'"[vV]?[0-9]+\.[0-9]+\.[0-9]+"'`（与 061 源报告"建议"一致）。

### 根因分析（三线确认，同 fix-060 类）

1. **代码未变**：`git diff origin/master feat/pg/strip-enterprise-features -- transports/bifrost-http/handlers/config.go` 为空，本变更未触碰 getVersion handler。
2. **API 契约**：handler 实为 `SendJSON(ctx, version)`，`version` 是 string（init.go `version = v`），JSON 编码后即裸字符串 `"v1.0.0"`（Content-Length 8/9）。design.md 未声明 /api/version 返回对象形态。
3. **实测验证**：`curl http://localhost:9080/api/version` → HTTP 200，body = `"v1.0.0"`；修正后断言正则 `"[vV]?[0-9]+\.[0-9]+\.[0-9]+"` 与 body 匹配 = True。

Endpoint 本身工作正常（200 + 有效版本串），旧断言期望对象形态 `{"version": ...}`，
与实际 API 契约（裸 JSON 字符串）不一致 → **scenario 断言设计漂移，非代码缺陷**。

### 修改的文件
无（design_drift 路径，不改代码；scenario-scenario.yaml 断言修正已在 escalate 提交
265e8d5 完成，属于 SSOT 维护方，非本 agent 修改）

### 单元测试证据
N/A — 不改代码，未跑测试

### 修复策略
design_drift：不改源码，record `--fix-root-cause design_drift` + `--design-drift` JSON
（decision=ACCEPT）。所需人工/编排动作（重跑 scenario-execute 验证修正后的断言）已
由编排器在 escalate 阶段完成 yaml 修正，重跑后该场景应通过。
