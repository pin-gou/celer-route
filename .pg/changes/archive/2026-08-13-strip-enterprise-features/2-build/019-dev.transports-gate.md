# Gate Assessment: dev.transports

**Track**: dev.transports
**Change**: strip-enterprise-features
**Date**: 2026-08-13
**Cycle**: 3（014 verify→015 fix / 016 verify→017 fix->018 verify→本 gate）

## 整体判定

PASS — gate-score: 92.5 ≥ 80，无 P0 失败

## 检查项

| # | 检查项 | 判定 | 证据来源 |
|---|--------|------|---------|
| 1 | V-* 验证项通过率（V-transports-1 degraded-pre-existing / V-transports-2 PASS） | ✅ | report: Design Comparison + Evidence |
| 2 | design.md 一致性：12.x 全部 9 项任务对应删除均已在代码确认 | ✅ | 步骤 2 结构化比对 |
| 3 | 无 scope creep：31 个文件全在 transports/ 模块根内，连带修改均为 enterprise 引用删除 | ✅ | 步骤 3: git diff --name-only + 逐文件 diff |
| 4 | 文件位置合规：所有变更文件在 transports/ 或 .pg/ 内 | ✅ | 步骤 3: git diff master...HEAD --name-only |
| 5 | Evidence 充分：V-transports-1/2 均有原始输出，degraded 有 pre-existing 豁免 | ✅ | report: Evidence 块逐项检查 |
| 6 | 旧测试无回归：预存失败 1（oauth drift）/ 新引入 0 | ✅ | 步骤 1.6: master 三线确认 |
| 7 | 证据可追溯（抽查）：2/2 项（ui.go 404 逻辑 / oauth drift 基线） | ✅ | 步骤 1.5 抽查 |
| 8 | 测试质量：正反路径覆盖、命名反映业务语义；WARNING: 部分删除路径无独立断言 | ⚠ | 步骤 5 |
| 9 | 实现完整性（v3.x）：硬标记扫描 0 命中、tasks-V 映射 10/10 完整 | ✅ | git diff master...HEAD grep |
| 10 | runtime_boot（B3）：启动/就绪/e2e 证据齐，失败处置由 015/017 fix 覆盖 | ✅ | report 14.3 + evidence Step 1/6 + fix 报告 |

## 维度分与加权评分

| 维度 | 权重 | 维度分 | 加权分 |
|------|------|--------|--------|
| V-* 验证项通过率 | 50% | 95 | 47.50 |
| design.md 一致性 | 20% | 100 | 20.00 |
| scope creep 检查 | 15% | 100 | 15.00 |
| 测试质量 | 0%（v3.x 并入实现完整性） | 95 | 0.00 |
| 实现完整性（v3.x） | 10% | 100 | 10.00 |
| **总计** | **95%** | | **92.50** |

**gate-score: 92.50**（PASS 条件 ≥ 80）
**P0 失败: 无**

## 不通过项详细说明

（无 FAIL 项）

## 各维度详细说明

### 1. V-* 验证项通过率 (50% × 95 = 47.50)

| ID | 预期 | 实际 | 判定 |
|----|------|------|------|
| V-transports-1 | config.schema.json 同步校验通过 | ⚠️ DEGRADED — TestConfigSchemaSync FAIL（5 个 oauth_* 字段 drift） | ✅ PASS (degraded-pre-existing) |
| V-transports-2 | enterprise HTTP 端点返回 404 | 全部 9 端点 404；happy-path /health 200、/api/governance/virtual-keys 200 | ✅ PASS |

**V-transports-1 pre-existing 豁免（三线确证）**：
1. `git show master:core/schemas/mcp.go | grep -c oauth` = 37（master 基线已有 oauth 字段）
2. `git show master:transports/config.schema.json | grep -c oauth_authorize_url` = 0（master 基线 schema 同样缺失）
3. 本分支 mcp.go oauth 计数 37 == master，config.schema.json oauth 计数 0 == master —— 本变更未新增/未移除任何 oauth 字段，drift 与 strip-enterprise-features 完全无关
4. 017 fix 报告确认同一测试在 fork commit `60fc62c` 上同样失败

**V-transports-2 判定依据**：verify evidence Step 6 提供 9 端点原始 curl 输出（全部 404）+ verify 报告冒烟表（10 项含 happy-path）。根因（ui.go SPA catch-all 返回 200）经 017 fix 修复，本 gate 已抽查 ui.go diff：`+10` 行，`/api/` 前缀直接返回 fasthttp.StatusNotFound。

### 2. design.md 一致性 (20% × 100 = 20.00)

**结构化期望提取（对照 tasks.md 12.1-12.10 与 design.md 后端架构段）**：

| 期望项 | 实际代码 | 匹配 |
|--------|---------|------|
| 12.1 删除 server.go `enterprisePlugins` 列表 + `slices.Contains` 跳过 | server.go/plugins.go diff：`-var enterprisePlugins = []string{"datadog","bigquery","pubsub","kafka"}` + `-slices.Contains(enterprisePlugins, cfg.Name)`；grep 全模块 0 命中 | ✅ |
| 12.2 删除 router.go `LargePayloadHook`/`LargeResponseHook` 类型/字段/方法 | grep `LargePayloadHook\|LargeResponseHook` 生产代码 0 命中 | ✅ |
| 12.3 删除 handlers/integrations.go `SetLargePayloadHook`/`SetLargeResponseHook` | diff 确认方法整段删除（-23 行） | ✅ |
| 12.4 删除 lib/config.go `IsEnterprise` 字段、`initVault`、enterprise 注释 | grep `Config.IsEnterprise` 0 命中（仅保留 design 声明的 `BifrostContextKeyIsEnterprise` 运行时读取，config.go:1726）；`initVault` 0 命中 | ✅ |
| 12.5 删除 config.schema.json 9 顶层 + 6 嵌套字段 | diff 确认 alerting/access_profiles/audit_logs/cluster_config/guardrails_config/large_payload_optimization/load_balancer_config/scim_config/circuit_breaker_config + business_units/tool_groups/kafka/access_profile_id 及对应 $defs 全部删除 | ✅ |
| 12.6 删除 config_test.go `enterpriseSchemaPaths`/`enterpriseSchemaFields` | grep 0 命中 | ✅ |
| 12.7 utils_test.go enterprise 表名清理 | grep `enterprise_access_profiles\|enterprise_users` 0 命中 | ✅ |
| 12.8 plugins_test.go `enterprise-governance` 期望删除 | grep 0 命中 | ✅ |
| 12.9 governanceroutes_test.go `"enterprise"` 覆盖期望删除 | grep 0 命中 | ✅ |
| design 风险 9：middlewares.go SCIM OAuth 公开路径未实现 → 404 | middlewares.go:982-985 仍有 `/api/scim/oauth/*` 白名单声明但无 handler 注册，curl 404，行为与设计一致 | ✅ |

**diff 摘要**：期望 10 项 / 实际匹配 10 项 / 缺失 0 项

### 3. Scope Creep 检查 (15% × 100 = 15.00)

**变更文件清单**（git diff master...HEAD，135 文件全仓 / 31 个 transports 文件）：
```
transports/bifrost-http/ 生产 17 文件 + 测试 13 文件 + config.schema.json
1234567890abcdefghijklmnopqrstuvwxyz
```

**逐文件分析**：
- **tasks.md 12.x 声明内**：server.go、plugins.go、integrations.go、router.go、config.go、config_test.go、utils_test.go、plugins_test.go、governanceroutes_test.go、config.schema.json（10 文件）
- **015 fix 连带引用清理**（enterprise key 删除后的编译依赖传播，非新范围）：lib.go、integrations/utils.go、integrations/genai.go、handlers/mcpserver.go、middlewares.go、logging.go、wsrealtime.go、webrtc_realtime.go、mcpsessions.go、realtime_client_secrets.go、mcpoauth2jwt.go、featureflags.go、ui.go（+ 对应 _test.go）
- 逐文件 diff 确认：全部为删除对已删 `BifrostContextKey*` 的引用 / 注释清理 / no-op 语义简化。无新增 if-else 业务逻辑、无数据流变化
- **ui.go +10 行**：017 fix 的 `/api/` 404 处理，V-transports-2 直接修复，设计内
- **config.schema.json 3927 行**（2074+/1853-）：$defs 定义块删除 + 缩进重排（4 空格→2 空格），字段级别删除与 design 完全一致。**WARNING**（不 FAIL）：单文件变更超 200 行，但已抽查核心删除字段，属合理大规模删除
- **前 15 fix 轮次对 plugins/core 的连带**不在本 track diff 范围判定内（已由 005 core gate / 010 framework gate 审查）

**修改行数**：所有 transports 生产文件单文件 ≤ 100 行（除 schema），无异常超大改动
**结论**：无 scope creep

### 4. 文件位置合规 (检查项 #4)

`git diff master...HEAD --name-only`：transports 31 文件全部在 `transports/` 模块根内。track.modules=['transports']，允许目录前缀 `transports`。✅ 100% 合规

### 5. 测试质量 (0% — 已并入实现完整性维度)

**抽查 mcpserver_auth_test.go**（017 fix 改写断言）：
- 正反路径：headers 模式"user identity resolution is enterprise-only in OSS"断言 `require.Error` + `assert.Contains(err, "virtual key required")`；"user JWT with no dashboard session is accepted in OSS"断言 `require.NoError` + `assert.Equal(h.globalMCPServer, res.mcpServer)` ✅
- 命名反映业务语义（非 `should return false` 式）✅
- 断言均为精确值比较（require/assert + Contains/Equal），无非条件断言 ✅
- **WARNING**：部分删除路径（如 wsrealtime.go 身份继承删除、logging.go createdBy 置空）的测试仅验证"删除后不 panic"或未知断言，未独立验证 OSS 空值语义；不阻塞（0% 权重 + WARNING 不触发 FAIL）

### 6. 实现完整性 (v3.x, 10% × 100 = 10.00)

**硬标记扫描**：
```bash
$ git diff master...HEAD > /tmp/opencode/sfe.diff  # 17864 行
$ grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException|panic\("not implemented"\)' /tmp/opencode/sfe.diff \
    | grep -vE '^[-+][-+](a|b)$' | grep -E '^\+' | grep -v '\[skip-review\]' | grep -v '_test\.go'
# 输出：空（0 命中）
```
✅ 无 TODO/FIXME/XXX/stub/not implemented 新增，无豁免

**tasks-V 映射核查**：
| tasks.md 任务 | 对应代码变更 | 状态 |
|--------------|------------|------|
| 12.1 enterprisePlugins 列表 + slices.Contains | server.go/plugins.go 删除确认 | ✅ |
| 12.2 router.go LargePayloadHook/LargeResponseHook | 删除确认 | ✅ |
| 12.3 integrations.go Set*Hook 调用 | 删除确认 | ✅ |
| 12.4 config.go 注释/initVault/IsEnterprise | 删除确认 | ✅ |
| 12.5 config.schema.json 9+6 字段 | 删除确认 | ✅ |
| 12.6 config_test.go enterpriseSchemaPaths/Fields | 删除确认 | ✅ |
| 12.7 utils_test.go 表名 | 删除确认 | ✅ |
| 12.8 plugins_test.go enterprise-governance | 删除确认 | ✅ |
| 12.9 governanceroutes_test.go enterprise | 删除确认 | ✅ |
| 12.10 build + vet | verify 14.1 go vet 0 警告 + 14.2 go test ok | ✅ |

10/10 映射完整，无缺失。额外清理（015 fix 的 lib/integrations/handlers 引用传播）与 design.md 总设计意图一致。

### 7. Evidence 充分性 (检查项 #5)

- V-transports-1：原始 FAIL 输出 + 5 字段列表 + 豁免理由（pre-existing 三线确证）✅
- V-transports-2：9 端点原始 curl 每行输出 + happy-path sanity + 冒烟表 ✅
- 14.1 lint：`go vet ./...` 原始输出（空=成功）+ EXIT_CODE: 0 ✅
- 14.2 测试：handlers 22.120s ok / integrations 0.230s ok 原始输出 ✅
- 14.3 服务启动：pg-invoke-hook start 原始输出（Waiting → ready on 9080）✅

### 8. 回归分析校验 (检查项 #6)

- **预存失败：1 个**（TestConfigSchemaSync oauth drift）——master 三线确证与本变更无关
- **新引入 FAIL：0 个**（transports 相关所有测试包 handlers/integrations 均 ok，extraneous FAIL 无）

### 9. 证据可追溯性抽查 (检查项 #7)

抽查 2 项：
1. **V-transports-2 curl 404 证据** → 反向追溯：ui.go serveDashboard `/api/` 前缀 `ctx.SetStatusCode(fasthttp.StatusNotFound)` 逻辑存在（diff +10 行）；middlewares.go:982-985 SCIM OAuth 白名单无 handler 注册，404 行为成立 ✅
2. **V-transports-1 失败证据** → 反向追溯：master `core/schemas/mcp.go` 37 处 oauth 字段 vs master `transports/config.schema.json` 0 处 —— drift 在基线真实存在，证据链完整 ✅

**结论：2/2 项证据可追溯**

### 10. runtime_boot (B3 修复，P0 硬约束)

| 步骤 | 证据 | 结果 |
|------|------|------|
| 1. 启动服务 | evidence Step 1：pg-invoke-hook start → `[32mbifrost-api is ready on port 9080[0m` + `{"ok": true, "exit_code": 0}`；netstat 当前确认 9080 LISTEN | ✅ |
| 2. 等待就绪 | Step 1：`Waiting for bifrost-api to be ready on port 9080...` → ready 输出 | ✅ |
| 3. 真实 e2e | Step 6：9 端点 + /health + virtual-keys 真实 curl 实测（打真实运行服务） | ✅ |
| 4. 失败处置 | 本轮启动成功无失败；失败场景（plugins 编译失败 014→015、404 根因 016→017）由 fix 报告完整记录并在 018 验证闭环 | ✅ |

装配验证充分：服务真实启动（ready on 9080）且所有端点经真实 HTTP 请求验证，无"代码存在但装配失败"风险。

## 实现完整性核查段

### grep 输出

```bash
$ git diff master...HEAD | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException|panic\("not implemented"\)' \
    | grep -E '^\+' | grep -v '\[skip-review\]' | grep -v '_test\.go'
(no output — 0 命中，全部 catch 为已删除行的注释/文档文本)
```

### 豁免列表

无豁免。

### tasks-V 映射核查结论

dev.transports:dev 全部 10 个任务（12.1-12.10）已完成代码映射，0 缺失。额外清理文件属 015 fix 的编译依赖传播（enterprise key 删除后引用同步删除），与 design.md 总设计意图一致，非 stub/占位实现。

## 整体判定

**PASS** — gate-score: 92.50 ≥ 80，无 P0 失败（V-* 无 FAIL、实现完整性通过、runtime_boot 4 步证据齐）
