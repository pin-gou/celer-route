# celer-route + PostgreSQL 数据库密码
# 复制此文件为 .env 并按需修改

POSTGRES_USER=celer-route
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=celer_route

# 可选：自定义 postgres 镜像版本（默认 16-alpine）
# POSTGRES_IMAGE_TAG=16-alpine

# 可选：celer-route 镜像版本
# CELER_ROUTE_IMAGE_TAG=latest