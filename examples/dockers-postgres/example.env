# pg-gateway + PostgreSQL 数据库密码
# 复制此文件为 .env 并按需修改

POSTGRES_USER=pg-gateway
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=pg_gateway

# 可选：自定义 postgres 镜像版本（默认 16-alpine）
# POSTGRES_IMAGE_TAG=16-alpine

# 可选：pg-gateway 镜像版本
# PG_GATEWAY_IMAGE_TAG=latest